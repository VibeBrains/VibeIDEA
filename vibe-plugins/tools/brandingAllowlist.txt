# Законные упоминания чужого бренда — по строке на случай, каждая с причиной.
#
# Формат: <путь>|<подстрока>|<причина>
# Гейт падает и на упоминании вне списка, и на записи, которая больше ничего не находит:
# устаревшее исключение — такой же долг, как забытое упоминание.

vibe-plugins/vibe-agent/resources/lang/base.json|JetBrains|Описываем ЧУЖОЙ баннер, который выключаем по умолчанию. Назвать его иначе нельзя — пользователь ищет глазами именно это слово.
vibe-plugins/vibe-agent/resources/lang/en.json|JetBrains|Тот же текст в переводе: строка обязана совпадать с базовой по смыслу.
vibe-plugins/vibe-agent/src/com/vibe/agent/acp/AcpConfig.kt|JetBrains|Путь ~/.jetbrains/acp.json — общий реестр ACP-агентов. Свой путь = потеря совместимости с уже настроенными агентами.
vibe-plugins/vibe-agent/src/com/vibe/agent/settings/VibePromoSettings.kt|JetBrains|Комментарий объясняет, ЧТО именно мы гасим; без имени продукта объяснение бессмысленно.
vibe-plugins/deps/download.sh|JetBrains|Сказано, что LSP4IJ берётся с GitHub-релиза, а НЕ с площадки JetBrains — это про происхождение зависимости.
