# Входящий HTTP API VibeIDEA — спецификация

> **Как этим пользоваться.** Скопируйте весь этот файл целиком своей LLM и попросите:
> *«По этой спецификации напиши мне <скрипт для CI / шаг GitHub Actions / крон-задачу>, который
> запускает агента VibeIDEA с задачей <…> и продолжает ту же сессию на следующем шаге».*
> Это самодостаточный документ: всё, что нужно для корректного вызова, — ниже.

---

## 1. Что это

VibeIDEA умеет принимать задачи для агента по HTTP: «дёрнуть агента из CI, бота или крона».
Каждый вызов запускает агента в открытой IDE и возвращает `sessionId`, по которому следующий
вызов продолжает **ту же** сессию.

**Это исполнение кода на компьютере владельца.** Поэтому:

- сервер слушает **только** `127.0.0.1` — никогда внешний адрес;
- каждый запрос требует токен;
- по умолчанию всё выключено.

## 2. Включение

1. **Settings → Tools → VibeIDEA → Агент → «Входящий HTTP API»** — включить. Применяется сразу,
   перезапуск IDE не нужен.
2. Там же «Порт»: `0` (по умолчанию) означает «любой свободный»; выбранный порт пишется в журнал
   IDE и показывается вместе с токеном.
3. Токен создаётся сам при первом включении и хранится в защищённом хранилище ОС (Keychain) —
   **не** в настройках, потому что настройки синхронизируются между машинами и попадают в
   демонстрацию экрана. Посмотреть: действие **«VibeIDEA: показать токен HTTP API»**
   (Help → Find Action). Утёк — **«VibeIDEA: перевыпустить токен HTTP API»**: старый умирает
   немедленно.

IDE должна быть запущена: задачу выполняет агент в открытом окне.

## 3. Аутентификация

Каждый запрос обязан нести заголовок:

```
Authorization: Bearer <токен>
```

Слово `Bearer` регистронезависимо, сам токен — нет.

Дополнительно проверяется заголовок `Host`: он обязан указывать на эту машину
(`localhost`, `127.0.0.1`, `[::1]`, с портом или без). Это защита от DNS rebinding — приёма, при
котором открытая владельцем веб-страница резолвит собственный домен в `127.0.0.1` и обращается к
вашему порту из браузера. Обычные клиенты (`curl`, `fetch` из скрипта) ставят правильный `Host`
сами, ничего делать не нужно.

## 4. Методы

### `GET /health`

Проверка живости. Ответ `200`:

```json
{ "ok": true, "version": 1 }
```

### `POST /run`

Запуск задачи. Тело — JSON, не больше **1 000 000 байт**.

| Поле | Тип | Обязательное | Смысл |
|---|---|---|---|
| `task` | строка | да | Что сделать. Пустая строка и пробелы отклоняются. |
| `sessionId` | строка | нет | Продолжить эту сессию. Без поля — новая сессия. |
| `wait` | булево | нет | `true` — ответить только когда прогон закончится. По умолчанию `false`. |

Ответ `200`:

```json
{ "sessionId": "<id>", "status": "started" }
```

При `wait: true` — `{"sessionId": "<id>", "status": "completed"}`.
При ошибке — код `500` и `{"sessionId": "...", "status": "failed", "error": "<причина>"}`.

**Неизвестный `sessionId` не является ошибкой:** начнётся новая сессия, её id придёт в ответе.
Сессия могла быть очищена пользователем, и отказ оставил бы конвейер без выхода.

## 5. Коды ответов

| Код | Когда |
|---|---|
| `200` | Принято / выполнено |
| `400` | Тело не JSON, не объект, или `task` пуст |
| `401` | Нет заголовка `Authorization`, не `Bearer`, или неверный токен |
| `403` | Запрос не с этой машины, либо недопустимый `Host` |
| `404` | Известны только `GET /health` и `POST /run` |
| `413` | Тело больше миллиона байт |
| `500` | Прогон завершился ошибкой |
| `503` | API включён, но токен не настроен |

## 6. Примеры

Запустить задачу:

```bash
curl -sS -X POST http://127.0.0.1:7391/run \
  -H "Authorization: Bearer $VIBE_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"task":"прогони тесты и почини падения"}'
```

Продолжить ту же сессию следующим шагом конвейера:

```bash
SESSION=$(curl -sS -X POST http://127.0.0.1:7391/run \
  -H "Authorization: Bearer $VIBE_TOKEN" -H "Content-Type: application/json" \
  -d '{"task":"собери проект"}' | jq -r .sessionId)

curl -sS -X POST http://127.0.0.1:7391/run \
  -H "Authorization: Bearer $VIBE_TOKEN" -H "Content-Type: application/json" \
  -d "{\"task\":\"опиши, что изменилось\",\"sessionId\":\"$SESSION\",\"wait\":true}"
```

## 7. Чего API намеренно не делает

- **Не работает по сети.** Слушается только петлевой адрес; настройки «слушать снаружи» нет и не
  будет — она превратила бы исполнение кода на личной машине в сетевую услугу.
- **Не отдаёт токен по HTTP.** Токен выдаётся только в интерфейсе IDE.
- **Не запускает агента без открытой IDE.** Прогон выполняет окно; если ни одно окно не ответило
  за десять минут, вызывающий получает `failed` с явной причиной, а не молчание.
- **Не отвечает браузеру.** CORS-заголовков нет: страница может отправить запрос, но прочитать
  ответ ей не дадут.

## 8. Отличия реализации VibeIDEA

Контракт (пути, коды, поля, заголовки) перенесён дословно — скрипты, написанные для VibeIDE,
работают без правок. Разница только в том, чего в VibeIDE нет:

- **Куда попадает задача.** Выполняет её панель VibeAgent в открытом окне. Если окон с панелью
  несколько, `sessionId` решает: продолжение разговора уходит в тот проект, которому этот тред
  принадлежит; задача без `sessionId` — в окно, работавшее последним.
- **Что такое `sessionId`.** Это id треда чата: он переживает перезапуск IDE, виден в истории и
  открывается вкладкой, когда приходит задача.
- **Ход идёт тем же путём, что и набранный руками** — очередь, предохранители, хуки, VERIFY-GATE.
  Автоматический вызывающий не получает ослабленных гарантий по сравнению с человеком.
- **Один ход за раз.** Второй запрос, пришедший во время работы агента, встаёт в очередь панели,
  а не отклоняется.
