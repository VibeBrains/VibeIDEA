# Живая проверка: что подтверждается только руками

Тесты и гейты закрывают логику, но не видят того, что человек видит и нажимает: всплывашку под
мышью, вставку из чужого приложения, машину с другим менеджером Node, корпоративный прокси. Этот
файл — очередь таких проверок.

Пункт живёт здесь, пока его не проверили руками. Проверенный вычёркивается из файла, а итог
записывается строкой в [roadmap](../roadmap.md): «проверено руками, дата». Не прошёл — пункт
остаётся, рядом строка с тем, что увидели, и задача в roadmap.

Регламент на каждый выпуск — отдельно: [acpSmoke.md](acpSmoke.md). Здесь — разовые проверки новых
возможностей, по сборкам.

## Как проверять

1. Образ — из комплекта рядом с репозиторием (`../VibeIDEA-release-X.Y.Z`), а не из `out/`.
2. Открыть проект, в котором есть `.vibe/`: сиды засеваются при открытии.
3. Не прошло — номер пункта и строка из ленты или снимок экрана: этого достаточно.

## Сборка после 0.7.0 (`next`, 23.09.2026)

| # | Что делаем | Что должно быть |
|---|---|---|
| 1 | Вставить в композер картинку из буфера: снимок экрана, картинка из браузера, картинка из мессенджера | Во всех трёх случаях появляется вложение-картинка; текст, лежащий в буфере рядом с ней, не вставляется вместо неё |
| 2 | Сузить панель агента до минимума во время хода | Строка состояния идёт своей строкой над полем ввода и не наезжает на шапку |
| 3 | Settings → Языковые серверы на машине, где Node стоит через fnm: «Проверить», затем «Установить последнее» | Проверка и установка идут в фоне той нодой, которой IDE запускает серверы; интерфейс не замирает |
| 4 | При открытом `.ts` и запущенных серверах переключить ветку (`git checkout`) | Серверы перезапускаются с прогрессом, подсказки и ошибки — по новой ветке |
| 5 | Windows: открыть страницу провайдеров после обновления со сборки 0.6.x | Ключи прежних сборок видны. Не видны — это задача переноса ключей из roadmap, пункт не вычёркивается |
| 6 | У подключённого MCP-сервера поменять описание инструмента и спросить агента | Перед первым вызовом — подтверждение изменившегося инструмента с отпечатком |
| 7 | Провайдер моделей за корпоративным прокси, сертификат прокси добавлен в настройках IDE | Запрос к модели проходит; без сертификата — внятная ошибка, а не зависание |
| 8 | Навести мышь с Cmd (Ctrl) на вызов функции в `.ts`, затем на метод класса в `.php` | Подсказка — сигнатура: `function …(…): …`; у PHP без строки `<?php`. Первое наведение на холодном сервере может показать «символ TypeScript» — второе обязано показать сигнатуру |
| 9 | Ключ `vibe.lsp.precise.navigation` не трогать — он включён по умолчанию; Cmd+Click и Cmd+B по импортированному имени, по методу, по имени из `node_modules` | Переход ведёт на объявление; при нескольких объявлениях список различает их по файлу и строке; подсказка — сигнатура |
| 10 | С ключом по умолчанию и выключенным (Help → Find Action → Registry…) навести с Cmd на ключевое слово, пробел, строку, комментарий | Ничего из этого не подчёркивается. Если без ключа подчёркивается всё подряд — записать, на чём: причина тогда не в обработчике перехода LSP4IJ (разбор — [lspNavigation.md](../knowledge/languages/lspNavigation.md)) |
| 11 | Спросить агента «откуда пришло `<имя>`» про импортированное имя в `.ts` | Ответ начинается строкой «точно, по языковому серверу …», дальше `путь:строка [LSP]` и сигнатура |
| 12 | То же про имя в `.kt` или `.md` | Первая строка — «разбор текстовый: языковой сервер этот файл не обслуживает», дальше текстовая цепочка |
| 13 | Пайплайн из примера «Волны» в [pipelinesSpec.md](pipelinesSpec.md) на проекте с каталогами `server/` и `web/` | После плана в ленте два блока «Шаг 2/5 [backend-dev]» и «Шаг 3/5 [frontend-dev]», текст идёт в оба одновременно и не перемешивается; ревью получает резюме обоих шагов |
| 14 | Во время волны нажать «Стоп» | Останавливаются оба шага волны, дальше прогон не идёт |
| 15 | Волна из двух шагов, которым агент задаёт вопросы о разрешении | Диалоги приходят по одному, в каждом назван шаг: «Шаг 2/5 [backend-dev] спрашивает: …» |
| 16 | Закрыть IDE посреди волны, когда один её шаг уже закончился, открыть и запустить тот же пайплайн | Предлагается продолжить; закончившийся шаг волны не выполняется второй раз |
| 17 | Во время волны переключиться на другую вкладку чата и вернуться | Блоки незакончившихся шагов на месте и продолжают наполняться |
| 18 | В пайплайне два пишущих шага одной волны без `paths` | Пайплайн пропущен с причиной, где названы оба шага; остальные пайплайны файла доступны |
| 19 | Прямой чат с Claude Opus 5.5 (ключ Anthropic), глубина рассуждений «выключено», вопрос посложнее | В блоке рассуждения видна мысль (не пустой блок); в логе IDE запрос несёт `output_config.effort: "low"`, ответа 400 нет |
| 20 | Там же — задача с инструментами прямого чата на несколько вызовов | Между вызовами видны короткие заметки модели в блоке рассуждения, а не тишина |
| 21 | Прямой чат с GPT-6 Sol или Luna без `protocol` в записи (chat/completions), инструменты включены, глубина «low» или выше | Инструменты вызываются, ответа 400 нет; один раз строка в ленте, что ходы с инструментами идут без рассуждения, и как получить и то и другое |
| 22 | Прямой чат с MiMo v2.6 Pro, задача на два-три раунда инструментов | Второй раунд проходит (рассуждение уходит обратно), отчёт о расходе показывает деньги по ценам вендора |
| 23 | Модели Claude из каталога вендора (без записей в `providers.json`) — длинный ответ | Ответ не обрывается на ~8 тысячах токенов; если оборвался — строка «[обрыв] Ответ упёрся в лимит вывода…» |
| 24 | Спросить модель так, чтобы сработал фильтр вендора (или дождаться отказа в работе) | Строка «[отказ] … категория …» в ленте, а не пустой пузырь |
| 25 | Каталог ACP-агентов → добавить агента, который ставится через `npx` | В `~/.jetbrains/acp.json` команда без `-y`; агент запускается и отвечает |
| 26 | Включить запись `kimi` в `.vibe/agents.json` (Kimi Code CLI установлен, вход выполнен) | Агент Kimi запускается по ACP и отвечает; без входа — понятная ошибка «auth required» |
| 27 | Запись GPT-6 Sol с `"protocol": "openai-responses"` (образец в сиде `openai.jsonc`), инструменты включены, глубина «medium», задача на два-три раунда инструментов | Второй и третий раунды проходят без 400; в блоке рассуждения — изложение, если вендор его прислал; отчёт о расходе показывает деньги, запись в кэш — отдельной статьёй |
| 28 | GPT-6 Astra: сперва запись без `protocol`, потом с `"protocol": "openai-responses"` | Без — строка в ленте, что инструменты у модели только через Responses API, ход идёт без них; с — инструменты вызываются |
| 29 | OpenCode Go: модель `grok-4.6` или `gpt-5.6-luna` из сида | Ответ приходит: запрос идёт на `/v1/responses`, а не получает 404 от `/chat/completions` |
| 30 | MiMo или Kimi по Anthropic-маршруту (раскомментировать блок в сиде), задача на два-три раунда инструментов | Второй раунд проходит: блоки `thinking` уходят обратно |
| 31 | Claude Opus 5 или Sonnet 5, глубина «medium», задача с инструментами на несколько вызовов | Ответа 400 нет; мысль после вызова продолжает прежнюю, а не начинает заново |
| 32 | Каталог ACP-агентов: выбрать агента в списке | Под описанием ссылки «Текст лицензии», «Репозиторий», «Сайт» открываются в браузере |
| 33 | В `.vibe/agents.json` запись с `"registry": {"id": "<id агента в реестре>", "version": "…"}` под своим именем | Каталог не предлагает этого агента к добавлению; при новой версии в реестре строка обновления называет и `registry.version` |
| 34 | Модель по умолчанию — Opus 5.5 с `"extraBody": {"temperature": 0.3}`; открыть диагностику | Строка «Мышление модели» с `output_config.effort`; строка «Поля extraBody» называет `temperature` — модель отвергает выборку |
| 35 | Открыть любой `.md` проекта, контекстное меню редактора → «Нейрослоп в тексте»; затем выделить абзац и повторить | Список находок, самые тяжёлые сверху; выбор находки выделяет найденный кусок в редакторе, и при выделении — ровно в том месте документа; на чистом тексте — подсказка со счётом и ничего больше |
| 36 | Settings → Tools → VibeIDEA → Агент → «Нейрослоп в тексте»: `notify`; попросить агента написать README со штампами («раскрывает потенциал», «это не просто X — это Y») | После хода строка в ленте с файлом, счётом и правилами; на чистом тексте — тишина |
| 37 | То же в режиме `enforce`, две попытки | Агент возвращается к тексту с находками и двумя ограничениями правки; после второй неудачи — строка «решаете вы», ход не зацикливается |
| 38 | В `.vibe/slop.json` записать `"disable": ["RU-W1"]` и сломанное правило без `kind`; повторить п. 35 | Находок `RU-W1` нет; внизу списка — предупреждение о правиле без вида, остальное работает |
| 39 | Дизайн-детектор: страница с заголовком «Бесшовная платформа выводит работу на новый уровень» | Находка `copy-slop` на заголовке, одна на элемент, вкусовая (пол не пробивает) |
| 40 | Settings → Tools → VibeIDEA → Агент: задать «Прокси для запросов к моделям», в «Напрямую, мимо прокси» отметить MiniMax; спросить модель MiniMax и модель DeepSeek | Оба отвечают; в журнале прокси виден только запрос DeepSeek |
| 41 | Там же при заданном прокси запустить внешнего агента Claude, затем отметить его «мимо прокси» и перезапустить | Журнал агента: сначала «прокси: агент ходит через …» с паролем под `***`, после отметки — «агент ходит напрямую» |
| 42 | Модель `minimax-m3` провайдера OpenCode Go (протокол anthropic) | Отвечает; раньше шлюз отвечал «Missing API key» |
| 43 | Провайдер на `http://[::1]:11434/v1` или `http://0.0.0.0:11434/v1` без ключа (Ollama) | Модели видны, чат помечен «локальная модель» |
| 44 | Нейрослоп: в `.vibe/slop.json` правило `{"id":"X","lang":"any","kind":"regex","severity":"minor","name":"X","fix":"-","patterns":["((a+)+)+b"]}`, файл со строкой из 40 букв `a` и `!`, действие «Нейрослоп в тексте» | IDE не замирает; через ~5 с итог называет правило X пропущенным |
| 45 | Провайдер на `http://localhost:4000/v1` (LiteLLM или другой прокси к облаку) с `"runsLocally": false` и без ключа: спросить модель, затем включить офлайн-режим и спросить снова | Ответ приходит без метки «[локальная модель]»; в офлайн-режиме — строка «офлайн-режим включён: запрос к провайдеру … не отправлен» |
| 46 | «VibeIDEA: диагностика», когда единственный провайдер — сервер в сети с `"auth": "none"` без ключа | Строка «Провайдеры» без предупреждения (раньше — «нет пригодного»); «локальных» — только провайдеры с `"runsLocally": true` или на адресе этой машины |
| 47 | Свежая установка: спросить модель провайдера «почему компонент React перерисовывается» | Ответ короткий, без «Конечно!» и пересказа шагов; код и имена API дословно |
| 48 | Кнопка быстрых настроек в ряду композера: «Сжатые ответы» → «Выключены», повторить вопрос п. 47; затем «Ультра» | Без режима — обычный развёрнутый ответ; на «Ультра» — самый короткий; страница «Агент» показывает тот же уровень |
| 49 | Внешний агент Claude: два вопроса подряд, затем сменить уровень в меню и спросить третий | Стиль держится на втором вопросе без повтора инструкции; после смены агент отвечает в новом уровне |
| 50 | Меню быстрых настроек с клавиатуры: открыть, набрать «офл», Enter | Офлайн-режим переключается; «Все настройки агента…» открывает страницу «Агент» |
| 51 | MiniMax M3 (провайдер `minimax`, провод openai), задача в три вызова инструментов подряд | Ход доходит до конца без «пустого ответа» и без 400; в журнале запроса у прошлых ответов `content` начинается с `<think>` |
| 52 | Kimi K2.7 через Anthropic-совместимый адрес: два вопроса подряд без инструментов, затем с инструментами | Второй и третий запросы без 400; прошлые ответы несут блок `thinking` без `signature` |
| 53 | Цепочка запасных `anthropic/claude-opus-5-5,deepseek/deepseek-v4-pro`, вопрос, на который классификатор Claude отказывает | Строка об отказе с категорией, затем «перехожу к deepseek/…»; отказанный текст из ленты убран; в `/spend` есть строка за отказанный запрос |
| 54 | В `~/.vibe/providers.json` имя `"fast": "zai/glm-5.3"`, в проектном `providers/routes.jsonc` — `"fast": "minimax/MiniMax-M3"`; пайплайн с шагом `@fast` | Шаг идёт на `zai/glm-5.3`: ваш файл сильнее засеянного каталога |
| 55 | Модель Ollama (`runsLocally`) в прямом чате, «Сжатые ответы» на «полные», спросить что угодно; журнал запроса в логе IDE | Системное сообщение стиля начинается с уровня и `## Short`, разделов `## Rules` и `## Boundaries` нет; облачной модели уходит полный текст без `## Short` |
| 56 | У модели `anthropic/claude-opus-5-5` в `providers.json` — `"extraBody": { "tool_choice": { "type": "any" } }`, «VibeIDEA: диагностика» | Строка «Поля extraBody» называет `tool_choice.type` и причину «принудительный выбор инструмента, 400 на каждом запросе» |
| 57 | Агент Claude: попросить «найди файлы *.md в docs» (вызов Glob), в вопросе о разрешении и в карточке посмотреть подпись | Рядом с заголовком стоит имя `Glob`; у вызова `Bash` с заголовком `Bash` имя не повторяется |
| 58 | Claude Opus 5.5 в прямом чате: ход с двумя вызовами инструментов, затем второй вопрос; в строке состояния — чтение кэша | Второй ход читает из кэша и прошлый ход с инструментами (чтение кэша растёт, строки «префикс сломан» нет); ответа 400 про thinking нет |
| 59 | Тот же тред после перезапуска IDE: третий вопрос | Ответ без 400; если сжатие истории не срабатывало, чтение кэша снова покрывает прошлые ходы |
| 60 | Свой MCP-сервер ревизии 2026-07-28 из `.vibe/mcp.json`, чей инструмент отвечает `input_required` с `elicitation/create` (стенд VibeIDE `/Volumes/Storage/Caches/vibeide/mcp-mrtr-stub/server.mjs`; сам клиент на нём прогнан 25.09 — обе цепочки, `deploy_service` и `shed_load`, прошли, здесь проверяются диалог и потолок); ответить на форму дольше 30 с | Диалог уточнения с полями формы; после «ОК» модель получает результат повторного вызова, а не «инструмент не ответил»; отказ в диалоге уходит серверу как `decline` (стенд отвечает на него своим результатом) |
| 61 | У модели Anthropic в `providers.json` — `"extraBody": { "fallbacks": "default" }`, «VibeIDEA: диагностика» | Строка «Поля extraBody» называет `fallbacks` и объясняет, что IDE уходит на запасную цель сама |
| 62 | Прокси или агрегатор, отвечающий другой моделью; два хода, затем диагностика | Строка «Подмены модели»: пара «вместо X отвечала Y», число раз и дата |
| 63 | Цепочка запасных `anthropic/claude-fable-5-1,anthropic/claude-opus-4-8`, длинный разговор, вопрос, на который Fable отказывает | Ход уходит на Opus 4.8 без ошибки; в отчёте о расходе у повтора чтение кэша, а не запись с нуля |
| 64 | Claude Opus 5.5 в прямом чате, больше 20 инструментов (порог поиска); попросить задачу, для которой модель ищет инструмент, затем второй вопрос | Строки «префикс сломан» и «набор инструментов изменился» нет; ответа 400 про thinking или `tool_reference` нет |
| 65 | Тот же тред после перезапуска IDE: третий вопрос, требующий найденного инструмента | Модель вызывает его без повторного поиска; чтение кэша покрывает прошлые ходы |
| 66 | Навык `demo` в проекте, затем панель «Демо» | Прогон в списке сверху, отчёт открывается внутри IDE, видео `webm` проигрывается во встроенном браузере |

## Сборка 13.09.2026 (0.5.0) — не подтверждено с тех пор

Пункты восстановлены 23.09.2026 из журнала сессии: исходный файл жил во временной папке и пропал.
Если возможность с тех пор менялась, проверять по её нынешнему описанию в [functional.md](../functional.md).

| # | Что делаем | Что должно быть |
|---|---|---|
| 1 | Открыть `.vibe/roles.json` | Файл засеян; подсказка по схеме работает — например, `writePaths: 5` подчёркнуто как ошибка |
| 2 | Пайплайн из одного шага `{ "role": "qa", "task": "Добавь тест и поправь README" }` | Тест записан, запись в README отклонена |
| 3 | В `roles.json` поставить `"writePaths": []` и повторить п. 2 | В чате строка `[roles] roles.json: roles.qa.writePaths пуст…`, записей нет ни одной. Вернуть: удалить `roles.json` — он засеется снова |
| 4 | Пайплайн с `"roles": { "code-reviewer": { "model": "<провайдер>/<модель>" } }` и шагом `code-reviewer` без своей модели | Шаг идёт на указанной модели — видно в строке шага и в счёте. `"roles": { "backend-dev": { "model": "…" } }` — пайплайн пропущен с предупреждением о пишущей роли |
| 5 | У агента в `agents.json` — `"limits": { "costPerRun": 0.01, "currency": "USD" }`, запустить задачу подлиннее | Прогон остановлен с сообщением о потолке, в журнале расхода есть `runId` |
| 6 | `{ "id": "dyn", "dynamic": true, "steps": [{ "role": "orchestrator", "task": "Спланируй фичу X" }] }` | Оркестратор выдаёт план, открывается диалог с шагами, без согласия ничего не запускается; отказ — ни одного шага |
| 7 | Пайплайн из трёх шагов, остановить на втором | Предлагается продолжить со второго шага, а не с начала |
| 8 | Попросить ACP-агента «найди в памяти хендофф pipelines-contract» | Агент вызывает `memory_search` сервера `vibememory` и находит запись |
| 9 | Открыть TS-проект, где в зависимостях TypeScript 7 | Языковой сервер — TypeScript 7 проекта, а не vtsls (видно в диагностике) |
| 10 | Выбрать в композере модель провайдера, спросить «Найди в памяти поток pipelines-contract», затем «Сохрани в память запись-проверку» | Строка `🔧 memory_search` и ответ по найденной записи без диалога; на запись — диалог «Запись в общую память» с аргументами, «Нет» — модель говорит, что не разрешили. Галка инструментов снята в Settings → Tools → VibeIDEA → Агент — строк `🔧` нет |
| 11 | Проект с Ext JS после индексации: клик по `xtype: 'имя'`, `me.callParent(arguments)`, `this.getStore()`, `this.методРодителя()`, `fireEvent('имя')` | Открываются класс с `alias: 'widget.имя'`, тот же метод у родителя, ключ `store` в `config`, метод в родителе или его `mixins`, список слушателей события; `ext-all.js` среди целей не появляется |
| 12 | Модель провайдера: «где используется resolveExtends?», затем «запиши решение: …», затем «а что ты нашёл в прошлый раз?» | Строка `🔧 vibe_symbol_usages` со списком мест; диалог «Действие инструмента», «Нет» — решение не записано; ответ по результатам прошлого поиска, а не пересказ |
