# Правила проекта для агента

Этот файл агент получает по упоминанию `@agent` в чате (вместе с AGENTS.md,
если он есть в корне). Пишите сюда то, что агент обязан знать о проекте:
стек, команды сборки/тестов, что нельзя трогать, стиль.

<!-- Примеры — замените своими:
- Сборка: `npm run build`; тесты: `npm test`.
- Папку migrations/ не менять — только руками.
- Комментарии в коде — на английском.
-->
