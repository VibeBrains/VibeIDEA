# База знаний VibeIDEA

**Правило:** запись без строки в этой таблице не существует — её никто не найдёт. Добавил файл → добавь строку. Дублирующих индексов не заводить: «дублирующие списки одного множества расходятся по построению» (урок VibeIDE).

Формат записи: role-тег (`[правило]`/`[инструмент]`/`[архитектура]`/`[баг]`/`[инцидент]`/`[квирк]`) + Контекст / Суть / Применение.

## ui

| Файл | О чём |
|---|---|
| [editorScrollBar.md](ui/editorScrollBar.md) | [квирк] Скролл редактора обходит общую правку тонких скроллов: при включённой полосе разметки `EditorMarkupModelImpl` ставит ему свой UI мимо `JBScrollBar.createUI`, а ширину бегунка берёт из UI-свойства `Editor.scrollBarWidth`. Второй правки платформы не нужно — `UIInteger` читает `UIManager` на каждый запрос; применять надо в трёх точках (старт, смена настройки, смена ТЕМЫ — она пересоздаёт UIManager). Полностью тонким он не станет: в ширину входит полоса разметки. Грабля: `grep` без `-F` совпал с переименованным свойством, и первая версия гейта подсадку пропустила. |
| [settingsScroll.md](ui/settingsScroll.md) | [баг] `Configurable.NoScroll` = «скролл делаю сам»: без маркера платформа добавляет второй скролл и горизонтальную полосу по ширине html-подсказок (Провайдеры/Модели, 24.08), с маркером но без своего `JBScrollPane(TracksViewportWidthPanel(...))` страницу обрезает по краю окна (Агент, 28.08 — нашёл владелец). Почему на это нет юнит-теста: тестовый модуль намеренно без платформенных зависимостей. |
| [swingChatLayout.md](ui/swingChatLayout.md) | [баг] BoxLayout-лента без maxHeight у строк растягивает сообщения на весь вьюпорт; пузырь в BorderLayout.CENTER — на всю ширину. ChatRow с maxHeight=preferred + якорь NORTH. Урок: UI не отдаётся без визуальной проверки. |
| [focuslessListPopup.md](ui/focuslessListPopup.md) | [архитектура] Меню `@` под JTextArea без потери фокуса: `PopupChooserBuilder.setRequestFocus(false)` (паттерн FileTextFieldImpl); клавиши — ТОЛЬКО локальные шорткаты `registerCustomShortcutSet`: KeyListener/InputMap на JTextComponent не видят стрелки/Backspace/Esc — их раньше забирает IdeKeyEventDispatcher через keymap (`EditorBackSpace` и т.п. включены на любом текстовом поле); `showUnderneathOf` не флипает вверх — место считать самим; ACP-блоки контекста и режимы. |
| [settingsPageWidth.md](ui/settingsPageWidth.md) | [грабли+правило] Страница настроек едет вбок: длинная подсказка `JBLabel` сообщает ширину в одну строку, и `JScrollPane` добавляет горизонтальную прокрутку. Способ один — `VibeScroll.pane(TracksViewportWidthPanel(...))` плюс `<html>` вокруг подсказки. Возвращалось трижды, поэтому гейт `checkVibeUi.sh` требует вызова у каждой нашей `Configurable`; гейт ищет ВЫЗОВ, а не имя — на имени он оставался зелёным при снятой обёртке.  Дополнено 19.09.2026 — ПЯТЫЙ возврат: гейт был зелёный, а шесть вкладок обрезали текст. Замер: подсказка просила 1702 точки при выданных 420, высота при сужении не менялась вовсе — переноса не было никогда, а `setCopyable(true)` его и не даёт. Правка от 18.09 лишь выключила горизонтальную полосу, то есть сделала дефект молчаливым. Теперь перенос считает `WrappingHint` по корневому `View`, а гейт МЕРЯЕТ результат (`SettingsHintWidthTest`) и смотрит на строку в каталоге, а не на литерал в коде. |
| [borderLayoutOverlap.md](ui/borderLayoutOverlap.md) | [баг+замер] `BorderLayout` при нехватке ширины не сжимает и не переносит — он КЛАДЁТ `EAST` поверх `WEST`. Строка состояния композера с несжимаемым минимумом (`minimumSize = preferredSize`) легла поверх шапки, как только в ней оказалось длинное имя инструмента. Тот же класс дефекта, что трижды обрезал страницы настроек. Правило: минимум компонента с текстом не равен предпочтительному; длинный текст усекается сам с многоточием, полный — в подсказке; строка, длина которой приходит извне, не делит полосу с соседями. |
| [pasteHasTwoDoors.md](ui/pasteHasTwoDoors.md) | [баг+правило] У вставки в наше поле ДВА входа: `TransferHandler` Swing и действие `$Paste` IDE через `PASTE_PROVIDER`. Пока логика жила в одном, картинка терялась молча там, где нажатие уходило в другой. Плюс приоритет стоял наоборот: картинка принималась, только если рядом НЕТ текста, — а браузер и мессенджер всегда кладут рядом адрес или подпись. Правило: решение о буфере — одна чистая функция на оба входа; проверка «наше ли» — по видам данных, без конвертации; неудачный разбор возвращает вставку тексту. Рядом ловушка: `Image.getWidth(null)` у незагруженной картинки даёт -1 и роняет `BufferedImage`. |
| [keyPresenceLies.md](agents/keyPresenceLies.md) | [баг+правило] «Спросить не удалось» сворачивалось в «ключа нет», а проба связки отвечает «не знаю» всюду, кроме macOS — значит **на Windows ответ «ключа нет» выдавался ВСЕГДА**, при любом сохранённом ключе. Осмотр связки владельца показал: записи на месте, страховочных нет — ключи не терялись, их не видел интерфейс. Правило: три ответа вместо двух, третий показывается человеку; диалог вызывает чтение значения и только у связки macOS, где связки нет — читаем свободно; ветка, недостижимая на машине разработчика, выносится в чистую функцию, иначе не измеряется. Рядом: на Windows ключи прежних сборок остались в каталоге `VibeIdea2026.3` — переноса настроек у нас нет. |
| [mcpToolDrift.md](agents/mcpToolDrift.md) | [защита] Описание инструмента MCP — граница безопасности: кампания Deadbugz меняет описания `tools/list` ПОСЛЕ третьего вызова, подставляя инструкции искать SSH-ключи и ключи AWS. Вопрос человеку на каждый вызов от этого не защищает — решение принимается по описанию, а меняют его. Отпечаток набора (имя + описание + схема) снимается при первой встрече и сверяется при каждом подключении; дрейф отключает инструменты сервера до подтверждения и называется в ленте; подтверждение перечитывает набор заново. Не защищает от сервера, вредного изначально. |
| [clientInheritsNothing.md](agents/clientInheritsNothing.md) | [находка+правило] `HttpClient`, собранный с нуля, не берёт из IDE ни сертификаты, ни авторизацию прокси: корпоративный CA из настроек на запросы к провайдерам не действовал, а прокси с паролем мы не проходили вовсе — снаружи это «чат не работает, остальная IDE работает». Взяты доверие и авторизация из платформы, маршрут остался свой (трафик моделей ходит своим прокси намеренно). Правило: клиент, собранный с нуля, наследует ноль настроек, и каждая своя сборка обязана явно ответить, что берёт. Нашлось при разборе удаления OkHttp в 2026.3, которое само нас не касается. |
| [installWithIdeNode.md](languages/installWithIdeNode.md) | [правило] Одна нода на всё: ею языковой сервер ставится, рядом с ней ищется и ею же запускается. Было три разных ответа: установка — `npm` оболочки (при менеджере версий — чужая нода), поиск — без папки ноды IDE (установленное через fnm/nvm не находилось, IDE молча работала на встроенной копии), проверка — прямой запуск без ноды в `PATH` (обёртки npm `#!/usr/bin/env node` падали, исправный сервер выглядел сломанным). Плюс проверка в EDT замораживала настройки на весь таймаут. Ловушки: `Path.of("C:\\…")` на macOS не отделяет каталог; поиск, спрашивающий ноду, без приложения падал NPE. |
| [feedSelfScroll.md](ui/feedSelfScroll.md) | [баг+правило] Лента ездила сама не нашим кодом: `JTextComponent.select` = `setCaretPosition` + `moveCaretPosition`, каждый через `DefaultCaret.adjustVisibility` зовёт `scrollRectToVisible` — отсюда и прыжок в конец при дописывании ответа, и невозможность выделить хвост длинного ответа. Лечится кареткой с пустым `adjustVisibility`. Того же рода третий дефект: выбор строки меню `/` вставлял тот же текст, слушатель документа считал его новым триггером и открывал меню заново — «строка не выбирается». Правило: сперва проверить, не делает ли это компонент Swing сам. |
| [themeSwitch.md](ui/themeSwitch.md) | [баг+правило] Тёмная тема со светлым диалогом настроек: `LafManager.setCurrentLookAndFeel` ставит тему и схему редактора, но НЕ зовёт `DarculaInstaller`, а только он переключает `JBColor.setDark` и `IconLoader.setUseDarkIcons`. `JBColor.DARK` — кэш, посеянный при старте IDE: без этого шага каждая пара `JBColor(светлый, тёмный)` и каждый значок отдают сторону ПРЕЖНЕЙ темы, сколько ни зови `updateUI()`. Цвета из json темы при этом применяются правильно — отсюда расслоение «окно тёмное, диалог белый» (0.6.17, нашёл владелец). Правило: тему переключает только платформенный `QuickChangeLookAndFeel.switchLafAndUpdateUI`; прямой вызов запрещён гейтом 8ж. Урок шире: повторяя своим кодом то, что платформа делает своим входом, надо звать её вход — в нём окажутся шаги, о которых мы не знаем. |
| [lightTheme.md](ui/lightTheme.md) | [правило+гейт] Восьмая тема (первая светлая) показала, где в хозяйстве тем зашита сторона: генератор цветов проекта считал светлоту как `min(0.42, панель + 0.13)` и на светлой теме дал бы тёмную плашку поперёк шапки, причём гейт остался бы зелёным — он сверяет значения с тем же генератором. Гейт контраста, наоборот, пережил появление светлой стороны без правок: WCAG через `max/min` симметричен. Правило: светлая тема не делается инверсией тёмной (на свету акцент темнее фона, а не светлее); в наборе обязаны быть обе стороны, иначе половина пары «день/ночь» чужая. |
| [themePalette.md](ui/themePalette.md) | [правило+гейт] Две находки одного разбора палитр. Первая: в каждой из восьми тем 7–8 цветов повторялись до восьми раз — вынесены в именованные роли (`Text`, `Error`, `Border`, `BorderControl`, `Hover`, `Ok`, `Warn`, `ButtonBg`) миграцией с доказанным инвариантом (карта из 109 разрешённых цветов совпадает до и после). Вторая, важнее: **гейт контраста меряет объявленное, а не отсутствующее** — незаявленный атрибут схемы молча берётся у родительской, и наша подпись Neon Dark от основания набора рисовала комментарии серым цветом Darcula. Правила: цвет, встреченный трижды, обязан иметь имя; схема обязана объявлять общий минимум из семи атрибутов. Дополнено 20.09.2026 третьей находкой: гейт контраста мерил схемы редактора и не мерил НАШИ панели — первый же замер по одиннадцати парам дал двенадцать провалов (худший 2.08), потому что `AccentDim` служил и украшением, и текстом. Роль разделена (`TextDim`), таблица пар заземлена кодом, мёртвая запись палитры теперь тоже валит гейт. Четвёртая находка: `Warm` и `Error` были одним цветом в шести темах — сигнал тревоги выглядел украшением. Разведены по ΔE76 в Lab (планка 20, взята по измерению задуманных расхождений 22.0 и 51.1); тон красного задан, светлота от темы, насыщенность минимальная достаточная. Два захода подбора выброшены — розовый и пастельный. |
| [halfSwitchedLaf.md](ui/halfSwitchedLaf.md) | [баг+правило] Окно тёмное, диалог светлый, кнопки налезают: `LafManager.updateUI()` падает на `ToolbarComboButton` («no ComponentUI class») при переходе на классическую тему под новым интерфейсом и бросает обход окон на середине. Мы предлагали классические темы в списке и в паре — у владельца ночной стояла Darcula. Правило: только темы без `isRestartRequired()`. Рядом: NPE от `accessibleContext` у голого `JComponent` (страница не создавалась) и связка при старте — обновление каталогов читало ключ каждого провайдера. |
| [themeParentId.md](ui/themeParentId.md) | [баг+гейт] Корневая причина всей темовой чехарды: `parentTheme` платформа ищет по **id**, а у новой тёмной темы id `ExperimentalDark` при имени «Dark». Семь наших тем писали «Dark», совпадения не было, и платформа молча подставляла запасного родителя — КЛАССИЧЕСКУЮ Darcula. Под новым интерфейсом это давало таблицу умолчаний без классов UI новых компонентов: `no ComponentUI class for ToolbarComboButton` при каждом старте, светлые диалоги поверх тёмного окна, слипшиеся кнопки. Жило с основания набора; гейт теперь сверяет родителя с реестром платформы. Урок: молчаливый запасной вариант опаснее отказа, а причина лежала в логе владельца с первого снимка. |
| [launchCheck.md](ui/launchCheck.md) | [метод] Проверка собранного образа ЗАПУСКОМ в отдельном профиле `/Volumes/Storage/Caches/vibeidea-check` — гейты и юнит-тесты мерят форму вызова и зеленеют на сломанном виде, а таблицу умолчаний темы, залипшую схему редактора и светлые диалоги поверх тёмного окна видно только поднятой целиком IDE. Что смотреть: `no ComponentUI class` обязан быть 0, `colors.scheme.xml` либо отсутствует, либо называет нынешнюю тему, снимок своего окна через `osascript` + `screencapture -R`. Грабли: Swing-радиокнопки не жмутся ни `click at`, ни `AXPress`, а `keystroke` идёт через раскладку — тему менять перезапуском с правкой `laf.xml`, и `themeId` там это id из `plugin.xml`, не имя. |

## design

| Файл | О чём |
|---|---|
| [detectorSilence.md](design/detectorSilence.md) | [правило] Каталог из 81 детектора: каждое правило обязано иметь ветку «не знаю» и в ней молчать (нечитаемые стили, неразложенный элемент, крутилка, явный `inputmode`, отсутствующая рамка) — обвиняющий по догадке детектор выключают целиком. Плюс: класс правила (пол/вкус) решает каталог, а не само правило; русские регэкспы — данные, и живут в `DesignPhrases`. |

## architecture

| Файл | О чём |
|---|---|
| [foreignHookContract.md](architecture/foreignHookContract.md) | [грабли] Хук, написанный для Claude Code, у нас молча разрешал: там код выхода остаётся `0`, а решение печатается джейсоном. Теперь `deny`/`block`/`continue: false` — отказ, `allow`/`approve` — разрешение без заметки агенту, `ask` — предупреждение пользователю; решением считается только вывод от `{` до `}`, код `2` сильнее JSON. Урок: «у нас такой ошибки быть не может» проверять в обе стороны. |
| [gateDesign.md](architecture/gateDesign.md) | [правило] Гейт, зелёный впустую: пять форм одной ошибки, собранных за месяц. Сторож ставится у той двери, за которой будет опасность (наш глоб `**/*.jsonc` мимо десяти файлов набора); гейт обязан заявлять, что что-то нашёл (молча пустой хуже отсутствующего); искать вызов, а не имя; мёртвое исключение — такой же долг, как забытая проверка; гейт, отвергающий обычные случаи, выключат. Формулировка пришла от VibeSweep: у теста спрашивать не «проходит ли он», а «мимо чего он смотрит». Шестая форма: исключение выдано ФАЙЛУ, а причина про часть файла — под честным обоснованием пряталось 11 интерфейсных строк; лечится числом рядом с исключением. |
| [deadConfigFields.md](architecture/deadConfigFields.md) | [архитектура] Настройка, которую IDE читает из файла и молча игнорирует: два таких поля за один день в разных плагинах (`maxTokens`/`maxSteps` шага, `autoStart` дев-стека). Хуже отсутствующей возможности, потому что неотличима от работающей; тесты бессильны по построению — они проверяют разбор, а разбор как раз работает. Закрыто шестым гейтом `checkVibeConfigFields.sh` (слияние и copy() потребителем не считаются). Второй урок: отговорка «пока нет гейта доверия» пережила своё основание на месяцы. Вторая форма (10.09.2026): поле мертво не вообще, а В КОНТЕКСТЕ — `maxSteps` и `paths` на шаге со своей моделью; гейт видит потребителя и молчит, ищется чтением «на всех ли видах записи потребитель включён». |
| [benchmarksMeasureHarness.md](architecture/benchmarksMeasureHarness.md) | [правило] Бенчмарк меряет связку «модель + харнесс»: тот же Opus 4.8 — 69,2 % своим стендом и 51,9 % на SEAL (17,3 пункта разницы), у Gemini 3.1 Pro разброс 26,4. Вендорских процентов в `ModelQuirks` и в выборе модели не приводить; сравнивать только своим харнессом через `/measure`. |
| [upstreamBoundary.md](architecture/upstreamBoundary.md) | [правило] Граница «наш код ↔ upstream»: платформу не правим ради стиля, свой код — в модулях `com.vibe.*`; каждая точечная правка платформы — запись в FORK_CHANGES.md. Перенос фундаментального правила VibeIDE с обострением: IC огромен, merge-налог выше. |
| [pipelineStepModelContract.md](architecture/pipelineStepModelContract.md) | [инцидент] Модель шага в общем `pipelines.json` читалась двумя продуктами по-разному: VibeIDE — строкой `провайдер/модель`, мы — парой `provider`+`model`. Набор был записан парой, и у VibeIDE шаги `cascade-review` молча шли на модель роли. Канон — написание соседа, пара — синоним; набор переписан. Урок: перед новым полем в общем формате читать код загрузчика соседа. |
| [directChatToolWires.md](architecture/directChatToolWires.md) | [архитектура] Цикл вызова инструментов в прямом чате на трёх проводах: результаты раунда — ОДНО сообщение (anthropic отвергает два пользовательских подряд, gemini сопоставляет по имени, openai разворачивает на сообщения); вызов собирается из кусков по индексу и заново на повторе; Gemini отвергает `$schema`/`additionalProperties`; без инструментов тело побайтно прежнее. Неизвестный инструмент — запись. |
| [sharedSeedContract.md](architecture/sharedSeedContract.md) | [архитектура] Набор сидов общий с VibeIDE, и переименование поля у соседа ломает нас МОЛЧА: цена под `cost*` перестала читаться, тестам падать было не на чем. Канон — чужое написание, своё остаётся синонимом; тест контракта пишется в чужих именах; гейт полей научен ловить ключ без читателя. Продолжение: выключенная запись не валидируется вообще (кроме находок безопасности), таргетинг `products` не даёт совместимости назад, а сторож смотрел только в `.jsonc` — мимо десяти файлов набора. |
| [liveJournalPath.md](architecture/liveJournalPath.md) | [инцидент] Живой журнал был открыт агенту: правило охраняло `.vibe/audit.jsonl`, а `AuditLog` и `CheckpointService` пишут через `VibeLocal` в `.vibe/local/`. Тест правила проверял строки, а не путь писателя — обе половины зелёные, между ними ничего. Теперь тест берёт путь у `VibeLocal.file`; терминал, обходящий `AccessPolicy` по построению, закрыт анализатором команд (запись в журнал и эвалы — опасна, чтение молчит). |
| [mcpBridge.md](architecture/mcpBridge.md) | [архитектура] MCP-сервер в IDE: почему свой протокол вместо третьего пути в нашем HTTP API и почему НЕ отдельный процесс (второй порт = второе место ошибиться с токеном). Ревизия 2026-07-28 stateless — подарок для сервера внутри GUI, но старую 2025-06-18 нельзя не поддержать. Детерминированный `tools/list`, `ttlMs`, `resultType`, новые коды ошибок; упавший инструмент — результат с `isError`, а не ошибка протокола. |

## build

| Файл | О чём |
|---|---|
| [crossBuildFromMac.md](build/crossBuildFromMac.md) | [грабли] Кросс-сборка трёх платформ с мака: апстрим выключает шаг инсталлятора Windows именно на macOS (наша цель возвращает его обратно), каждая сборка стирает `out/vibeidea` целиком — копии уносить вне него, macOS собирать последней. Кросс-артефакты обязаны попасть в штамп: публикация видит только его. «Проверено по коду» — не доказательство про артефакт. |
| [bundledPluginIndex.md](build/bundledPluginIndex.md) | [инцидент] LSP4IJ не грузился НИ В ОДНОЙ собранной сборке: платформа грузит встроенные плагины только по `plugins/plugin-classpath.txt`, а каталог, скопированный туда после генерации индекса, невидим. Штатный механизм — `ProductProperties.getAdditionalPluginPaths()`. После каждой сборки читать в логе `Loaded bundled plugins`. |
| [platformClassesInDist.md](build/platformClassesInDist.md) | [грабли] Зависимость модуля в `.iml` — это компиляция, а не дистрибутив: UAST лежит внутри плагина Java, и граф кода падал `NoClassDefFoundError` во всех установленных сборках, пока тесты были зелёными. Второй урок — зависимость оказалась не нужна: UAST понимает JVM-языки, а на TypeScript-проекте граф и так не имел ни одного ребра. |
| [threePackagingSystems.md](build/threePackagingSystems.md) | [грабли] Состав дистрибутива решают ТРИ места: `BUILD.bazel` (компиляция), `.iml` (модель проекта) и раскладка плагина в `VibeIdeaProperties.kt` (упаковка). Правка первых двух даёт зелёные тесты и мёртвую фичу в dmg — проверено на ZXing и PDFBox. Проверка — только сборкой инсталлятора.  Дополнено 19.09.2026: ЧЕТВЁРТОЕ место — npm-пакеты едут не из `servers-npm`, а из `extracted/servers`, который собирает `download.sh`; забытый шаг ловил только гейт дистрибутива, то есть после сборки. Теперь сверка есть в `checkServerDeps.sh` и стоит две секунды. |
| [projectModelGate.md](build/projectModelGate.md) | [грабли+гейт] Ссылка в `.iml` проверяется ТОЛЬКО сборкой инсталлятора: `bazel test` читает граф Bazel и до JPS-модели не доходит, а генератор `bazel-targets.json` падает на неразрешённой зависимости. Библиотеки в дереве IC — модули-обёртки (`intellij.libraries.gson`), а не библиотеки уровня проекта. Гейт `checkVibeModules.sh`, проверен поломкой. |
| [bazelBuild.md](build/bazelBuild.md) | [инструмент] Сборка IC на Bazel (2026): тулчейн герметичный (bazelisk + JBR сами качаются), android-репо обязателен для инсталлятора, ~100 GB диска на полный цикл, 16 GB RAM впритык (CI добавляет 30 GB swap). Команды запуска/сборки/тестов. |
| [bazelCaches.md](build/bazelCaches.md) | [инструмент+грабли] Кэши Bazel на macOS: пути задаёт апстримный `common.bazelrc`, машинные переопределения — только в `.bazelrc-user.bazelrc` (он читается позже, и стартовая опция из него побеждает). Вторая база — не «два написания пути» (Bazel разворачивает путь сам), а остаток переезда проекта: md5 от пути, которого больше нет. Переезд корня на внешний том: сперва `shutdown`, копировать только repository cache (адресован содержимым), базу сборки — никогда (абсолютные симлинки в старый корень); старый корень без `chmod -R u+w` не удалить. Замер на M4 с NVMe-томом: пересборка та же, старт сервера +0,3–2,3 с. Внешний том индексирует Spotlight, `~/Library/Caches` — нет: папку исключить. Симлинк вместо настройки — нет. |
| [windowsBuild.md](build/windowsBuild.md) | [грабли+инцидент] **Phpactor на Windows: проверок в phar две, чинили не ту** — падает собственная `extension_loaded('posix')` в `bin/phpactor`, а не проверка Box; «эмуляция Windows» отключением функций `posix_*` слепа по построению (расширение остаётся загруженным), поэтому обещание «PHP на Windows заработал» уехало в 0.4.0 при неработающем сервере. Плюс сборка инсталлятора под Windows (02.09.2026): клон без `core.longpaths` обрывается и оставляет пустой индекс (лечится `git reset` + `restore`, не переклонированием); `python3` — заглушка магазина Microsoft, гейты берут интерпретатор через `pythonBin.sh`; разделители путей в Python-блоках гейтов; `strings` нет; иконка лаунчера и картинки NSIS без явных путей — стоковые IntelliJ (генератор `makeWinImages.py` из того же `.icns`); клон `android/` — по дате синка, не по HEAD; **Phpactor на Windows не стартует** (phar требует `ext-posix`); сравнение exe/zip сборка в dev-режиме пропускает — проверять тихой установкой. |
| [windowsAntivirus.md](build/windowsAntivirus.md) | [грабли] Касперский удалил `idea64.exe` с вердиктом `PDM:Trojan.Win32.Generic`: префикс PDM — поведенческий модуль, а не сигнатура, `Generic` — «зловред не опознан». Совпали неподписанный бинарь, нулевая репутация и поведение IDE (чужие процессы, лавина записей в профиль, `.lnk`, докачка архивов). Тот же вердикт получала Kiro от AWS. Лечится подписью кода; до неё — исключение, отправка FP и честная строка в заметках релиза. |
| [codeSigning.md](build/codeSigning.md) | [инфраструктура] Подпись macOS/Windows: почему не через `SignTool` платформы (он под инфраструктуру вендора), что делают `signMac.sh` (codesign deep+runtime → dmg → notarytool → stapler) и `signWindows.ps1` (signtool с меткой времени по exe/dll и инсталлятору), что нужно от владельца и что проверить нечем без сертификата. |
| [updateChannel.md](build/updateChannel.md) | [грабли+решение] Канал обновлений через `ExternalProductResourceUrls` (патч платформы не нужен) упёрся в то, что `263.SNAPSHOT` платформа читает как `Integer.MAX_VALUE`: установка считает себя новее всего, канал молчит навсегда. Настоящий номер `263.<major·10000+minor·100+patch>` ставится в нашем билд-таргете, `updates.xml` пишется по `product-info.json` артефакта, штамп отказывает на SNAPSHOT. Патчей нет и не обещаем; 0.3.0 обновления не увидит. |

## gitAndTools

| Файл | О чём |
|---|---|
| [upstreamTagSync.md](gitAndTools/upstreamTagSync.md) | [квирк] Синк форка идёт по тегам релизов, а инструмент отставания мерил `upstream/master` — верный ответ на другой вопрос. Наивное «взять последний стабильный тег» откатывает платформу на линию назад (наша база 263 = 2026.3, последний релиз апстрима 2026.2.2): считать надо по линии из `build.txt`. EAP не предлагается молча. Грабля: `grep` без совпадений под `set -e` роняет скрипт до строки, которая всё объясняет. |
| [shallowClonePush.md](gitAndTools/shallowClonePush.md) | [инцидент] Первый пуш форка отвергнут: `remote unpack failed: index-pack failed` — у поверхностного клона корень ссылается на несуществующих родителей. Лечится пересборкой корня через `commit-tree` (13 с на 183 коммита), а не докачкой истории. Проверять `.git/shallow` ДО публикации. |
| [shallowDeepenGithub.md](gitAndTools/shallowDeepenGithub.md) | [квирк] `git fetch --deepen` против GitHub падает «error processing shallow info: 4» — ретраи бесполезны; дозакачивать историю через `--shallow-since` со сдвигом даты. |
| [caseInsensitiveFs.md](gitAndTools/caseInsensitiveFs.md) | [квирк] Case-insensitive APFS: `README.md` ≡ `readme.md` — копирование в каталог апстрима молча перетирает его файл (наш README перетёр их docs/readme.md; восстановлен). Свои файлы — только в своём неймспейсе; после копирования смотреть `git status`. |
| [checkpointTempIndex.md](gitAndTools/checkpointTempIndex.md) | [инцидент] `GIT_INDEX_FILE` на пустой файл из `createTempFile` — git отвечает «index file smaller than expected», и чекпоинты не создавались ни разу с v0.1.0: отказ читался как «недоступно». Временный индекс — несуществующий путь в свежей папке, старт с копии настоящего индекса (иначе хеш всего дерева и тайм-аут). Урок: функция, деградирующая до «недоступно», требует теста на настоящем инструменте. |
| [whisperTwoClis.md](gitAndTools/whisperTwoClis.md) | [грабли] Два «whisper» — openai-whisper (`whisper`, Python) и whisper.cpp (`whisper-cli`, `whisper-server`) — с разными командными строками: аудио операндом против `-f`, своя модель против обязательного файла ggml `-m`, `--output_dir` против `-of`, язык по умолчанию — сам против английского. Наша команда была от openai-whisper, и после «brew install whisper-cpp» по нашей же подсказке каждая заметка кончалась справкой. Проверять запуском на настоящем бинаре. |

## languages

| Файл | О чём |
|---|---|
| [platformLspVsLsp4ij.md](languages/platformLspVsLsp4ij.md) | [архитектура] `platform/lsp` открыт, лежит в дереве и уже едет в наш дистрибутив (31 кастомайзер, API не экспериментальный). DAP у платформы есть только в закрытой части (с 2024.2), поэтому LSP4IJ всё равно пришлось бы оставить — миграция без выигрыша, а с 11.09 известна и её цена: у платформенного клиента нет файловых операций LSP (правка импортов при переносе файла). Решение: ждать DAP в открытом дереве, сторож при синке — `git grep -l DebugAdapterSupportProvider`. |
| [lspStack.md](languages/lspStack.md) | [архитектура] PHP/TS в открытом дистрибутиве: PhpStorm/WebStorm-плагины закрыты → LSP4IJ (EPL-2.0, +DAP) + Phpactor (MIT) + vtsls (MIT); Intelephense бандлить НЕЛЬЗЯ (EULA §5c); `platform/lsp` открыт (2026-06, Apache-2.0), но без DAP. Лицензии проверены по первоисточникам. |
| [kdocNestedComments.md](languages/kdocNestedComments.md) | [грабли] Блочные комментарии Kotlin ВКЛАДЫВАЮТСЯ: `docs/**` внутри KDoc открывает вложенный комментарий, и файл до конца перестаёт быть кодом. Ошибка одна, указывает на последнюю строку файла, а рядом сыплются `Unresolved reference` из других файлов. Glob-примеры — только в `//`-строках. |
| [i18nRatchet.md](languages/i18nRatchet.md) | [грабли] Храповик локализации нельзя считать `grep`-ом: диапазон `[А-Яа-яЁё]` сравнивается по байтам UTF-8 и записывает в кириллицу `—`, `·`, `…`, `«»`, стрелки — планка держалась на пунктуации, а непереведённая кнопка тонула в шуме. Счёт переписан на python по кодовым точкам, комментарии вырезаны, планка 0. Плюс правила исключений и почему строка каталога — `val ... get()`. |
| [phpOnWindows.md](languages/phpOnWindows.md) | [грабли+решение] Встроенный Phpactor не стартовал на Windows: Box требует `ext-posix`. Разбор phar показал, что требование ЛОЖНОЕ — все `posix_*` внутри защищены `function_exists`, у самого Phpactor их нет. Решение — однострочный prepend, выключающий проверку Box, через `-d auto_prepend_file` (не окружение: процесс запускает клиент LSP). Проверено на macOS с отключёнными 40 функциями posix. Intelephense остался явным выбором. Плюс: гейт локализации считал упоминание ключа в комментарии использованием. |
| [extJsNavigation.md](languages/extJsNavigation.md) | [архитектура] Ctrl+Click по имени класса Ext JS: в открытой платформе JS-ядра нет (в PhpStorm это закрытый плагин вендора), PSI у .js плоское — значит ни парсера, ни ссылок. Работает только gotoDeclarationHandler (ему хватает документа и позиции), цель перехода строится FakePsiElement с OpenFileDescriptor на смещение, а объявления держит FileBasedIndex по тексту файла. Индексируются Ext.define и alias/xtype, extend — нет. Цена разбора по тексту названа явно. Гейты — ExtJsTest (разбор) и ExtNavigationPlatformTest (переход на живой платформе); он и нашёл, что textOffset у FakePsiElement по умолчанию 0, а тест с редактором поднимает LSP4IJ и требует lsp4j-jsonrpc в classpath. |
| [eslintServerSettings.md](languages/eslintServerSettings.md) | [баг] `textDocument/codeAction` падал на Windows («The "path" argument must be of type string. Received undefined») из-за нашего ответа на `workspace/configuration`: не слали `workspaceFolder`, слали `nodePath: ""` вместо `null` и навязывали `experimental.useFlatConfig: false`. Совсем убрать `experimental` нельзя — сервер разыменовывает его без проверки; нужен пустой объект. На macOS не воспроизводилось, разобрано по коду сервера и стендом. Гейт — `EslintSettingsTest`. |
| [typescript7Lsp.md](languages/typescript7Lsp.md) | [архитектура] TypeScript 7 сам говорит на LSP (`tsc --lsp --stdio`), а его нативный сервер лежит в пакете `typescript` проекта — двадцать платформенных пакетов по ~28 МБ. Везти его в дистрибутиве не нужно и вредно (npm ставит бинарь только хоста, версия разошлась бы с проектом): «Автоматически» берёт `tsc` проекта при typescript 7+, иначе vtsls. README typescript-go команду не называет — источник конфиг nvim-lspconfig. |
| [lspLanguageIds.md](languages/lspLanguageIds.md) | [баг] `languageId` — указание tsserver, каким парсером открыть документ: под `typescript` в `.tsx` каждый тег JSX это ошибка (152 диагностики на здоровом файле, `tsc` при этом чист). Шаблоны разведены на `typescript`/`typescriptreact`/`javascript`/`javascriptreact`, как в VS Code; то же у ESLint. Гейт — `LanguageIdMappingTest`, читающий XML как данные. |

## agents

| Файл | О чём |
|---|---|
| [orchestratorUsage.md](agents/orchestratorUsage.md) | [грабли] Токены модели-оркестратора (Sakana Fugu) аддитивны и биллятся по обычной цене — без них ход считается дешевле, чем стоит. Формат usage вендор не документирует: известны плоские `orchestration_*` и те же имена под `usage.token_details`, обе из вторичных источников. Читаются обе, вложенная побеждает, никогда не складываются. Расхождение нашлось только при подтягивании набора перед правкой сидов. |
| [promptCacheTiming.md](agents/promptCacheTiming.md) | [квирк] Промпт-кэш протухает по ЧАСАМ, а не по действиям, и таймер тикает с отправки запроса — ход, думавший четыре минуты, оставляет от пятиминутного кэша одну. Окупаемость записи считается по ценам пользователя (надбавка ÷ экономия попадания): 5m — со второго запроса, 1h — с третьего. Предупреждать надо ДО траты; тёплый кэш обязан молчать. Что из ролика не взяли и почему. |
| [acpClient.md](agents/acpClient.md) | [архитектура] Агентская обвязка = свой ACP-клиент: JetBrains AI Chat закрыт и в IC отсутствует; открыты спека ACP + Kotlin SDK (Apache-2.0). Референсы архитектур: Continue (core-процесс + JCEF), ProxyAI (чистый JVM), jetbrains-cc-gui. Точки расширения платформы для агентского плагина. Дополнено 20.09.2026: версия протокола сверяется на рукопожатии (спека требует отключаться при расхождении), версия крейта НЕ равна версии провода, три changelog с датами, нестабильное лежит отдельным файлом `schema.unstable.json`, а v2 — Draft с оговоркой «не выпускать по умолчанию». |
| [acpGatesTerminal.md](agents/acpGatesTerminal.md) | [архитектура] Перенос терминала/хуков/VERIFY-GATE/turn-checks в ACP-модель: цикл агента в процессе Claude Code → всё цепляется за `session/update` + `request_permission`, а не за свой tool-loop. Реестр tool-call'ов как предпосылка; Claude-терминал через `_meta.terminal_output` (стандартные `terminal/*` мертвы для CC); `wait_for_exit` уводить с reader-потока; preToolUse блокирует только в точках клиента; bounce = повторный `session/prompt`; предохранители переживают рестарт. |
| [acpSessionsPerThread.md](agents/acpSessionsPerThread.md) | [архитектура] Сессия агента на тред: несколько сессий на одном соединении, текущая не меняется под ходом («Стоп» — в сессию последнего `prompt`), старая запись «агент + папка» переезжает к одному треду; `session/fork` в ACP v1 нестабилен, `messageId` ветку не даёт. |
| [subscriptionQuota.md](agents/subscriptionQuota.md) | [грабли] Остаток подписки только по ответу вендора: у MiniMax запрос официальный, а смысл `usage_count` менялся (решает CLI вендора по проценту), у Z.ai запрос недокументирован и формат уже менялся (`CREDIT_LIMIT`); незнакомое — «не прочитано», а не 0. |
| [modelCatalogCache.md](agents/modelCatalogCache.md) | [архитектура] Кэш каталогов моделей: отдаём всегда без TTL, обновляем фоном и параллельно, пишем только успешный ответ (401/timeout не стирают вчерашний каталог), запись помнит отпечаток endpoint, место — системный каталог IDE, а не `.vibe`. Грабли: `invokeAndWait` с пула в модальных настройках = дедлок; снимок `staticModelIds` — до подмешивания кэша, иначе кэш зарастит curated-пикер; `connectTimeout` живёт на `HttpClient`, отсюда отдельный `LlmClient.forCatalog()`. |
| [contextPoisoning.md](agents/contextPoisoning.md) | [архитектура] Защита входящего контекста: невидимые символы (включая теговый блок U+E0000–E007F) и bidi вырезаем молча, фразы-инструкции только сообщаем (правка чужого файла хуже предупреждения), секреты — сообщаем, маскируем по настройке, файл не трогаем. Единая точка `ContextSerializer.load` + `IdeFileOps`. Грабли: в Java `(?i)` складывает регистр только ASCII, а `\b` не знает кириллицу — нужен `(?iU)`, иначе гейт молча не находит ничего. |
| [cascadeAndGates.md](agents/cascadeAndGates.md) | [архитектура] Каскад моделей и гейты приёмки: возможность, не объявленная в рукопожатии ACP, мертва (elicitation работал «в коде» и ни разу в жизни); шаг пайплайна на своей модели — только судящим ролям, потому что у прямого запроса нет инструментов; гейт `pipelineStepEnd` решает приёмку черновика, и «хук промолчал» ≠ «хука нет»; контекстный налог (≈$65 на PR у SonarSource) меряется в `/trace` и лечится символьным поиском по индексу платформы вместо чтения файлов целиком; трифекта вместо правил на последовательности вызовов. Что подтверждено источниками, а что нет. |
| [watchPipeline.md](agents/watchPipeline.md) | [квирк] `/watch`: кадры по сменам сцен (прореживание по времени, а не по индексу), якорь первого кадра против статичного ролика, `-o video.%(ext)s` против выбора контейнера yt-dlp, `s[:=]` в showinfo (**на ffmpeg 8.1.2 проверено: `s:`**, вопреки записи VibeIDE), видео-свидетельство сильнее подсказки по имени, `(attached pic)` — обложка, ненулевой код `ffmpeg -i` — норма. Почему бинари не бандлятся. |
| [providersCatalog.md](agents/providersCatalog.md) | [архитектура] Авточитаемый каталог `.vibe/providers/*.jsonc` вместо вшитых провайдеров (решение №24): каталог — СЛАБЕЙШИЙ слой (глобальный каталог → проектный каталог → глобальный providers.json → проектный providers.json), иначе засеянный active:false глушил живую глобаль пользователя; «запись без active активна» — осознанно не тристейт; один строгий resolveExtends по слитому реестру; ключ без рестарта через топик ProvidersChangeListener; пустое поле не стирает общий apiKeyRef. Грабли: вложенные блочные комментарии Kotlin (`/*` в KDoc от глоб-паттерна = Unclosed comment), trailing lambda против новых дефолт-параметров, где в исходниках VibeIDE лежат конфиги «встроенных» (sendLLMMessage.impl.ts, vibeideSettingsTypes.ts). |
| [decisionsAsFiles.md](agents/decisionsAsFiles.md) | [архитектура] Журнал решений и входящее — файлы в репозитории, а не база рядом с ним: второй источник правды расходится с первым, формат обязан пережить инструмент, дисциплина «нет строки в индексе — нет записи» уже есть и уже проверяется гейтом. Заголовок записи — вопрос, а не выбранный вариант; причина обязательна. Текст формата — не строка интерфейса. |
| [agentPathCanonicalization.md](agents/agentPathCanonicalization.md) | [архитектура] Путь агента сравнивался как текст: `..`, ссылка наружу и другой регистр обходили границу проекта, журналы, `.vibe/ignore` и права шага (все выпуски с v0.1.0). Два вида пути: лексический для ввода-вывода (иначе промах мимо документов редактора у проекта за симлинком), физический для политики; запреты свёрнуты по регистру и NFC всегда, разрешения точные — ФС определять не нужно; висячая ссылка — отказ; корни развёрнуты тем же способом; хук и аудит видят развёрнутый путь. Честные границы: терминал, TOCTOU, жёсткие ссылки. |
| [voiceDelegation.md](agents/voiceDelegation.md) | [архитектура] Схема делегирования GPT-Live («голос принимает, агент исполняет») против нашей: голосовой фронт не несёт задачу — её несёт текст расшифровки в черновике; три канала обратной связи (указание / молча / вслух) у нас — служебная строка, блок размышлений и ответ; **перебивание ≠ отмена** сверено по коду — новое сообщение встаёт в очередь, отмена только явная и объявляется после `stopReason: cancelled`. Почему облачный full-duplex и локальный full-duplex не взяты. |
| [reachableDocs.md](agents/reachableDocs.md) | [правило] Наши доки ездили в сборке с самого начала (33 файла, гейт на расхождение), и агент не пользовался ими ни разу: инструмента не было, а `/help` искал по ИМЕНАМ файлов. Проверять надо не «положили ли в сборку», а «может ли агент найти и прочесть»: поиск по тексту, названный источник, отличие «здесь не написано» от «инструмент сломался». То же правило, что «фича с форматом обязана иметь спеку», с другой стороны. |
| [lspNavigation.md](languages/lspNavigation.md) | [грабли+развилка] Надпись `LSP Symbol` под Cmd — строка из чужого плагина LSP4IJ, а не из платформы; перебивается своим `ElementDescriptionProvider` с `order="first"` (их отвечает всегда). Подчёркивание ВСЕХ идентификаторов их же: `LSPDeclarationFeature.isSupported` спрашивает про файл, а не про позицию, — из их API не чинится. Сигнатуру из `hover` в описание элемента не поставить: платформа спрашивает его синхронно на EDT.  Дополнено 19.09.2026: развилка закрыта — свой слой, форк LSP4IJ не берём. Четыре части (чистый кэш, запрос через LSPDefinitionSupport, свой gotoDeclarationHandler, выключение перехода LSP4IJ), и ни одна не работает без остальных: подчёркивает ЛЮБОЙ обработчик, вернувший цель. Под ключом реестра, выключено по умолчанию. Грабля: сервер отдаёт URI с процентным кодированием, платформа — URL без него. |
| [angularSupport.md](languages/angularSupport.md) | [решение] Форк плагина «Angular Architect» с площадки отклонён: ни исходников, ни лицензии (зато свой пакет лицензирования) — бесплатность прав на распространение не даёт. Взят официальный `@angular/language-server` (MIT). Грабля: сервер ищет TypeScript и `@angular/language-service` по «probe locations», а не по PATH, и без них не стартует вовсе. Плагин с площадки — надстройка-аналитика, а не языковая поддержка: он сам ходит за ней в закрытый плагин JetBrains. |
| [frameworkFilesHaveNoType.md](languages/frameworkFilesHaveNoType.md) | [грабли+решение] Языковой сервер и тип файла — разные вещи: LSP4IJ цепляется по имени файла, а редактор открывает по ТИПУ. У платформы нет типа ни для `.vue`, ни для `.svelte`, ни для `.astro`, ни для `.sass`, ни для `.styl` (из семейства CSS в её бандлах только `scss`) — такой файл открывается «неизвестным» и чёрно-белым, хотя подсказки сервера приходят. Юнит-тест слеп по построению. Лечится точкой расширения `com.intellij.textmate.bundleProvider`: бандлы едут файлами рядом с плагином, `scopeName` берётся из самой грамматики скриптом, язык объявляет только основная. Плюс: метка `NOASSERTION` у лицензии — повод сходить к первоисточнику, а не вывод. |
| [takeOnGrowingList.md](languages/takeOnGrowingList.md) | [квирк] `Iterable.take(n)` в Kotlin при `n >= size` отдаёт копию ВСЕЙ коллекции, а не n элементов. На растущем списке (`CopyOnWriteArrayList`, который дописывает чужой поток) это даёт снимок больше запрошенного: тест знакомства MCP сравнивал `seen.take(2)` и через раз получал три элемента. Лечится ожиданием последнего события до сравнения, а не увеличением таймаута. |

## data

| Файл | О чём |
|---|---|
| [rowEditAddressing.md](data/rowEditAddressing.md) | [правило] Правка данных предлагается только при доказуемом адресе строки (наш запрос предпросмотра + первичный ключ в выборке); всё прочее — отказ кодом. Правки копятся в буфере и показываются операторами до выполнения, применяются одной транзакцией, после чего запрос перечитывается. `"readOnly": true` отключает правку настройкой, а не вниманием. |
