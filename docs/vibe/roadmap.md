# Roadmap VibeIDEA

Единственный источник правды «что уже сделано». Маркеры: `[ ]` открыт · `[/]` в работе · `[x]` закрыт (обязательна приписка `— ✅ факт/артефакт`) · `[~]` частично (приписка «что осталось»).

## Фаза 0 — Фундамент (2026-08-23, ночь основания)

- [~] **Клон intellij-community** — рабочее дерево `master @ 38a8934174` (2026-08-23, shallow: полный клон дважды рвался по сети на ~78%, curl 56). Осталось: фоновая дозакачка истории master кусками `--deepen` (см. отчёт ночи основания).
- [x] **Разведка перед стройкой** — ✅ (2026-08-23) 9 агентов: опыт VibeIDE (дисциплина форка, upstream-playbook, доки, база знаний, агентская обвязка) + веб (Bazel-сборка IC, механика брендинга, LSP/лицензии, ACP). Выжимки легли в [knowledge/](knowledge/README.md) и [idea.md](idea.md).
- [x] **Git-дисциплина форка** — ✅ (2026-08-23) remote `upstream` (клоном `--origin upstream`), push в него отключён физически (`no_push://`); ветки `master` (зеркало) / `main` / `next`, работа в `next` — подтверждено `git branch -vv`, `git remote -v`.
- [x] **Docs-пакет** — ✅ (2026-08-23) CLAUDE.md, FORK_CHANGES.md, docs/vibe/{README,idea,roadmap,functional,decisions}.md, docs/vibe/knowledge/ (5 записей + индекс). Свой неймспейс docs/vibe/ — см. решение №11.
- [~] **Проверка сборки из исходников** — ✅ `./bazel.cmd build //build:idea_community`: Build completed successfully, 4009 действий, 37 мин (2026-08-23, лог ночи основания); тулчейн герметичен — JBR/Bazel скачались сами, ничего не ставилось. Осталось: запуск GUI (`run`) — не гонялся ночью намеренно, проверить днём.
- [x] **Инсталлятор из исходников (dmg)** — ✅ (2026-08-23; команда устарела 23.08 же — с появлением брендинга собирать надо `./vibeidea-installers.cmd`, см. запись в knowledge/build) `./installers.cmd -Dintellij.build.target.os=current -Dintellij.build.target.arch=current` + skip `sources_archive,cross_platform_dist,non_bundled_plugins`: `out/idea-ce/artifacts/ideaIC-263.SNAPSHOT-aarch64.dmg` (783M, `hdiutil verify` VALID, sha256 OK), ~5 мин на тёплом bazel-кэше. Не подписан — Gatekeeper «Open Anyway». Внутри ванильный «IntelliJ IDEA OSS.app» — наш брендинг это Фаза 1.
- [x] **Правила для AI-агентов** — ✅ (2026-08-29, ночная работа) вынесены в [agentsGuide.md](agentsGuide.md), а НЕ в корневой `AGENTS.md`: тот принадлежит апстриму (генерируется их `community/.ai/render-guides.mjs`, последний коммит в нём чужой) и правка означала бы конфликт на каждом синке. Наш файл живёт в своём неймспейсе и на него ссылается CLAUDE.md.
- [x] **Гейт целостности доков** — ✅ (2026-08-23) `vibe-tools/docsGraphCheck.py` (stdlib, dead links + wikilinks + unindexed + unreachable); прогнан: 16 файлов, 0 проблем. Осталось: CI-провязка (когда появится CI).

## Фаза 1 — Свой продукт (брендинг)

- [x] Customization-модуль — ✅ (2026-08-23) `vibeidea-customization/`: `idea/VibeIdeaApplicationInfo.xml` (product=VibeIDEA, код `VI`), свои SVG-иконки (оригинальная V-геометрия, без знаков JetBrains); splash отключён (`useSplash=false`).
- [x] `VibeIdeaProperties : IdeaCommunityProperties` — ✅ (2026-08-23) `build/src/org/jetbrains/intellij/build/VibeIdeaProperties.kt`: prefix `VibeIdea`, selector `VibeIdea2026.3`, mac `com.vibe.vibeidea`/`VibeIDEA.app`, вырезаны `intellij.mcpserver.plugin` и `intellij.featuresTrainer`. Корневой дескриптор `META-INF/VibeIdeaPlugin.xml` — зеркало IdeaPlugin.xml.
- [x] Свой installers-таргет — ✅ (2026-08-23) `build/src/VibeIdeaInstallersBuildTarget.kt` + `//build:vibeidea_installers` + обёртка `vibeidea-installers.cmd`. Гейт фазы: `out/vibeidea/artifacts/vibeIdea-263.SNAPSHOT-aarch64.dmg` (778M, hdiutil VALID, product-info: name=VibeIDEA, code=VI, `VibeIDEA.app`).
- [x] **Ревизия `bundledPluginModules`** — ✅ mcpserver/featuresTrainer вырезаны (2026-08-23); полная ревизия проведена по **собранному дистрибутиву** 01.09.2026 и закончилась выводом «резать нечего без решения о продукте».
  - Android-плагина в сборке **нет вовсе** (прежняя формулировка пункта была неверной): есть три мелких куска — `android-gradle-declarative-lang-ide` (1,0 МБ), `android-gradle-dsl` (3,5 МБ), `compose-ide-plugin` (1,8 МБ). Первый и третий ни от кого не зависят, но 2,8 МБ из 2,5 ГБ — это шум, а не оптимизация; второй снять **нельзя**: `gradle-java-plugin` объявляет `<plugin id="org.jetbrains.idea.gradle.dsl"/>` обязательной зависимостью, и без него отвалится поддержка Gradle.
  - Реальный вес: Kotlin 388 МБ, JCEF 336 МБ, Gradle 204 МБ, Java 123 МБ, Terminal 97 МБ, Grazie 74 МБ. JCEF нужен превью и дизайн-детектору. Остальное — **вопрос о том, чем продукт является**: «IDE PhpStorm-класса» без Java/Kotlin/Gradle/Maven весит на ~800 МБ меньше, но это уже другой продукт, и решать это молча правкой списка модулей нельзя.
- [ ] **Решение владельца: режем ли базовые языки.** Снять Java+Kotlin+Gradle+Maven+Groovy — минус ~800 МБ из 2,5 ГБ; отдельно Grazie (74 МБ, тот самый грамматический движок, чей баннер мы гасили). Против: наш собственный репозиторий на Kotlin и Bazel открывается в этой же IDE.
- [x] **Grep-гейт брендинга** — ✅ (2026-08-29) `vibe-plugins/tools/checkVibeBranding.sh`: ищет «IntelliJ IDEA»/«JetBrains» ТОЛЬКО в наших каталогах (`vibe-plugins/`, `vibeidea-customization/`; апстрим и submodule сидов не трогает — там чужой брендинг законен). Исключения — в `brandingAllowlist.txt` С ПРИЧИНОЙ, по строке на случай; гейт падает и на упоминании вне списка, и на записи, которая больше ничего не находит (устаревшее исключение — такой же долг). Пять законных упоминаний объяснены: описание чужого баннера в каталоге строк (ru/en), путь `~/.jetbrains/acp.json` (общий реестр агентов — свой путь потерял бы совместимость), комментарий про то, что именно мы гасим, и происхождение LSP4IJ. Обе стороны гейта проверены на настоящем нарушении.

- [x] Свой `.icns` для macOS — ✅ (2026-08-27, вместе с лого) `vibeidea-customization/resources/mac/vibeidea.icns`, прописан `icnsPath`/`icnsPathForEAP` в `VibeIdeaProperties`; проверено в собранном dmg — иконка в бандле и Dock наша. (Пункт оставался открытым по недосмотру: работа была сделана в задаче про лого.)

## Фаза 2 — Языки PhpStorm-класса (TS + PHP)

- [x] Бандл **LSP4IJ** (EPL-2.0) — ✅ (2026-08-23) `bundleExternalPlugins` в VibeIdeaProperties раскладывает пиненный релиз с GitHub (`vibe-plugins/deps/download.sh`, sha256) в `plugins/lsp4ij/`; проверено содержимым .sit.
- [~] **TypeScript**: vtsls — ✅ плагин `intellij.vibe.lsp` (`com.vibe.lsp`): server+fileNamePatternMapping через LSP4IJ (optional depends, PROVIDED-библиотека `lib/vibe/lsp4ij/`), резолв бинаря с учётом GUI-PATH macOS; подсветка — TextMate из комплекта IC; `plugins/vibe-lsp/lib/vibe-lsp.jar` в дистрибутиве. ✅ авто-установка кнопкой (2026-08-31) и ✅ **настройки путей** (01.09.2026): Settings → Tools → VibeIDEA → Языковые серверы — путь к своему серверу перекрывает всё, включая PATH. Пустое поле = прежний порядок. Правило «свой сервер сильнее встроенного» до сих пор действовало только для тех, кто лежит в PATH, а проекты, которым своя версия нужнее всего, держат её в стороне: `vendor/bin`, общая папка контейнера, сборка из исходников. Переставший работать путь доктор называет отдельно — молча игнорировать настройку хуже, чем не иметь её (4 теста). Осталось: живой прогон на реальном проекте.
- [~] **PHP**: Phpactor — ✅ в том же плагине (server+mapping `*.php`); Intelephense — задокументированный путь самостоятельной установки ([мануал](manuals/languageServers.md)). Осталось: живой прогон на реальном проекте (настройки путей и авто-установка — общие с TS, см. выше).
- [x] **Доктор языковых серверов** — ✅ (2026-08-30, next) мы поставляем клиента LSP, но не серверы (vtsls — npm, Phpactor — composer/brew): бандлить значило бы тащить чужой рантайм и лицензию. Цена — свежая установка выглядит сломанной, а переход к определению молчит. `LspDoctor` (чистый) описывает сервер бинарём, однострочной командой установки и расширениями, за которые он отвечает; проверка — функция от резолвера, поэтому тестируется без машины с бинарями. `ServerBinaries.find` возвращает null вместо выдуманного пути (путь из голого имени врал бы «установлен»). Поверхности: действие Tools → «VibeIDEA: языковые серверы» (отчёт по обоим серверам, путь или команда установки, напоминание о перезапуске — PATH читается при старте процесса) и уведомление в момент открытия `.ts`/`.php` с кнопками «скопировать команду» и «больше не напоминать», один раз на сервер и проект. 7 тестов; у `vibe-lsp` появилась своя тестовая цель.
- [~] **Отладка** — ✅ (2026-08-30, next) клиент DAP уже в дистрибутиве (LSP4IJ), адаптеры `js-debug-adapter` и `vscode-php-debug` проверяются доктором отдельной секцией (их отсутствие ломает точки останова, а не навигацию), мануал `manuals/debugging.md` описывает установку и подключение. ✅ **одним действием** (01.09.2026, next): свои `JsDebugAdapterFactory` и `PhpDebugAdapterFactory` на точке расширения `debugAdapterServer` заполняют командную строку адаптера, JSON запуска/подключения и признак готовности; решения вынесены в чистый `DebugAdapters` (11 тестов), в фабриках осталась только проводка. Заодно исправлены **мёртвые команды установки**: `npm install -g js-debug-adapter` и `composer global require xdebug/vscode-php-debug` обе отвечают 404 (проверено 01.09.2026) — тот же дефект, что `brew install phpactor`; теперь распаковка релизных архивов туда, где фабрика адаптер и ищет, и тест падает на возврате мёртвой команды (проверен подложенным дефектом). Точка расширения помечена у LSP4IJ как экспериментальная — риск назван. ✅ **ступень 2: адаптеры в дистрибутиве** (01.09.2026, решение владельца) — vscode-js-debug 1.117.0 и vscode-php-debug 1.40.1 закреплены по sha256 в `download.sh`, распаковываются в `servers/dap/<id>`, объявлены в лицензионном отчёте; гейт дистрибутива их **запускает** (js поднимает сервер отладки, php отвечает успехом на `initialize` — pathFormat обязателен, без него адаптер отвечает отказом, и проверка «ответил ли» приняла бы отказ за успех). Собран dmg 01.09.2026 (826 МБ) и прогнан гейт дистрибутива: адаптеры на месте внутри `plugins/vibe-lsp/servers/dap/`, php-debug ответил `"success":true` **из самой сборки**, js-debug поднял сервер отладки. ✅ **формы запуска** (01.09.2026): `npm run dev` для фронта и встроенный сервер PHP с включённым Xdebug рядом с «текущим файлом» и attach — пока была одна форма, всем, чей код живёт за дев-сервером, приходилось дописывать JSON руками, то есть ровно то, ради чего фича и делалась (5 тестов). Осталось: живой прогон точки останова человеком в установленной IDE — юнит-тест её не ставит.
- [x] **`platform/lsp` проверен** — ✅ (2026-09-01) модуль открыт, лежит в дереве и уже едет в наш дистрибутив; 31 кастомайзер покрывает языковые фичи. Но **DAP в платформе нет**, значит LSP4IJ остаётся ради отладки и миграция не даёт выигрыша. Решение: мигрировать, когда появится DAP; проверять при каждом синке (`ls platform | grep -iE "^dap|debugAdapter"`). Разбор — [knowledge/languages/platformLspVsLsp4ij.md](knowledge/languages/platformLspVsLsp4ij.md).

## Фаза 3 — Агентская обвязка (плагин `com.vibe.agent`)

- [~] ACP-клиент — ✅ (2026-08-23) `vibe-plugins/vibe-agent/` (`com.vibe.agent`): собственный минимальный клиент (ndjson JSON-RPC 2.0/stdio, ~250 строк, без внешнего SDK — kotlinx-serialization JsonElement без компилятор-плагина): `initialize`/`session/new`/`session/prompt`/`session/update`, permission-диалог (закрыт = отказ), спавн без оболочки, PATH-фикс для GUI macOS. `plugins/vibe-agent/lib/vibe-agent.jar` в дистрибутиве. Осталось: живой прогон с реальным агентом; миграция на официальный Kotlin SDK — осознанная развилка (см. decisions №14).
- [~] UI — ✅ ToolWindow «Vibe Agent» (Swing, путь ProxyAI): выбор агента, стриминг-транскрипт, ввод, стоп. ✅ rich-рендер (сверка 01.09.2026): `MarkdownInline`, `MessageSegments`, `CodeBlockPanel` с подсветкой, `ThoughtsBlock`, `TerminalConsole`. JCEF по-прежнему не берём — решение по мере роста.
- [~] Маппинг ACP→платформа — ✅ `fs/read_text_file` видит несохранённые правки (Document-first), `fs/write_text_file` — WriteCommandAction для открытых файлов, NIO+async VFS refresh для остальных. ✅ **закрыто** (сверка 01.09.2026): diff-превью перед записью — `acp/WritePreview.kt` на `DiffManager`, вопрос «был→стало» ДО записи, закрытый диалог = отказ; `terminal/*` реализованы в `AgentPanel` целиком (create/output/wait_for_exit/kill/release) со своим реестром процессов, а не через тулвиндоу Terminal — вывод нужно перехватывать, фильтровать и сжимать до отправки модели, чего чужая консоль не даёт.
- [x] Совместимость с `~/.jetbrains/acp.json` — ✅ (2026-08-23) толерантный парсер (битая запись пропускается), дефолт — Claude Code через `npx @agentclientprotocol/claude-agent-acp`; спека формата: [manuals/acpAgentsSpec.md](manuals/acpAgentsSpec.md).
- [~] **providers.json (контракт VibeIDE)** — ✅ (2026-08-23) парсер/мерж/extends/Guard/ключи (`com.vibe.agent.providers`), прямой стриминговый LLM-чат (openai+anthropic SSE) в панели Vibe Agent; спека [providersSpec.md](manuals/providersSpec.md). ✅ models.fetch (авто-каталог, мерж по id, static сильнее) и нативный gemini-протокол (streamGenerateContent SSE) — 2026-08-23. ✅ FIM-автокомплит (2026-08-23), ✅ каталог провайдеров вместо встроенных (2026-08-28, №24). ✅ кэш и вотчер (сверка 01.09.2026): `VibeProvidersWatcher` следит за `providers.json`, каталогом `providers/` и `.vibe/.env` обоих контуров и публикует `ProvidersChangeListener.TOPIC` — панель перечитывает реестр без перезапуска IDE; каталог моделей кэшируется в системном пути IDE (`ModelCatalogCache`).
- [x] **Перенос контрактов VibeIDE** — ✅ (2026-08-30) hooks, turn checks, предохранители, VERIFY-GATE, HTTP API, Skills закрыты отдельными пунктами; бюджеты — ролевые с отчётом расхода; Config Guard проверяет пять конфигов при открытии проекта; защита папок — `AccessPolicy` (проект, `sourceFolders`, папки-справочники, `.vibe/ignore`).

- [x] **Тема Vibe Neon Dark** — ✅ (2026-08-23) `vibe-plugins/vibe-theme/` (`themeProvider`, parentTheme Dark, editorScheme от Darcula, неоновые акценты); бандлится как обычный плагин.
- [x] **FIM-автокомплит** — ✅ (2026-08-23) `com.vibe.agent.fim.VibeFimProvider` (DebouncedInlineCompletionProvider, 250 мс): модели `fim: true` из providers.json, `/completions` prefix/suffix, механика VibeIDE (25 строк, стопы, 300/96). Превосходит оригинал: у VibeIDE dynamic-FIM был `null` («follow-up»).
- [x] **Хаб настроек Tools → VibeIDEA** — ✅ (2026-08-23, next) «Провайдеры»: карточка на провайдер, ключ → PasswordSafe, «Проверить» пингует каталог моделей, статус называет победивший источник ключа; «Модели»: группы по провайдерам, AND-поиск, тумблеры видимости (side-map в PropertiesComponent). Фикс 2026-08-23: карточки следуют ширине вьюпорта (Scrollable + авто-перенос html-лейблов) — раньше длинная подсказка уводила поле и кнопку за край. Фикс 2026-08-24: `Configurable.NoScroll` на «Провайдерах» и «Моделях» + общий `TracksViewportWidthPanel` — платформа оборачивала страницу вторым скроллом и давала горизонтальную прокрутку по ширине html-подсказок. Дополнение 2026-08-27: подсказка карточки называет источник записи — глобальный `~/.vibe` / проектный / переопределение (`ProviderOrigin` проставляется при мерже в `ProvidersService.load`).
- [x] **Композер чата (волна B UX-паритета с VibeIDE)** — ✅ (2026-08-23, next) `com.vibe.agent.ui.composer`: автовысотное поле, @-меню без потери фокуса (паттерн FileTextFieldImpl), чипы контекста, резолвер ручных @-токенов (+тесты), вложения-картинки (выбор/вставка/дроп), пилюли «Режим ▾» (ACP `session/set_mode`) и «Модель ▾» (поиск, персист), очередь инъекций, лендинг сверху → композер под лентой, Стоп = `session/cancel` (процесс живёт), один ход за раз. Протоколы: ACP `resource`/`resource_link`/`image` + capabilities/modes в клиенте; LLM — image-части в трёх протоколах, поле `vision` в providers.json. Настройка «Чат» (текст ▷). Токены `Vibe.Composer.*`. Тесты модуля: `./bazel.cmd test //vibe-plugins/vibe-agent:vibe-agent_test`. Вне волны: PDF-вложения (нужен PDFBox — решение по зависимости), быстрые действия лендинга (ждут `vibe.*`-команд), кольцо контекста (нет учёта токенов). Slash-меню добавлено 2026-08-24 по запросу владельца: `/commit` + `/skill:` из `.vibe/skills/` (SlashPopup, тот же фокус-безопасный скелет); `/watch`/`/shot` — с их пайплайнами.
- [x] **История чатов (волна C UX-паритета)** — ✅ (2026-08-24, next) `com.vibe.agent.history` + `ui/history`: глобальный стор тредов (JSON в конфиг-каталоге — по спеке VibeIDE, НЕ `.vibe/`; дебаунс 1,5 с, атомарная запись, кап 500 с маркером), три поверхности (лендинг «Прошлые чаты», попап «история ▾», рейка 280px с датами), скоуп «Этот проект/Все проекты +N», транскриптный поиск (веса 12/6/2/1, цитаты, открытие на сообщении с подсветкой 2,6 с), вкладки (22 символа, ×, «+», лимит 5, персист на проект), per-thread черновик/модель, действия строки (переместить/дублировать/удалить ✕/✓), 4 команды палитры (история/экспорт md+json/чистка/привязка), настройки вкладок и капа. Тесты: 82 юнита (стор, кодек, поиск, даты, скоуп).
- [x] **Убрана приписка «-EAP» из имени приложения** — ✅ (2026-08-24) `VibeIdeaApplicationInfo.xml`: `eap="false"` + `majorReleaseDate` (без него сборка релиза падает: «majorReleaseDate may be omitted only for EAP»).
- [x] **Лого VibeIDEA** — ✅ (2026-08-27, next) выбран владельцем вариант «терминал + искра ИИ» (промпт `>`, строка ввода, четырёхлучевая искра; неон циан→фиолет→маджента на тёмной плашке) из ~14 кандидатов за 3 итерации. `vibeidea.svg` (64) + упрощённый `vibeidea_16.svg` (+eap-копии) в `vibeidea-customization/resources`; macOS Dock-иконка — свой `resources/mac/vibeidea.icns` (icns не берётся из SVG — прописан `icnsPath`/`icnsPathForEAP` в `VibeIdeaProperties`). Проверено в собранном dmg: иконка в бандле и Dock — наша.
- [x] **Локализация плагина vibe-agent** — ✅ (2026-08-30, next) завершена: волна 1 (2026-08-29): механизм по правилам владельца — база вкомпилирована и служит подложкой, языки файлами в `~/.vibe/lang/<код>.json`, свой английский поставляется таким же файлом (правила проверены на себе), файл базового языка НЕ засевается, но создаётся кнопкой; папка читается один раз при старте; цепочка отката сохранённый → системный → базовый; плоские ключи, именованные подстановки, отсутствующее значение оставляет видимый `{name}`. Гейт `vibe-plugins/tools/checkVibeI18n.sh`: ключ без строки, мёртвый ключ, неизвестный ключ в языке и **храповик** — число русских литералов не может вырасти (916 → 859 после первой волны). Спека [manuals/langFileSpec.md](manuals/langFileSpec.md) + README рядом с языками. Перенесены: страница «Интерфейс», диспетчерская, панель «Дизайн», сообщения `/watch`. 14 тестов (в т.ч. сверка подстановок базы и перевода), 471 зелёный. Волны 2–8 (2026-08-30): каталог стал **общим для всех плагинов** (`SRC=vibe-plugins`), перенесён весь остальной интерфейс — чат-панель, композер, @-меню, слэш-меню, история, вкладки, терминал, пикеры, лендинг, настройки, звук, ACP-слой, санитайзер, хуки, скиллы, гейты, предохранители, дизайн-панель, отчёт каталогов, граф, прогоны, серверы. Каталог 718 ключей, английский полный. Литералы, которые человек не видит (регэкспы детекторов, преамбулы ролей, тело ответа HTTP API, значения `.vibe/codeGraph.json`), вынесены в `i18nExclusions.txt` с причиной у каждой строки; русские комментарии и логи переписаны по-английски. Счёт храповика переписан с `grep` на python — POSIX-диапазон `[А-Яа-яЁё]` считал кириллицей типографские тире и точки-разделители, из-за чего планка держалась на пунктуации; после починки **планка 0**, и любая новая русская строка мимо каталога валит гейт (проверено подсадкой). 495 тестов зелёные.
    - [x] Волна 2 — ✅ (2026-08-29): перенесены страницы «Чат», «Провайдеры», «Модели», «Агент» (целиком, включая 14 длинных подсказок) и действия истории. 176 ключей, храповик 916 → 715, английский перевод полный.
    - [x] Волна 3 (2026-08-30): перенесены сообщения ВСЕХ дизайн-правил (пол качества, разметка, стиль) — 346 ключей, храповик 569 → 534. Попутно исправлен дефект вчерашнего решения: исключение из храповика файла целиком (`DesignStyleRules.kt` ради семи регэкспов) прятало ~29 интерфейсных строк; регэкспы вынесены в `DesignPhrases`/`SecurityPhrases`, исключаются только они. Тест: у каждого правила каталога есть и сообщение, и объяснение «почему».
    - [x] Волны 4–8 — ✅ (2026-08-30): перенесён весь остальной интерфейс (чат-панель, композер, @- и слэш-меню, история, вкладки, терминал, пикеры, лендинг, настройки, звук, ACP-слой, санитайзер, хуки, скиллы, гейты, предохранители, дизайн-панель, отчёт каталогов, граф, прогоны, серверы). Каталог 718 ключей, английский полный, планка храповика **0** — гейт строгий (проверен подсадкой строки).
- [x] **PDF-вложения в композере** — ✅ (2026-08-31, next) внешний `pdftotext` (poppler), чип с числом страниц, отказ на скане, отчёт об обрезке. Первая сборка dmg показала, что PDFBox из flexmark в дистрибутив не попадает вовсе, — решение по зависимости пересмотрено на внешний движок.
- [x] **Пайплайны `.vibe/pipelines.json`** — ✅ (2026-08-23) контракт VibeIDE (роли закрытым списком, ≤20 шагов, пути+резюме, провал→«пропущен», толерантный парсер); прогон поверх ACP; отклонение зафиксировано в спеке: роль не ограничивает инструменты физически. Спека: [pipelinesSpec.md](manuals/pipelinesSpec.md).

## Путь переноса VibeIDE→VibeIDEA (очередь по gap-анализу 2026-08-23)

1. [x] E2E ACP — ✅ (2026-08-29) два слоя: **автоматический** `AcpClientE2ETest` — настоящий `AcpClient` против `FakeAcpAgent` (свой агент ACP на JVM в testSrc: NDJSON по stdio, 10 сценариев), 18 проверок: рукопожатие и анонс capabilities (включая `_meta.terminal_output` и условный `terminal`), порядок и полнота стрима, выживание reader'а на битом кадре, обратные вызовы (permission: разрешение/отказ/исключение обработчика; fs read+write), terminal/* с флагом и без (без флага — ошибка JSON-RPC, а не тишина), неизвестный метод, set_mode, отмена (ход прекращён, процесс жив), сопоставление ответов по id при обратном порядке, смерть агента (код выхода + провал висящих запросов) и тихий `stop()`. Герметично: ни node, ни сети, ни установленного Claude Code. **Ручной** чек-лист — [manuals/acpSmoke.md](manuals/acpSmoke.md), 12 шагов (diff-превью, живой вывод терминала, гейт разрушительных команд, хуки, VERIFY-GATE). 265 тестов зелёных.
2. [x] Diff-превью перед записью — ✅ (2026-08-23) WritePreview: модальный платформенный диффер «Было→Станет» на каждый fs/write, закрыт = отказ; неизменённое содержимое — без диалога
3. [x] Терминал агента — ✅ (2026-08-27, next) два слоя: Claude-стрим `_meta.terminal_output` в консоль ленты (ANSI-очистка, head+tail 80k, бейдж выхода) + стандартные ACP `terminal/create|output|wait_for_exit|kill|release` для сторонних агентов через `GeneralCommandLine`/`OSProcessHandler` (kill дерева, `outputByteLimit` с обрезкой с начала). Гейт разрушительных команд — порт детерминированного классификатора VibeIDE (`ShellSafetyAnalyzer`, разбор строки по `&&`/`;`/`|`). Флаг capability `terminal` — Settings → Агент. Классы: `terminal/AgentTerminalService`, `terminal/TerminalOutputBuffer`, `guard/ShellSafetyAnalyzer`, `ui/TerminalConsole`; capability + `terminal/*`-ветки в `AcpClient`.
4. [x] Хуки `.vibe/hooks.json` — ✅ (2026-08-27, next) контракт VibeIDE дословно: preToolUse/postToolUse/turnEnd, exit 0=разрешено(+stdout)/2=отказ/прочее=поломка, stdin-JSON, env `VIBE_HOOK_*`, таймаут 30с/300с, тройной гейт (настройка+TrustedProjects+парсинг). Порт `hooks/HookConfig`+`HookOutcome` (чистые) + `HookRunner` (IO). preToolUse блокирует в точках клиента (request_permission, fs/write) — граница ACP зафиксирована в спеке. Улучшение: preToolUse-заметка exit 0 не теряется (баг VibeIDE). Спека [hooksSpec.md](manuals/hooksSpec.md), засев `.vibe/hooks.example.jsonc`.
5. [x] Turn checks + предохранители + `.vibe/audit.jsonl` — ✅ (2026-08-27, next) проверки изменённых файлов (no-secret-leak, no-protected-path) off/notify/enforce, bounce до N; залипающие security-предохранители (`VibeBreakerService`, PersistentStateComponent workspace-scope, переживают рестарт, блокируют старт хода, снятие вручную с confirm). Аудит `.vibe/audit.jsonl` (off-by-default, приватность как в VibeIDE, ротация 10МБ→gz) — формат [auditSpec.md](manuals/auditSpec.md); предпосылка всего — `ToolCallRegistry` (сборка tool-call'ов по id из session/update). Классы: `gates/TurnChecks`, `gates/VibeBreakerService`, `audit/*`.
6. [x] VERIFY-GATE поверх задач сборки — ✅ (2026-08-27, next) политика VibeIDE как есть (off/warn/enforce, bounce до maxAttempts, stop; launch-fail=инертен), команда/режим/попытки/таймаут в настройках; триггер — конец хода с мутацией файлов (устраняет дефект «гейт только на vibe_complete»). Классы: `gates/VerifyGatePolicy` (чистая) + `gates/VerifyGateRunner`; bounce-цикл в `AgentPanel.promptAcpTurn`. 64 юнит-теста на чистую логику всех четырёх пунктов.
7. [x] HTTP API loopback — ✅ (2026-08-29) контракт VibeIDE дословно (`/health`, `/run`, коды 200/400/401/403/404/413/500/503): `HttpApiPolicy` — вся безопасность одной ЧИСТОЙ функцией (порядок отказов origin→Host→токен→размер, чтобы чужой не узнал даже, верен ли токен; Bearer регистронезависим, сравнение `MessageDigest.isEqual`; Host-allowlist с портом и IPv6 против DNS rebinding), `VibeHttpApi` — только транспорт на JDK-сервере, привязка к `InetAddress.getLoopbackAddress()`, тело читается на байт дальше капа и обрывается, CORS не отдаётся. Токен — `SecureRandom` 32 байта в PasswordSafe (не в синхронизируемых настройках), действия «показать»/«перевыпустить». `VibeAgentGateway` выбирает окно (по `sessionId` — проект треда, иначе последнее активное), `AgentPanel.runExternalTask` идёт через обычный `startTurn` (те же очередь, предохранители, хуки, гейты) и освобождает ожидающего на ЛЮБОМ конце хода. Настройки на странице «Агент», применяются без рестарта. 22 теста: 13 чистых на политику + 11 на настоящем сокете. Спека: [manuals/httpApiSpec.md](manuals/httpApiSpec.md). 289 зелёных.
8. [x] Защита от отравления контекста + детекция секретов + нормализация tool-call'ов — ✅ (2026-08-29) асимметрия закрыта: раньше всё смотрело на ВЫХОД агента, за тем, что он читает, не следил никто. `security/ContextSanitizer` (чистый) — три класса, три политики: невидимое (zero-width, 00AD, теговый блок U+E0000–E007F) и bidi вырезаются молча, фразы-инструкции только сообщаются (правка чужого файла хуже предупреждения), секреты сообщаются и маскируются по настройке (файл не трогаем). Встроен в `ContextSerializer.load` — единственную точку обоих путей (ACP-блоки и прямой LLM) — и в `IdeFileOps.readTextFile`. `security/SecretPatterns` — общий список форм для входа и выхода (переехал из TurnChecks). `ToolCall.normalizeTitle/normalizeKind` — одна строка с капом и закрытый список видов: заголовок уходит в аудит и хуки, многострочный был бы подделкой записи. Предупреждение о незнакомом репозитории (`ForeignProjectNotice`, одна строка, не блокирует). Грабли Java-регекспов на кириллице — в [knowledge/agents/contextPoisoning.md](knowledge/agents/contextPoisoning.md). 20 тестов, 309 зелёных.
9. [x] providers: вотчер файлов — ✅ (2026-08-28) `VibeProvidersWatcher` (VFS-события обоих уровней; ~/.vibe через addRootToWatch+refresh; дебаунс 500 мс → ProvidersChangeListener.TOPIC → панель перечитывает сама); кэш последнего набора не переносим осознанно (решение №26, YAGNI). Чистый матчер путей + 3 теста (214 зелёных).
10. [x] Skills (SKILL.md, открытый стандарт) + валидатор — ✅ (2026-08-29) закрыт разрыв, из-за которого навыки были засеяны, но НЕ РАБОТАЛИ: `/skill:<id>` уходил моделью как текст, тела она не видела никогда. Теперь при отправке навык разворачивается в контекст (`SkillExpansion` — чистое выделение токенов; ACP — `resource` с `skill://<id>`, прямой LLM — `<skill ref>`), проходя тот же `ContextSanitizer`, что и файлы. `SkillPackage` (чистый разбор шапки: вложенные ключи не считаются верхним уровнем, незакрытая шапка не съедает тело) + `SkillValidator` (чистый; лишний ключ верхнего уровня — ОШИБКА, эталонный валидатор отвергает такой пакет целиком; вложение за пределами `.vibe/skills` — ошибка; `scripts/` — предупреждение) + `SkillsStore` (IO). Сломанный навык не отправляется и называет причину в ленте, в списке `/skill:` виден с пометкой. Действие «VibeIDEA: проверить скиллы проекта». Спека [manuals/skillsSpec.md](manuals/skillsSpec.md) по правилу «фича с форматом обязана иметь спеку». Засеянный набор (14 пакетов) прогнан через собственные правила — ноль замечаний. 20 тестов, 329 зелёных.
11. [x] Чекпоинты — ✅ (2026-08-23) CheckpointService: снимок дерева через временный индекс (write-tree/commit-tree, HEAD/индекс не трогаются), журнал .vibe/checkpoints.jsonl, откат с подтверждением + VFS refresh; UI-кнопка со списком
12. [x] Диспетчерская + `agent-runs.jsonl` (heartbeat, «Проверить запуск») — ✅ (2026-08-29) `runs/AgentRunLedger` (чистый): JSONL append-only со свёрткой по runId, недоверенный разбор (битая строка пропускается), правило сиротства ПО ФАКТУ (три пропущенных heartbeat + epoch не принадлежит живому окну; `finishedAt` сироты — момент последней отметки), усечение по лимиту и сроку с защитой незавершённых, сводка и поиск. `runs/VibeAgentRunService` (IO): epoch окна, heartbeat 30 с, атомарная компактизация раз в час. Пишутся ходы БЕЗ наблюдения — HTTP API и пайплайны; обычный чат осознанно не пишется (есть история тредов), ограничение названо в панели и доках. Тулвиндоу «Диспетчерская»: фильтры, поиск, сводка с явными нулями. Только метаданные — закреплено тестом. `agent-runs.jsonl` добавлен в .gitignore набора сидов (VibeBrains 41683bc, bump ревизии). Спека [manuals/agentRunsSpec.md](manuals/agentRunsSpec.md). 11 тестов, 340 зелёных.
13. [x] FIM: расширение (типы предсказаний VibeIDE, фильтры не-кода, кэш+SLA-метрики) — ✅ (2026-08-29) провайдер разобран на четыре ЧИСТЫХ куска: `FimPrediction` (пять типов запроса по месту курсора, включая отсутствовавший у нас отказ DO_NOT_PREDICT и перезапись хвоста вместо дописывания к скобке; окно 25/12 строк для облака/локали; факт принятия подсказки выводится по документу, а не через внутренний listener платформы), `FimFilters` (строки-пояснения и CJK-комментарии режутся, легальный юникод в коде — нет; это и есть граница, которую нельзя перейти), `FimCache` (LRU по нормализованному префиксу — без нормализации кэш не попадал бы ни разу) и `FimMetrics` (перцентили, а не среднее: среднее прячет ровно тот хвост, из-за которого автокомплит кажется сломанным). Действие «метрики автодополнения», настройки (вкл/дебаунс/размер кэша). 24 теста, 364 зелёных.
14. [x] `servers.json` — ✅ (2026-08-23) `vibe-plugins/vibe-server` (`com.vibe.server`): контракт VibeIDE целиком (task/service, 5 readyCheck, волны топосортом, skipIf, pathPrepend, envFile, stopCommand, исключения с причинами), тулвиндоу. Спека serversSpec.md. Осталось: autoStart с гейтом доверия, previewPath-превью. Решение: свой раннер, НЕ Run Configurations (волны+readyCheck в RC не выражаются)
15. [x] `code_graph` — ✅ второй срез (2026-08-29): `CodeGraphIndex` (чистый) строит рёбра из уже разобранных импортов с ПРОИСХОЖДЕНИЕМ — `факт` (импорт совпал с объявленным полным именем) против `догадки` (совпал только последний сегмент), при неоднозначном простом имени связи нет вовсе; запросы «кто импортирует», «что импортирует», кратчайшая цепочка BFS без учёта направления (цикл не зацикливает, отсутствие пути — тоже ответ). `CodeGraphStore` — инкрементальность по отпечатку (size+mtime, а не хэш: хэш требует прочитать всё, ради чего отпечаток и заведён), формат v2, старый файл читается без ошибки. Рёбра через ReferencesSearch сознательно НЕ делались: поиск ссылок по тысячам файлов стоит минут, а импорт — ровно то ребро, которое нужно, и оно уже разобрано. Спека [manuals/codeGraphSpec.md](manuals/codeGraphSpec.md) с примерами jq — она же «мост для агентов»: агент читает файл сам. 13 тестов, 377 зелёных.
    - [x] MCP-мост — ✅ (03.09.2026, next) развилка решена в пользу **своего MCP-сервера на существующем транспорте**: `POST /mcp` рядом с нетронутыми `/health` и `/run`, тот же loopback и тот же токен. Расширение HTTP API своим неймспейсом отвергнуто: та же работа, но отвечать он умел бы только скриптам, написанным под нас. `McpServer` чистый (14 тестов) + прогон по настоящему сокету; две ревизии протокола, 5 инструментов. Спека — [manuals/mcpSpec.md](manuals/mcpSpec.md), разбор — [knowledge/architecture/mcpBridge.md](knowledge/architecture/mcpBridge.md).

16. [x] Дизайн-контур — ✅ **этап 7: сравнение замеров** (02.09.2026, next) `DesignDiff`: появилось/исправилось/осталось по ключу «правило + селектор + вьюпорт», появившиеся нарушения пола уходят в композер сами, первый замер регрессией не считается, дрейф измеренного значения не считается новой находкой (7 тестов). Прежнее: — ✅ контракт `.vibe/design/` (2026-08-23) + ✅ **этап 1: движок детекторов** (2026-08-29): `DesignSnapshot` — контракт «что видно правилам» (родство ЧИСЛОМ, а не префиксом селектора: правило, сравнивавшее строки, объявляло обычную вложенность перекрытием), `DesignRuleCatalog` — единственное место, где решается «пол или вкус» (правило, называющее свой класс само, могло бы разойтись с каталогом, который читает проект), `DesignFloorRules` (контраст WCAG с порогом 3:1 для крупного текста, мелкий кегль, зоны нажатия, обрезание, перекрытие, страница шире окна, битая картинка, снятый фокус, выключенное выглядит рабочим, пропуск уровня заголовка), `DesignMarkupRules` (кнопка-иконка без имени, плейсхолдер вместо подписи, отсутствующий alt — пустой законен, ошибка без привязки, обязательность звёздочкой), `DesignStyleRules` (14 примет сгенерированного вида + русская типографика по ИЗМЕРЕННОЙ разбивке строк), `DesignReview` (класс из каталога, приём отклонений с причиной — но пол принять нельзя, слияние двух вьюпортов). Каждое правило молчит, когда не может знать (стили не прочитаны, нет замера). 36 тестов: каждое правило проверено И на срабатывании, И на легальном случае — детектор, ругающийся на правильное, перестают читать. 413 зелёных.
    - [x] Этап 2 — ✅ (2026-08-29): мост замера на **JCEF**, а не `ui.webview`: платформенный WebView отдаёт бандленные ассеты, а мерить надо произвольную страницу пользователя (localhost, staging) — `JBCefBrowser.loadURL` это умеет, WebView нет. Сборщик `resources/design/collect.js` (275 строк) читает вычисленные стили, эффективный фон подъёмом по прозрачным предкам, правила `:focus`/`:hover` из таблиц стилей документа (нечитаемые помечает — «правила нет» иначе означало бы «не смогли посмотреть»), доступные имена по обычному порядку и разбивку строк через Range с бюджетом на страницу; страницу не меняет ничем. `DesignBridge` — JCEF возвращает значения только через `JBCefJSQuery`, одноразовый обработчик снимается сразу (иначе второй замер приходил бы всем прежним слушателям). `DesignSnapshotCodec` (чистый, 6 тестов): битый элемент выбрасывается, отсутствующее поле не превращается в замер. Тулвиндоу «Дизайн»: адрес, «Открыть», «Замерить», список находок с селектором, измеренным значением и вьюпортом; мобильный проход ФИЗИЧЕСКИ сужает компонент до 390 px и ждёт перекладку. Отклонения берутся из `.vibe/design/` — того же файла, что читает агент. 419 зелёных.
    - [x] Этап 3 — ✅ (2026-08-29): `resources/design/overlay.js` рисует рамку с подписью правила поверх найденного (цвет различает пол и вкус), клик по подписи отдаёт находку в **композер**, а не запускает ход — решение, что с ней делать, остаётся за человеком. Кнопка «⚑ разметка» убирает разметку. Страница получает находки ДАННЫМИ и имя колбэка, а не кодом; принятые проектом отклонения не рисуются вовсе. Клик-канал (`JBCefJSQuery`) создаётся один раз на панель: запрос на каждую отрисовку тёк бы обработчиком, а страница звала бы исчезнувшую функцию. `DesignOverlay.asChatNote` у вкусовой находки прямо говорит, что это не дефект — иначе агент «чинит» то, что проект выбрал осознанно. 5 тестов.
    - [x] Этап 4 — ✅ (2026-08-29): `DesignHookPolicy` (чистая) — off/notify/enforceFloor, «затрагивает ли правка интерфейс» по расширениям (`.test.tsx` — не интерфейс), bounce ТОЛЬКО по полу качества с лимитом попыток, требование перечисляет нарушения с числами и прямо говорит, что стиль не блокирует. Подключено к тому же циклу, что VERIFY-GATE и turn-checks (`GateBounce` получил третий счётчик попыток). `DesignMeasurementService`: панель «Дизайн» регистрируется измерителем, а закрытая панель даёт не молчание, а причину — молчание читалось бы как «страница в порядке». Настройки на странице «Агент»; по умолчанию off (гейт, который не может работать, не должен ворчать). 9 тестов, 433 зелёных.
    - [x] Этап 5 — ✅ (2026-08-30, next) `DesignDoctor` (чистый) с правилом сходящейся арифметики (пол + вкус = всего) и поиском опечаток в принятых отклонениях; действие «проверка дизайн-контура». `UiKitMap` (чистый) снимает карту построенного с кода в `.vibe/design/uiKit.md`; существующий файл не перезаписывается без спроса — скан затёр бы ручные правки, ради которых карту и держат.
    - [x] Этап 6: каталог правил 29 → 53 → **81** — ✅ (2026-08-30 и 2026-08-31) «находимость страницы» (11) и «ритм и типографика» (6), затем формы, движение и цвет (см. отдельный пункт ниже). Счёт сверен фактом 01.09.2026: `DesignRuleCatalog.ALL` — ровно 81 идентификатор, дублей нет.

17. [x] Видео-разбор (`/watch`) — ✅ (2026-08-29) пайплайн дочерними процессами: проба → субтитры → медиа → кадры по сменам сцен, перехват команды в композере, кадры вложениями, транскрипт с тайм-кодами, vision-гейт до и после, работающий Стоп на многоминутном скачивании. Перенесены грабли VibeIDE вместе с решениями и закрыты тестами: `#` режется только у URL (в имени файла он легален), yt-dlp сам выбирает контейнер при мерже (`-o video.%(ext)s` + глоб), статичный ролик не пробивает порог сцен (якорь `eq(n,0)` + автоповтор на 0.1), `showinfo` печатает размер как `s:WxH` или `s=WxH` (разбор принимает оба — **живая проверка на ffmpeg 8.1.2 дала `s:`**, вопреки записи VibeIDE про 8.x), положительное свидетельство видео сильнее подсказки по имени, `(attached pic)` — обложка, а не видео, ненулевой код `ffmpeg -i` без выходного файла — норма. Бинари из системы: своего зеркала и релизной инфраструктуры нет, поэтому при отсутствии — точная команда установки. STT-фолбэка нет (нужен п.18), аудио без субтитров даёт отказ с причиной. 24 теста, 457 зелёных.
18. [ ] Голосовой ввод (sherpa-onnx локально) — M

Платформа даёт бесплатно (не переносить): PasswordSafe, TrustedProjects, Document/VFS-трёхслойка, DiffManager/LineStatusTracker, Local History, PSI-поиск вместо ripgrep, Terminal/RunConfigurations/Services, InlineCompletionProvider, git4idea, инспекции+авто-импорт, AsyncFileListener, Markdown-рендер.

## Ревизия и доработка (2026-08-30, после переноса)

Перечитывание сделанного: что найдено, что чинится, что добавляется.

### Найденные дефекты

- [x] **Индекс справки списком в коде** — разошёлся с папкой (16 против 18), два мануала `/help` не находил. Индекс стал файлом от `syncHelp.sh`, заголовки читаются из документов, тест проверяет полноту.
- [x] **Таймер дневной сводки жил вечно** — не останавливался с закрытием проекта. Привязан к проектному сервису.
- [x] **Учёт расхода писал файл на каждый чанк стрима** — вопреки обещанному в доке дебаунсу. Раз в 15 с, со сбросом перед чтением.
- [x] **Счётчики чата не сбрасывались при переключении вкладки** — новый чат наследовал токены и «уже сказанные» предупреждения.
- [x] **Дельта расхода уходила в минус** — ACP отдаёт суммарное заполнение окна, новый ход начинает заново, дневной расход тихо уменьшался.
- [x] **`PromptCache.cacheBoundary` был мёртвым кодом** — кешировалась только системная часть; теперь помечается настоящая граница префикса.
- [x] **Три места обходят дерево проекта по-своему** — панель «Документы» игнорировала `.vibe/ignore`, хотя карта построенного и RAG его чтут: файл, спрятанный от агента, оставался виден в панели. Обход вынесен в общий `ProjectFiles`.

### Вторая ревизия (2026-08-30)

- [x] **Команды были описаны дважды** — строка в меню и `startsWith` в панели, синхронизированные грепом. Разъехалось уже: `/watch` работал, а в меню его не было. Таблица команд стала единственным источником — меню строится из неё, панель из неё же диспетчеризует.
- [x] **Команда без обязательного аргумента уходила модели как текст** — `/bg` и `/blame` без аргумента превращались в вопрос. Теперь ответ единообразный: чат называет, чего не хватает.
- [x] **Панель разрослась до 3388 строк** — разбор команд вынесен в `ChatCommands` и наконец покрыт тестами (раньше ни один обработчик тестом не закрывался).
- [x] **Осиротевший импорт** после переезда на общий обход дерева.
- [x] **`/map`** — карта проекта mermaid-диаграммой: модули и зависимости между ними, читаемые одним взглядом.
- [x] **`/rules`** — какие правила проекта сейчас применяются и почему: всегда, по globs или только по имени.
- [x] **Настройки, до которых нельзя было дотянуться** — папка документации, языки субтитров, число и высота кадров `/watch`, проект и прокси Telegram правились только редактированием properties. Настройка, которую нельзя открыть, — это настройка, которой нет.
- [x] **Дубли в каталоге строк** — три пары ключей с одинаковым текстом («Агент · {time}», «Правка на месте», «Карта построенного»): переводчик, видящий одну фразу дважды, однажды переведёт её по-разному, и интерфейс разойдётся сам с собой.

### Добавляется

- [x] **JSON-схемы для `.vibe/*.json`** — автодополнение и проверка прямо в редакторе для providers, servers, hooks, pipelines, commands. Конфиг, который правят вслепую, — источник большинства «почему не работает».
- [x] **`/undo`** — откат последней правки агента без поиска нужного чекпоинта глазами.
- [x] **`/blame <файл>`** — почему код такой: последние коммиты, тронувшие файл, с их сообщениями, вместо угадывания намерения по коду.
- [x] **`VibeIDEA: диагностика`** — один отчёт для разбора «почему не работает»: версии, провайдеры, гейты, языковые серверы, состояние индексов.
- [x] **Кнопка обновления в панели «Документы»** — панель читала дерево только при открытии.

## Паритет с VibeIDE (сверка 2026-08-30)

Полная таблица — [parityVibeIde.md](parityVibeIde.md). Очередь собрана по одному признаку: **мешает ли
это каждый день**. Самостоятельные продукты внутри продукта (Deploy, Telegram, обучение) стоят последними
не потому, что неинтересны, а потому что до них нужно закрыть то, обо что спотыкаешься в работе.

### Волна 1 — то, обо что спотыкаешься каждый день

- [x] **Правила проекта** — ✅ (2026-08-30, next) `.cursor/rules/*.mdc` + legacy `.cursorrules`: три вида применения (always / globs / по имени `@правило`), ссылки `@имя` подтягивают правила один раз и без циклов, почта ссылкой не считается, файл без заголовка всё равно правило, тело длиннее 20 000 символов обрезается и проходит санитайзер. Блок правил идёт ПЕРЕД задачей в обе ветки — ACP и прямой LLM.
- [x] **Папки-справочники и `sourceFolders`** — ✅ (2026-08-30, next) `AccessPolicy` (чистая) отвечает READ_WRITE / READ_ONLY / DENIED до действия; команды подключения и отключения папки; запись в справочник отклоняется всегда, чужой путь вне проекта не читается вовсе.
- [x] **`.vibe/ignore`** — ✅ (2026-08-30, next) подмножество gitignore (имя, путь, `**`, каталог, отрицание с правилом «побеждает последнее совпадение»); действует и на чтение агентом, и на `@`-меню — предлагать файл, который запрещено читать, значит расставлять ловушку.
- [x] **Булавки на сообщениях** — ✅ (2026-08-30, next) закреплённое вынимается из уничтожаемой части и переносится вперёд хвоста; маркер «история обрезана» считает только реально уничтоженное (иначе врал бы про спасённое); булавка переживает дублирование чата и обход через JSON.
- [x] **Ветка разговора** — ✅ (2026-08-30, next) копия до выбранного сообщения открывается отдельным чатом, исходный не меняется; точка среза отъезжает назад через служебные строки — ветка, кончающаяся на «ход отменён», дала бы модели разговор, который нечем продолжить.
- [x] **Git-инструмент состояния репозитория** — ✅ (2026-08-30, next) команда `/git [вопрос]`: ветка с upstream и расхождением, изменённые файлы с размером правки, последние коммиты. Парсеры чистые и покрыты тестами на реальные формы вывода git — стрелка переименования (берём имя, которое у файла сейчас), «- -» двоичного файла (иначе читалось бы как «ноль изменений»), detached HEAD, ветка без upstream. Сортировка по размеру правки, потолок 25 файлов с честным «ещё N». git вызывается argv-списком, без shell: путь проекта — это данные, а через shell папка с `;` стала бы командой. Осталось: перенаправление `git status/diff` из терминала агента на этот же отчёт.
- [x] **Сжатие вывода команд, обратимое** — ✅ (2026-08-30, next) голова 30 строк + хвост 20 + строка с числом убранных и меткой; полный текст по `/output <метка>` (стор последних 20 выводов сеанса, старые вытесняются). Порог 60 строк: ниже него маркер стоит дороже экономии.
- [x] **Страж окна контекста и лимит на чат** — ✅ (2026-08-30, next) `ContextBudget` (чистый): 75% — предупреждение, 90% — строка о том, что ответ уже формируется выпавшим; каждый порог произносится один раз за чат. Лимит токенов на чат отдельной настройкой (0 = выключен) проверяется ДО хода. Окно, о котором цель молчит, не считается заполненным — красный бейдж без данных это шум.
- [x] **Правка не затирает чужие изменения** — ✅ (2026-08-30, next) `WriteGuard` (чистый) сверяет отпечаток прочитанного с тем, что на диске сейчас: SAFE / CONFLICT / UNSEEN. Сравнивается содержимое, а не время (сохранение без изменений — не конфликт, правка внутри миллисекунды — конфликт); частичное чтение (line/limit) отпечаток не ставит, иначе оконное чтение кричало бы конфликтом; создание нового файла всегда безопасно.
- [x] **Быстрые исправления редактора после правки** — ✅ (2026-08-30, next) `EditorAutoFix` применяет уже посчитанные демоном исправления уровня ERROR по политике `AutoFixPolicy`: только импорты, только однозначные, не больше пяти на файл; применённое называется строкой в ленте. Своего анализа не запускаем — запись не должна превращаться в полный прогон инспекций.

### Волна 2 — надёжность длинных прогонов

- [x] **Детектор петель** — ✅ (2026-08-30, next) `LoopDetector` (чистый) ловит две формы: повтор (4 одинаковых вызова подряд) и ЦИКЛ периодом 2–4, повторённый трижды, — форму «A B A B», которую проверка «как в прошлый раз» не видит. Период ищется от короткого, иначе отчёт называет не ту петлю. Найденная петля взводит предохранитель, а не просто отменяет ход.
- [x] **Dead man's switch** — ✅ (2026-08-30, next) `DeadManSwitch` (чистый): молчание считается от последнего признака жизни, а не от начала хода; на отметке — строка, на удвоенной — остановка; 0 в настройке действительно выключает. Один таймер на панель, а не на ход: таймер осиротевшего хода остался бы греть процессор.
- [x] **Персистентные планы** — ✅ (2026-08-30, next) `AgentPlan` (чистый) разбирает ACP-обновление обеих форм (entries и вложенный plan), `PlanStore` держит план на чат в `.vibe/plans.json` рядом с проектом (план описывает работу ЭТОГО репозитория и стоит того, чтобы быть видимым в diff). При открытии чата незаконченный план называется вслух со следующим шагом. Незнакомый статус — «не выполнен»: ошибка в другую сторону теряет работу. Законченные планы из файла удаляются, всего хранится 20 последних.
- [x] **Права на роль** — ✅ (2026-08-30, next) `RoleRights` (чистый): судящие роли (`code-reviewer`, `security`, `qa`, `explore`, `planner`) не пишут файлы; ревьюеру и QA разрешены команды (прогнать сборку — часть отчёта), разведчику — нет. Принуждение в точке записи и в создании терминала, а не в промпте. Роль сбрасывается по концу шага и пайплайна: унаследованное ограничение в обычном чате хуже отсутствия ограничения; неизвестная роль сохраняет полные права по той же причине. Тест сверяет список читающих ролей с контрактом `PipelinesFile.ROLES`.
- [x] **Бюджет на роль и расход** — ✅ (2026-08-30, next) `SpendLedger` и `RoleBudget` (чистые) + `VibeSpendService` (системный каталог IDE, неделя истории, дебаунс записи). Отчёт по ролям и целям за сутки и за неделю, сортировка по расходу; смешанные валюты не складываются — вместо суммы честное «в разных валютах». Суточный лимит на роль проверяется ПЕРЕД шагом: 80% — предупреждение, 100% — шаг пропущен. Ноль действительно выключает. ACP отдаёт суммарное заполнение окна, поэтому в учёт идёт разница с прошлым отчётом, а не само число.
- [x] **Самовосстановление** — ✅ (2026-08-30, next) `RetryPolicy` (чистая): 429 — очередь, 5xx/408/сетевые — временные, 401/403/400/404 и неизвестное — фатальны (повтор на неизвестной ошибке это способ задолбить чужой сервер). Задержка по `Retry-After` провайдера, иначе удвоение с потолком в минуту; абсурдный `Retry-After` подрезается. Ожидание называется в ленте, «стоп» во время паузы срабатывает сразу. Тест поймал ловушку наследования: `SocketTimeoutException` — наследник `InterruptedIOException`, и без порядка проверок каждый таймаут чтения считался бы остановкой пользователя. Зависший ход закрывает выключатель мёртвой руки.

### Волна 3 — качество ответа

- [x] **Совет моделей** — ✅ (2026-08-30, next) команда `/council`: `CouncilPlan` (чистый) отбрасывает дубликаты моделей и не пускает совет из одного советника, битые записи называет вслух; советники опрашиваются параллельно и не видят друг друга, ответы нумеруются, а не подписываются (судья соглашается со знаменитой моделью); сведение идёт обычным ходом и попадает в переписку.
- [x] **Библиотекарь** — ✅ (2026-08-30, next) `Librarian` (чистый) разбирает индекс базы знаний (таблицы и списки, внешние ссылки не записи) и подбирает 2–3 записи по пересечению значимых слов; в промпт идут пути, а не содержимое. Индекс ищется в четырёх привычных местах и кэшируется по mtime; пустой результат — молчание, а не «ближайшая» запись.
- [x] **Хендофф по форме и чек-лист «проверь глазами»** — ✅ (2026-08-30, next) `HandoffForm` (чистая) собирает передачу из плана и затронутых файлов и называет незаполненные секции; пустые секции помечаются, а не выбрасываются (исчезнувшая читается как «граблей не было»). `EyesChecklist` (чистый) после хода перечисляет видимые файлы и три состояния, которые модель не отрисовывает в голове; тест компонента к осмотру не предлагается — проверка порядка правил поймана тестом (`Button.test.tsx` оканчивается видимым расширением).
- [x] **Трейс хода агента** — ✅ (2026-08-30, next) `TurnTrace` (чистый): события хода с длительностями, сводка по видам, самые долгие шаги и повторы; `/trace` печатает разбор. Шаг без измеренной длительности не считается мгновенным. Формат чисел зафиксирован через `Locale.ROOT` — иначе в русской локали трейс печатал «0,0s» и переставал грепаться (поймано тестом).
- [x] **Динамическая фильтрация контекста** — ✅ (2026-08-30, next) `ContextFilter` (чистый) с режимами auto/raw/aggregate/off: схлопывание повторов с сохранением первого, отсев полос прогресса и разделителей, режим «только форма». Чистка идёт ДО сжатия головы-хвоста, число убранных строк называется вслух.

### Волна 4 — интеграции и окружение

- [x] **Команды проекта `.vibe/commands.json`** — ✅ (2026-08-30, next) `ProjectCommands` (чистый): обе формы файла, отказ (не экранирование) на метасимволах и невидимых символах, первая запись при дубле id, порядок по `order` со стабильным тай-брейком, разрешение хешем текста команды, секреты по имени с видимой ссылкой при отсутствии значения. Список и запуск — действие «VibeIDEA: команды проекта»; спека — manuals/commandsSpec.md. Осталось: закреплённые в отдельном меню и горячие клавиши.
- [x] **Config Guard** — ✅ (2026-08-30, next) `ConfigGuard` (чистый) проверяет пять конфигов `.vibe` при открытии проекта: секрет открытым текстом, http наружу (loopback не в счёт), креды в URL, сырой IP. Предупреждение о пароле не пересказывает пароль (поймано тестом). Пусто — молчит.
- [~] **Прокси для провайдеров и failover** — ✅ (2026-08-30, next) `ProxySettings` (чистый, дефолтные порты, креды в адресе не становятся хостом, опечатка — ошибка, а не тихое «без прокси») применяется к HTTP-клиенту моделей отдельно от прокси IDE; `FailoverPlan` (чистый) уходит к следующей цели только после исчерпания ожидания и никогда на фатальной ошибке, повторов цели в ходу нет. ✅ **внешний каталог квирков** (01.09.2026, next): `.vibe/modelQuirks.json` обоих контуров, запись замещает вшитое правило, `"quirks": []` снимает его, битая запись и неизвестное имя стоят только себя; перечитывается вотчером без перезапуска; строка «Причуды модели» в диагностике называет источник. Спека [modelQuirksSpec.md](manuals/modelQuirksSpec.md) + образец в сидах VibeBrains. 11 тестов.
- [x] **Автодетект локальных моделей** — ✅ (2026-08-30, next) `LocalProbe` (чистый): обе формы ответа (Ollama `models`, OpenAI `data`), сопоставление по хосту и порту с приведением `127.0.0.1`→`localhost`, готовая запись `providers.json` с ограничением на число моделей. Проба четырёх портов параллельно с коротким таймаутом; предложение показывается, но НЕ применяется само, сервер без моделей не предлагается, есть «больше не предлагать». Условие показа — не «провайдеров нет» (дефолты засевают папку шаблонов, и такая проверка молчала бы всегда), а «этот локальный сервер ещё не описан».
- [x] **Справка о продукте внутри сборки** — ✅ (2026-08-30, next) `docs/vibe/functional.md`, `agentsGuide.md` и все 16 спек копируются в ресурсы плагина скриптом `syncHelp.sh`; `HelpBundle` держит явный индекс (перечисление ресурсов в jar ненадёжно), команда `/help` подмешивает подходящие документы в ход. `HelpBundleTest` — гейт синхронизации: индекс без файла и подозрительно короткий каталог валят тест. Гейт брендинга не проверяет копию — упоминания платформы проверяются в источнике.
- [x] **Дневная сводка** — ✅ (2026-08-30, next) `DailyDigest` (чистый): прогоны, упавшие и осиротевшие ОТДЕЛЬНО, расход и роль-лидер; расписание `ЧЧ:ММ` с окном опоздания в два часа (сводка за вчера в полдень — шум) и защитой «раз в сутки» через день последней отправки. Проверка по таймеру, а не точным расписанием: IDE не cron, её открывают в случайные часы.
- [~] **Панель «Документы»** — ✅ (2026-08-30, next) `DocsIndex` (чистый) читает markdown как граф: относительные пути с `..`, якоря и внешние ссылки, циклы, проект без README (обход от первого документа, иначе недостижимо всё). Панель и команда показывают недостижимые документы и ссылки в никуда, клик открывает файл. ✅ граф (сверка 01.09.2026): `DocsGraphLayout` (чистая раскладка) и `DocsGraphView` рисуют связи документов.

- [x] **Офлайн-режим и кеширование промпта** — ✅ (2026-08-30, next) офлайн проверяется в `LlmClient` и в эмбеддингах, а не в интерфейсе: проверка в UI это напоминание, а не гарантия; локальные провайдеры не затронуты. `PromptCache` (чистый) помечает кешируемым только достаточно большой префикс и никогда — последнее сообщение (запись кэша, использованная один раз, — худшее из обоих миров).

### Волна 5 — самостоятельные направления (браться после волн 1–4)

- [x] **Инлайн-правка Ctrl+K** — ✅ (2026-08-30, next) `InlineEditPrompt` (чистый): разбор пяти команд в двух написаниях и свободной инструкции, сборка промпта с постоянными правилами проекта, извлечение кода из ответа с ОТКАЗОМ на прозу (распознаётся по форме, а не по ключевым словам). `/explain` не заменяет код, а показывается. Применение — через тот же diff-превью, что и запись агента.
- [x] **Поиск по смыслу (RAG)** — ✅ (2026-08-30, next) `Chunker` и `VectorIndex` (чистые) + `RagIndex`: окна по 60 строк с перекрытием, отсев вендоренных деревьев и пустых окон, косинусная близость плоским сканом (ANN — зависимость и класс багов «почему не нашло очевидное» в обмен на незаметное время), порог слабых совпадений, один кусок на файл в выдаче. Индекс инкрементальный по size+mtime, лежит в системном каталоге IDE. Команды `/index` и `/find`; модель эмбеддингов — настройкой, пусто = выключено.
- [~] **Превью: размеры, живая перезагрузка, адрес для телефона, ошибки в чат** — ✅ (2026-08-30, next) ширина меняется у компонента (медиазапросы срабатывают по-настоящему), перезагрузка при сохранении, `LanAddress` (чистый) отсеивает loopback, docker, link-local и диапазоны VPN, сборщик ошибок ставится до первой ошибки и отдаёт их в композер. QR-код адреса — ✅ (2026-08-31): `QrCode.kt`/`PhoneAddressDialog.kt` на ZXing, библиотека едет в дистрибутив по просьбе раскладки плагина. ✅ **Недавние адреса** (01.09.2026): полоса кнопок под тулбаром, переключение в один клик, список хранится по проекту (7 тестов). **Вкладок как таковых не делаем и вот почему**: каждая вкладка — отдельный процесс CEF на сотни мегабайт, и это сломало бы инвариант «панель владеет одним браузером, который и меряет дизайн-гейт» — второй браузер молча мерил бы другую страницу. Просили же не вкладки, а переключение между тремя-четырьмя адресами, которые открывают весь день.
- [x] **Минимализм, `/simplify`, Spec-First и оптимизация под метрику** — ✅ (2026-08-30, next) `MinimalismPolicy` — лестница off/light/full/ultra; `SimplifyPrompt` разбирает unified diff (тест поймал сдвиг на строку: заголовок ханка считался строкой файла) и читает ответ как делит-лист; `SpecPackage` проверяет PRODUCT.md на нумерованные инварианты и TECH.md на заземление в коде (отсутствие файла — ошибка, слабый файл — предупреждение: на черновик не кричат), действие заводит спеку из шаблона и не перезаписывает существующее; `MetricRun` берёт последнее совпадение шаблона (сводка печатается в конце), направление решается до работы, «числа нет» не превращается в ноль.
- [~] **Мост в Telegram** — ✅ (2026-08-30, next) `TelegramProtocol` (чистый): разбор сообщений и нажатий кнопок, команды с `@имя_бота`, обрезка по лимитам Telegram, чтение id отправленного сообщения для редактирования прогресса. `TelegramBridge`: long polling (webhook требует публичного адреса, а IDE живёт за NAT), токен в PasswordSafe, разрешение чата владельцем из уведомления, свой прокси, задачи через тот же gateway, что и HTTP API. Подтверждения разрушительных команд кнопками — ✅ (2026-08-30): вопрос идёт в оба места, отвечает первый, таймаута нет намеренно. ✅ голосовые (2026-08-31): расшифровка локальным whisper, причёсывание локальной моделью, эхо распознанного до старта работы — пункт закрыт, формулировка устарела с того же дня.
- [~] **План выкатки** — ✅ (2026-08-30, next) `DeployPlan` (чистый): тип проекта по файлам (Dockerfile побеждает догадки), порядок «сперва локальное и обратимое», пометка внешних шагов, предупреждения про `.env` в корне и отсутствие `.dockerignore`. Команда `/deploy`. Осталось решение владельца: интеграция с конкретным облаком (Timeweb) — нужны учётные данные и аккаунт, внешние действия без явного согласия мы не делаем.
- [x] **Обучение с накоплением прогресса** — ✅ (2026-08-30, next) `LearningPlan` (чистый): миссия как гейт (три вопроса, недостающие спрашиваются по одному), сложность по ДВУМ последним попыткам (одна — это день или везение), пустая попытка не читается как «ответил на всё», урок собирается из миссии, прогресса и источников. `LearningStore` держит прогресс в `.vibe/learning/` рядом с проектом и находит RESOURCES.md в трёх привычных местах. Команда `/learn`.

### Усовершенствования обвязки (2026-08-27, после пути переноса)

- [x] **Управление аудитом** — ✅ команда палитры «Журнал аудита агента»: просмотр последних записей, экспорт лога в файл, GDPR-очистка (`AuditLog.readRecent/exportTo/deleteAll` + `VibeAuditLogAction`).
- [x] **Управление предохранителями** — ✅ команда «Предохранители агента»: показать сработавшие и снять (`VibeBreakersAction`).
- [x] **Терминал-консоль: сворачивание + копирование** — ✅ клик по заголовку сворачивает вывод, кнопка «копировать» кладёт сырой вывод в буфер.
- [x] **Виджет статус-бара «Состояние агента»** — ✅ ⏳ ход / 🔎 проверка / ⛔ предохранитель (в покое скрыт), клик открывает чат (`VibeAgentStatusService` + `VibeAgentStatusWidget`).
- [x] **Уведомление о завершении хода** — ✅ балуон, когда окно IDE не в фокусе.
- [x] **Токены темы терминала** — ✅ `Vibe.Chat.terminal*` объявлены в `vibeNeonDark.theme.json` (правило: цвета только токенами).
- [x] **Ревью-фиксы (два прогона, 8+ находок)** — ✅ (2026-08-27): аудит-`files` теперь пишет цель edit-инструментов Claude (`file_path`/`notebook_path`; раньше всегда null — легаси-словарь VibeIDE snake_case); единый источник ключей путей `ToolCallAudit.PATH_KEYS` для аудита и harvest; harvest путей из ACP `locations`; команд-тул по ACP `kind==execute` (агент-агностично); regex секретов расширены (OpenAI `sk-proj-`/`sk-svcacct-`, GitHub `gh[pousr]_`); `ShellSafetyAnalyzer` спускается в `$(…)`/бэктики; `HookRunner.load()` синхронизирован (гонка кэша с 4 потоков); один общий `AuditLog` на проект (`VibeAuditService`) — просмотр/экспорт/очистка сериализованы с записью через один executor; `CheckpointService` не падает при сбое журнала; общий `ProcessSupport` (shell+drain) вместо дублей; предупреждение о непросканированных файлах сверх лимита; аудит чекпоинтов; именованные константы вместо магии.
- [x] **Волна 2 усовершенствований** — ✅ (2026-08-27): сворачиваемый блок «💭 размышления» (ACP `agent_thought_chunk` — раньше игнорировался); кнопка «копировать» на сообщениях агента; лимиты скана вынесены в настройки (макс. файлов/размер файла) + предупреждение о пропуске; таймаут рукопожатия агента настраивается (холодный npx > 60 с); удалены хардкод-константы (`HANDSHAKE_TIMEOUT_SEC`, `MAX_SCAN_FILE_BYTES`).
- [x] **Ревью-фиксы (3-й прогон, 19 находок)** — ✅ (2026-08-27): аудит-вьюер читает/чистит/экспортирует ОФФ-EDT (был фриз UI); один `ioLock` в `AuditLog` (гонка close/inline); `VibeBreakerService.getState/loadState` синхронизированы с defensive-copy (иначе предохранитель мог не сохраниться — security); `AcpClient.failPending` атомарный drain (late-put не осиротеет); `currentAgentMessage` читается только на EDT; буфер `TerminalConsole` ограничен (не только показ); `TurnChecks` защищённые пути якорятся (ловят относительные `.git/…`); harvest путей для всех мутирующих kind; `AgentTerminalService.disposeAll` будит waiter'ов + правка KDoc; хук-аудит `ok` по `flagged` (post-отказ ≠ ok); статус-бар в пайплайнах; терминал не безлимитный (дефолтный cap); именованные константы (`LlmClient`, `VibeFimProvider`); auditSpec — meta чекпоинта. 171 тест зелёный.
- [x] **Рендер код-блоков в ответе агента** — ✅ (2026-08-27): чистый парсер `MessageSegments` (fenced ``` + язык, незакрытый fence = остаток кодом) + `CodeBlockPanel` (моноширинный, метка языка, гориз. скролл, «копировать»); ре-рендер при завершении хода и при восстановлении из истории. Фикс попутно: снятие предохранителя сбрасывает ⛔ в статус-баре.
- [x] **Ревью-фиксы (4-й прогон, 4 находки + 3 попутных)** — ✅ (2026-08-27): `thoughtsBlock` сбрасывается на EDT (был reset с пул-потока — утечка размышлений в след. ход) и на КАЖДЫЙ ответ (свой блок на sub-turn bounce); `harvestMutation` больше не тащит `locations` execute-инструмента в changedPaths (read-only `grep .env` мог взвести предохранитель); `TerminalOutputBuffer` считает байты инкрементально (был O(n²) на append после дефолтного cap); статус-бар — список слушателей вместо одного слота (несколько окон одного проекта); снятие предохранителя сбрасывает ⛔; уточнение сообщения о пропуске скана; HiDPI-шрифт в аудит-вьюере. 180 тестов зелёные.
- [x] **Подсветка синтаксиса в код-блоках** — ✅ (2026-08-27): `CodeLangMapping` (тег fence → расширение, ~40 языков, чистый+тесты) + `CodeBlockPanel` рендерит read-only `EditorTextField` с реальной подсветкой по FileType; неизвестный язык / нет проекта / сбой создания редактора → откат на моноширинный `JTextArea`.
- [x] **Ревью-фиксы (5-й прогон, 11 находок)** — ✅ (2026-08-27): **AcpClient больше не умирает от битого кадра** (safe-cast + per-frame try/catch — раньше `session/update` без params-объекта убивал reader и всю ACP-сессию); `MessageSegments` по CommonMark (вложенный fence по длине backticks, язык = первый токен info-строки, CRLF, одинокий fence не рисует пустой бокс); `HookRunner`/`VerifyGateRunner` — kill дерева процессов на таймауте (демон-грандчайлд держал пайп), дренаж не роняет вердикт; `HookRunner` кап размера hooks.json; `AuditLog` латчится off после сбоя записи; `CodeBlockPanel` кап размера (гигантский блок не морозит ленту); `ShellSafetyAnalyzer` по basename (`/bin/rm` теперь ловится); при переключении на живой тред восстанавливаются размышления/терминалы; терминал/размышления убраны из recordRows (не смещают reveal-индекс). 189 тестов зелёные.
- [x] **Индикатор контекста «⛁ N%»** — ✅ (2026-08-28): ACP `usage_update` (занято/размер окна токенов, стоимость) → компактный чип в композере, тултип с точными числами, жёлтый с 80%, сброс при новой сессии агента; токены темы `Vibe.Composer.usage*`. Раньше кадр игнорировался.
- [x] **Ревью-фиксы (6-й прогон, 11 находок)** — ✅ (2026-08-28): Stop прерывает VERIFY-GATE (кансел-поллинг слайсами 1 с + ранний выход из evaluateGates — раньше Stop молча ждал сборку до 5 мин); stdin хука пишется с отдельного потока (payload > пайп-буфера парковал поток до недостижимого таймаута); `fs/read_text_file` — 1-базный `line` по ACP (был off-by-one); лента не дёргает скролл вниз, если читатель ушёл вверх (stick-to-bottom с зазором); конструктор панели без диск-IO на EDT (конфиги/сид грузятся пулом, публикуются на EDT); блок размышлений вставляется НАД ответом; `CheckpointService.git()` — drain+waitFor+kill-tree (60-с таймаут был недостижим); общие `ChatTheme` (токены терминала/кода один раз) и `copyLabel` (3 ручных копии → 1 фабрика); FIM/anthropic max_tokens — именованные константы; удалён мёртвый `hasSecurityFinding`. 189 тестов зелёные.
- [x] **Засев окружения `.vibe/`** — ✅ (2026-08-28, вариант А решением владельца: сеем всем безусловно — VibeIDEA агентская, без `.vibe` незачем): `VibeDefaults` (манифест 9 файлов в ресурсах: README, .gitignore, rules.md, 4 example.jsonc из наших спек, design/components+uiKit — мёртвые ресурсы ожили) + `VibeDefaultsSeeder` (ProjectActivity, фон). Create-if-missing, журнал `.seeded.json` (sha256) — задел под release-обновление сидов (механика VibeIDE vibeDefaults.ts). Точечный сид хуков из HookRunner удалён. 4 юнит-теста.
- [x] **Каталог провайдеров `.vibe/providers/*.jsonc`** — ✅ (2026-08-28, решение №24): 15 файлов в засеве (README, шаблон с примером кастомной модели, 7 активных: OpenCode Go/Zen, OpenRouter, MiniMax, Z.AI, Kimi, DeepSeek; 6 файлов выключенных: OpenAI, Anthropic, Alibaba Coding Plan, Meta Muse ×3 маршрута, Muse Glimmer local ×2, Ollama; example-образец удалён). `ProvidersService` читает 4 слоя (глобальный каталог → проектный каталог → глобальный providers.json → проектный providers.json — каталог как «встроенные» в слабейшем слое, пользовательские файлы не глушатся), `extends` поперёк файлов и от неактивных записей (один строгий резолв после полного слияния), активация переопределением по id, `models.fetch` — различимый тристейт (true поверх false работает). Вшитых моделей нет — `models.fetch` у активных (static только у Alibaba — каталог плана врёт — и выключенных маршрутов без каталога). Бейдж «кастом» в пикере и на странице «Модели»; страница «Модели» подтягивает fetch-каталоги; применение ключа в настройках перечитывает реестр открытой панели (топик ProvidersChangeListener); listModels шлёт headers/query/query-auth и anthropic-version; пустое поле ключа не стирает общий apiKeyRef (OpenCode Go/Zen). 10 catalog-тестов + сид-тест (204 зелёных). Адверсариальное ревью 26 находок закрыто до коммита.
- [x] **Страница «Модели»: аккордеон + фильтры + значки (UX-паритет VibeIDE §4–5)** — ✅ (2026-08-28, фидбек владельца по живой сборке): сворачиваемые группы провайдеров (свёрнуты по умолчанию, счётчики «(N)»/«(X/Y)»), AND-поиск по имени/id/провайдеру/характеристикам с авто-раскрытием, тумблер «активные» (дефолт вкл, чистый UI-фильтр), значки «кастом»/vision/text-only/fim/контекст («128K» бинарно, «1M» десятично); Apply «Моделей» публикует ProvidersChangeListener → пилюля «Модель ▾» сразу показывает только включённые (раньше — до переоткрытия панели). Чистая логика — ModelRows + 5 юнит-тестов (209 зелёных).
- [x] **Живое обновление каталогов + тонкие скроллы** — ✅ (2026-08-28, фидбек владельца): «Проверить и сохранить» (ключ из поля, сохранение на EDT со staleness-guard, обновление карточек-близнецов по общему apiKeyRef, честный статус для endpoint без авторизации, чтение PasswordSafe уведено с EDT); страница «Модели» перестраивается по топику, кнопки «Обновить каталоги»/«обновить» по провайдеру, поколения запросов против устаревших ответов, раскрытость групп и каталожные строки переживают перестройку, guard самопубликации на Apply. Тонкие скроллы (`VibeScroll` → платформенный `JBThinOverlappingScrollBar`) во всех панелях и попапах + гейт `vibe-plugins/tools/checkVibeUi.sh` и запись в FORK_CHANGES (зависимость от непубличного API). Адверсариальное ревью: 20 находок закрыто, 25 отклонено. 232 теста зелёные.
- [x] **Тонкие скроллы во всей IDE + настройка толщины** — ✅ (2026-08-28, решение владельца «4px для всех»): точечная правка платформы `JBScrollBar` (isThin по ключу `vibe.scrollbar.thickness`, тонкие UI получают толщину параметром) + страница настроек «Интерфейс» (спиннер 0–14, дефолт 4, применяется к живым окнам через `VibeScroll.refreshAllScrollBars`); ключ объявлен EP `registryKey` нашего плагина, `registry.properties` платформы не тронут. Запись в FORK_CHANGES + расширенный гейт `checkVibeUi.sh` (ловит потерю правки при синке).
- [x] **Curated-пикер: модели каталога скрыты по умолчанию** — ✅ (2026-08-28, фидбек владельца «в списке должны быть только активные»): семантика VibeIDE §7 — fetch-модель скрыта, пока не включена на «Моделях», кастомная видима, явный тумблер сильнее дефолта (ModelVisibility хранит только явные решения, строкой true/false — прежний setValue со сбросом-в-дефолт терял явное «показать»); панель после подтяжки каталога подсказывает, где включать; на «Моделях» подсказка при пустой группе под фильтром «активные». 210 тестов зелёных.
- [x] **Общие сиды: репо VibeBrains + submodule (решение №25)** — ✅ (2026-08-28): консолидация каталога под общий набор (дубли VibeIDE схлопнуты, знания их static — закомментированными кастомными записями в openai/deepseek/kimi.jsonc; README про обе роли: рабочий реестр VibeIDEA / примеры VibeIDE), репо VibeBrains (корень = образ `.vibe`), `resources/vibeDefaults` заменён submodule-ом (glob-exclude `.git`, запись в FORK_CHANGES про `.gitmodules`), гейт манифест↔ресурсы, правило «перед работой с сидами — submodule update --remote» в CLAUDE.md. 211 тестов зелёных. ✅ Опубликовано: github.com/VibeBrains/VibeBrains, submodule-URL переключён на GitHub; сессия VibeIDE перенесла свои сиды (d42b6cf), полный набор 62 файла подтянут (0f83381be0).
- [x] **Авточистка устаревших сидов (deprecated.json, решение №27)** — ✅ (2026-08-28): sha256-эталоны исторических версий в VibeBrains; `VibeDefaults.cleanupDeprecated` (удаление только байт-совпавших копий, журнал чистится, правленные репортятся; LF-нормализация против CRLF-чекаутов) + та же механика в VibeIDE (генератор эмитит VIBE_DEPRECATED_MANIFEST, чистка + `.defaults.lock.json`); разовая ручная чистка 5 проектов (15 файлов, все сверены с git-историей). 216 тестов зелёных у нас + 5 их тестов.
- [x] **Release-обновление сидов** — ✅ (2026-08-28, решение №28): реестр ревизий `versions.json` + `bump.mjs` в VibeBrains (история sha, `--check`-гейт, инициализация из git трёх репо); `SeedRevisions.verdict` — шесть вердиктов (CREATE/SAME/UPDATE/CONFLICT/USER_EDIT/SET_DRIFT); нетронутые обновляются молча, конфликт идёт балуном с «Сравнить» (DiffManager) / «Обновить, сохранив мои тумблеры» (SeedToggleMerge → `providers/zz-local-toggles.jsonc`) / «Оставить своё» (`reconciled` в журнале). Журнал `.seeded.json` v2 с обратной совместимостью. 232 теста зелёных.
- [x] **Кэш каталогов моделей + параллельный опрос** — ✅ (2026-08-29, фидбек владельца «при первом открытии список только из статики, ждём ~30 с»): `ModelCatalogCache` (JSON в системном каталоге IDE; запись на провайдера = отпечаток endpoint + id моделей + время; merge и возраст — чистые функции) отдаётся мгновенно на старте панели и на странице «Модели», сеть обновляет фоном. Опрос провайдеров стал параллельным, ответы публикуются поштучно (список наполняется по мере прихода, а не пакетом в конце), каталогу дан свой клиент с коротким таймаутом (10 с вместо 20 с connect + 30 с response — чат не тронут). Кэш пишет только успешный ответ (401/timeout не стирают вчерашний каталог), смена `baseURL`/`models.fetch.url` его обесценивает. В логе — возраст кэша. 239 тестов зелёных (+7).
- [x] **Реклама платных IDE JetBrains выключена по умолчанию** — ✅ (2026-08-29, нашёл владелец на живой сборке): баннер «*.tsx files are supported by WebStorm» (`PluginAdvertiserEditorNotificationProvider` платформы) гасится штатным флагом `ide.try.ultimate.disabled` — правок платформы нет. `VibePromoSettings` + `VibePromoDefaults` (postStartupActivity) применяют наш дефолт один раз и не перетирают явное решение пользователя; тумблер на странице «Интерфейс» с обновлением открытых редакторов без рестарта. 3 теста (242 зелёных).
- [x] **Каталоги: провайдеры без ключа не опрашиваются и не засоряют лог** — ✅ (2026-08-29, разбор 401 на живой сборке): диагноз — ключи есть только у zai/minimax, остальные 401 были честным ответом на запрос без ключа (панель, в отличие от страницы «Модели», проверку пропускала). Теперь: без ключа и не localhost — в сеть не идём; `CatalogReport` (чистая, 6 тестов) сводит раунд в одну строку с различением «без ключа / ключ отклонён / локальный не запущен / не ответил»; `ConnectException` без сообщения больше не печатается как «(null)»; провайдеры без ключа убраны из пилюли «Модель ▾» и возвращаются, как только ключ появился. 248 тестов зелёных.
- [x] **Звуковые уведомления** — ✅ (2026-08-29) `SoundPolicy` (чистая: глобальный тумблер, три события, молчание при активном окне, дебаунс 1,5 с — 6 тестов) + `VibeSoundService`: встроенный сигнал СИНТЕЗИРУЕТСЯ в PCM-буфер (ноль ассетов, ноль зависимостей, ноль вопросов к упаковке и лицензиям), свой файл валидируется попыткой открыть, громкость по логарифмической шкале. События подключены к завершению хода, остановке гейтом и запросу подтверждения. Настройки на странице «Чат», строки сразу через каталог.
- [x] **Markdown в пузырях** — ✅ (2026-08-29, ночная работа) `MarkdownInline` (чистый, 8 тестов): жирный/курсив/инлайн-код/ссылки/заголовки/списки. Экранирование ПЕРВЫМ (ответ модели недоверен), код-спаны вынимаются до применения эмфазы (иначе `**text**` внутри кода становился жирным — поймано тестом), ссылки только http(s)/file/mailto, заголовок — жирная строка, а не `<h1>`. Обычный и очень длинный текст остаются простыми: HTML-вид мигал бы на стриминге.
- [x] **Свёртка длинных код-блоков** — ✅ (2026-08-29, ночная работа) порог в настройках «Чат» (24 строки, 0 — выключено), свёрнутый блок показывает начало и число скрытых строк; копирование всегда отдаёт блок целиком.
- [x] **Инкрементальный просмотр аудита** — ✅ (2026-08-29, ночная работа) тулвиндоу «Аудит»: `AuditTail` (чистый, 9 тестов) — дочитывание с позиции, распознавание ротации по идентификатору файла (не по размеру: подмена той же длины иначе незаметна), перенос недописанной строки на следующий тик, кап вида. Фильтр неудач, «следить».

## Фаза 4 — Релизная дисциплина

- [x] **Двухфазный поток** — ✅ (2026-08-31, next) `releaseStamp.sh` штампует проверенное (версия, коммит, sha256, гейт дистрибутива), `releasePublish.sh` публикует только совпадающее и отказывает на расхождении; есть `--dry-run`. Свой README уже заменил апстримный.
- [x] **Слияние ченджлога при отложенной публикации** — ✅ (01.09.2026, next) `releaseChangelog.sh`: базлайн берётся у GitHub (`gh release list`), а не у git, поэтому отложенный выпуск не теряет своё содержимое; неопубликованные версионные теги в диапазоне выводятся отдельным предупреждением. Коммиты синка отфильтрованы по нашим путям — иначе первый же синк на 6016 файлов сделал бы ченджлог нечитаемым. Проверено фактом: на реальной истории базлайн `v0.1.0`, подложенный тег `v0.1.9-proba` всплыл в предупреждении, точка восстановления `sync-base-before` — нет (фильтр `v[0-9]*`).
- [x] **CI форка** — ✅ (01.09.2026, next) Свои воркфлоу рядом с апстримовскими, не вместо них: `vibeGates.yml` гоняет четыре гейта исходников на push и PR в `main`/`next`, `vibeAudit.yml` раз в неделю — отставание от апстрима и аудит поставляемого. Сборка dmg в CI не обещана: она требует ~100 ГБ диска и часа, и зелёная галочка, которой не было бы за что зацепиться, хуже её отсутствия.
  - `checkUpstreamLag.sh` — база из `FORK_CHANGES.md` против `git ls-remote upstream master`, порог в днях (по умолчанию 30); прогнан: база `320ed57059`, отставание 0 дн.
  - `checkServerDeps.sh` — `npm audit --omit=dev` по нашему дереву серверов плюс сверка версий в лицензионном отчёте с закреплёнными; проверен подложенным `^0.3.0` — упал на обоих проверках.
  - Полный OWASP Dependency-Check по форку не делаем осознанно: это чужая поверхность в тысячи библиотек, которой занимается JetBrains, и отчёт по ней стал бы шумом. Проверяем свою поверхность — то, что мы сами кладём в дистрибутив.
  - Гейт доков `checkVibeDocs.sh` (31.08.2026) теперь под CI, а не только локально.
- [ ] Погасить апстримовские воркфлоу на нашем origin (не файлами — правило границы форка, а `gh workflow disable`). Факт 01.09.2026: `Qodana` уже отработал по расписанию и упал, `Dependency Graph` шлёт по десятку прогонов на пуш с половиной падений, `IntelliJ IDEA` и `PyCharm` собирают по расписанию чужую IDEA CE целиком. Нужно решение владельца — это настройка репозитория, а не коммит.

## Фаза 5 — Синк с апстримом

- [x] **Первый синк с апстримом** — ✅ (2026-08-31) `38a8934174` → `320ed57059`, 6016 файлов, линия 263. По релизному тегу НЕ синкались осознанно: новейший `idea/2026.2.1` — это ветка 262, то есть откат на предыдущую линию. «Пост-merge чистка» наполнена шестью пунктами по факту, включая отдельную синхронизацию клона `android/`.

- [x] **Горячие клавиши закреплённых команд** — ✅ (2026-08-30, next) `Ctrl+Shift+Alt+1..9` запускают команды с `"pinned": true` по позиции в файле; пустой слот говорит, что он пуст.
- [x] **QR-код адреса превью** — ✅ (2026-08-30, next) адрес для телефона показывается QR-кодом (ZXing платформы), чёрным по белому независимо от темы; буфер обмена остался кнопкой.
- [x] **Дизайн-детекторы: формы, движение, цвет — 81 правило** — ✅ (2026-08-31, next) три новые области (10 + 9 + 9 правил) и рамка фокуса на низком контрасте; каталог доведён до паритета с VibeIDE (81).
- [x] **Каталог особенностей моделей** — ✅ (2026-08-31, next) `ModelQuirks`: снятие сэмплирования, переименование лимита ответа, складывание системной роли и отказ от стрима — по идентификатору модели, до `extraBody`.
- [x] **Карта документации** — ✅ (2026-08-31, next) панель «Документы» умеет рисовать граф: слои по кратчайшему расстоянию от входа, сироты отдельной полосой, битые ссылки рамкой, клик открывает файл; раскладка чистая и покрыта тестами.
- [x] **Территория прогона занимается сама** — ✅ (2026-08-31, next) `TerritoryGuess` читает угол проекта из слов задачи, ledger хранит его, пересечение с чужим идущим прогоном называется строкой в ленте.
- [x] **Автопилот с контрольными точками** — ✅ (2026-08-31, next) `AutopilotPolicy`: ведёт план сам, спрашивает каждые N ходов, предел ходов подряд, любая необычность возвращает управление; по умолчанию выключен.

### Версия продукта на виду (02.09.2026)

- [x] **Наша версия — главная** — ✅ (02.09.2026, next) `VibeIdeaApplicationInfo.xml` объявляет версию **продукта** (0.3.0), а не платформы (было 2026.3, унаследованное от линии IC). Номер платформы остался в номере сборки `VI-263.*` — по нему считается совместимость плагинов, и трогать его нельзя. Диагностика печатает обе отдельной строкой: «VibeIDEA 0.3.0 · платформа VI-263.SNAPSHOT», потому что путают их постоянно, а в issue нужна первая.
- [x] **Гейт версии при выпуске** — ✅ (02.09.2026, next) `releaseStamp.sh` сверяет версию в «О программе» с выпускаемым тегом и отказывается штамповать при расхождении: это два разных файла, разойтись им ничего не мешает, а расхождение обнаруживает пользователь, пишущий в issue не ту версию, что стоит. Проверено: `v0.4.0` на текущем XML отвергается с объяснением, `v0.3.0` проходит.

### Релиз 0.3.0 (02.09.2026)

- [x] **Выпущен v0.3.0** — ✅ (02.09.2026) 11 коммитов сверх v0.2.0, dmg 826 955 908 байт, sha256 `6786e6b9dd…`, гейт дистрибутива зелёный, 1182 теста. Заметки — [references/releaseNotes-v0.3.0.md](references/releaseNotes-v0.3.0.md).
  - Грабли выпуска: тег сперва встал на коммит документации, сделанный ПОСЛЕ штампа, и `releasePublish.sh` отказался публиковать — ровно то расхождение, ради которого штамп и существует. Правильный выход не «переставить штамп», а публиковать из проверенного коммита: тег переставлен на него, публикация сделана из detached HEAD на нём же.
  - Фаза 2 упирается в лимит времени одной команды (826 МБ), поэтому создание релиза и заливка ассета снова делались раздельно.
- [x] **Сборка 0.2.1 «пощупать»** — ✅ (02.09.2026) не публиковалась: dmg 826 919 559 байт, sha256 `ed0aaaaac8…`, гейт зелёный.
- [x] **Windows-артефакты v0.3.0** — ✅ (02.09.2026, main) первая сборка форка под Windows: инсталлятор `.exe` (NSIS) и `.win.zip`, гейт дистрибутива зелёный на Windows 11 — размеры и sha256 в [заметках релиза](references/releaseNotes-v0.3.0.md). По дороге закрыто то, без чего Windows-сборка была бы чужой или невозможной ([разбор](knowledge/build/windowsBuild.md)):
  - **брендинг Windows** — `icoPath`/`installerImagesPath` в `VibeIdeaProperties.createWindowsCustomizer` (без них лаунчер, панель задач и NSIS-инсталлятор шли со стоковыми картинками IntelliJ — те же грабли, что Dock на macOS), опрос JetBrains об удалении отключён; `vibeidea.ico`, `install.ico`, `uninstall.ico`, `headerlogo.bmp`, `logo.bmp` генерирует `vibe-plugins/tools/makeWinImages.py` из того же `vibeidea.icns`;
  - **гейты работают в Git Bash** — резолвер `pythonBin.sh` (`python3` на Windows — заглушка магазина Microsoft) с обёрткой `pythonLf.sh`, срезающей `\r`, который Windows-python пишет в конвейер (сухой прогон публикации отверг совпадающий sha256), нормализация разделителей путей в гейтах документации и локализации (иначе 21 «запись без индекса» и 91 «интерфейсный литерал» на пустом месте), `strings` заменён на `grep -a`;
  - **гейт дистрибутива и релизные скрипты — по ОС**: `checkVibeDist.sh` открывает `.win.zip` наравне с dmg, `releaseStamp.sh` штампует набор файлов текущей ОС, `releasePublish.sh` доливает артефакты в существующий релиз и приводит его текст к файлу заметок;
  - **правило «упаковочный потомок тега»** (`releasePackagingOnly.sh`): вторая ОС может собираться на потомке тега, если разница с тегом лежит только в `build/`, `vibeidea-customization/`, `vibe-plugins/tools/`, `docs/` и корневых `*.md`; штамп записывает `packagingOnly`, публикация перепроверяет и принимает это только для уже вышедшего релиза. Переставлять опубликованный тег — нельзя, собирать со стоковыми картинками — тоже; это единственный проверяемый выход между ними.
  - Не трогали: лаунчер зовётся `idea64.exe` (`baseFileName` унаследован от IDEA CE) — переименование задевает конфиг-каталоги и обе другие ОС, отдельное решение.
  - Найдено и названо в заметках: **Phpactor на Windows не запускается** — phar требует `ext-posix`, которого в Windows-сборках PHP нет; гейт отличает это от сломанного phar по тексту проверки требований Box. TypeScript/CSS/ESLint и оба адаптера отладки работают.
- [ ] **PHP-сервер на Windows** — Phpactor требует `posix`: либо сервер без этого требования, либо запуск Phpactor через WSL из коробки (доктор уже умеет свой путь к серверу). Решение владельца.

### К релизу 0.3.0 (02.09.2026)

- [x] **Экспорт расхода в CSV** — ✅ (02.09.2026, next) месяц таблицей: время, чат, роль, цель, токены, стоимость, валюта, доли файлов. Три решения не про форматирование: числа всегда с точкой (десятичная запятая в файле с запятыми-разделителями — сломанный файл), BOM в начале (Excel читает UTF-8 без него как кракозябры, а отчёт с битыми русскими путями не используют), и обезвреживание ячеек, начинающихся с `=`/`+`/`-`/`@` — это единственное место, где наши данные попадают в программу, которая исполняет прочитанное. 7 тестов; своё же ожидание в тесте пришлось исправить по факту: метка времени указывала на 2025 год, а не на 2026.

- [x] **Панель «Фоновые задачи»** — ✅ (02.09.2026, next) тулвиндоу со списком, кнопкой «остановить» у каждой работающей и «остановить все». Реестр переехал в сервис проекта: два реестра означали бы задачу, видимую в чате и невидимую в панели, — хуже отсутствия списка, потому что «задач нет» человек прочитает и поверит. Список обновляется и по событию (старты и остановки), и по таймеру (секунды на задаче, которую никто не трогал).
- [x] **Расход в статусной строке** — ✅ (02.09.2026, next) сумма за сутки, если цена известна, иначе токены; пусто при нулевом расходе, чтобы обычная правка кода не видела виджета. Подсказка — остаток по окнам, щелчок — полный отчёт. Опрос раз в пять секунд вместо подписки: реестр пишут четыре разных потока, и слушатель на каждом — четыре места, где о нём забудут.
- [x] **Сроки фоновых задач в настройках** — ✅ (02.09.2026, next) срок жизни и период отчёта переехали из констант в Settings → Tools → VibeIDEA → Агент: вшитые полчаса верны для проверки и неверны для ночной сборки, а правило проекта требует конфиг для всего пользовательски-значимого.

### Ревизия и развитие 01.09.2026 (перечитка своей же работы)

- [x] **Три дефекта, найденные перечиткой** — ✅ (01.09.2026, next):
  - стоимость из ACP приходит нарастающим итогом и записывалась целиком на каждом обновлении — реестр складывал running total столько раз, сколько агент отчитался о прогрессе; пока деньги были колонкой в отчёте, это никого не трогало, а вчерашний потолок расхода отказывал бы в работе на первом ходу. Приведено к приросту и вынесено в чистый `CumulativeMeter` (5 тестов);
  - причуды моделей жили в одной глобальной переменной: два открытых проекта — и загрузившийся последним решал за оба, тихо отбирая у другого `temperature`. Теперь `ModelQuirksRegistry` по проекту, клиент знает свой;
  - причуды применялись только на openai-пути, а MiniMax и Qwen живут на Anthropic-совместимом — том самом, где эти поля и игнорируются. Плюс имя поля на разных протоколах разное (`stop` против `stop_sequences`), а `max_tokens` у Anthropic собственный и обязательный: переименовать его значило бы отправить запрос без лимита ответа (3 теста).
- [x] **Цепочка целостности журнала** — ✅ (01.09.2026, next) `AuditChain`: каждая запись несёт отпечаток предыдущей, правка/удаление/вставка/перестановка ломают связь, проверка называет номер строки. Не подпись и не притворяется ею: ключ жил бы на той же машине, что и файл. Ротация цепочку не ломает — у архива своя, живой файл начинает новую, иначе каждая ротация выглядела бы как подделка. Записи старого формата — «старые», а не «подделка»: объявив честную старую запись подделкой, мы научили бы игнорировать проверку на первом прогоне. 10 тестов.
- [x] **«Кто что делал»** — ✅ (01.09.2026, next) `AuditSummary` + кнопка в окне журнала: сколько записей и неудач за каждым автором (роли не сливаются в одного «агента» — иначе прячется весь смысл пайплайна), какие действия, и тут же проверка цепочки. Атрибуция, которую нельзя прочитать, — это отсутствующая атрибуция. Битая строка не роняет отчёт. 6 тестов.
- [x] **Дефект в собственной новой фиче, пойманный до пуша** — ✅ (01.09.2026, next) ротация обнуляла цепочку **после** того, как звено уже посчитано: первая строка нового файла не сходилась со своим же генезисом, и каждая ротация читалась бы как подделка — проверка, которая кричит «волки», это проверка, которую выключают. Порядок исправлен (сперва ротация, потом звено), регрессионный тест на 200-байтовом пороге. Заодно `close()` перестал молча ронять очередь: последние записи перед выходом — ровно те, которые нужны разбору, а с цепочкой обрезанный хвост неотличим от удалённого.
- [x] **Строка «Расход и потолки» в диагностике** — ✅ (01.09.2026, next) все окна с остатком: потолок, до которого не видно расстояния, встречают только лбом.

### Третья ревизия 01.09.2026

- [x] **Модальный вопрос из write-action и сеть на EDT** — ✅ (01.09.2026, next) вчерашний вопрос о потолке звал `invokeAndWait` **с самого EDT**: по исходникам платформы (`ApplicationImpl.doInvokeAndWait`) в этом случае блок выполняется внутри intended write action, а показывать модальное окно из write-action платформа запрещает. Плюс отправка вопроса в Telegram читает токен из keychain и делает HTTP-запрос — тоже с EDT. Введён `askOnEdt`: с фонового потока это `invokeAndWait`, с EDT — прямой вызов; отправка на телефон ушла в фоновый поток, ответ телефона диалог всё равно замечает опросом. Тем же дефектом болел `confirmClearBreakers` (вызывается из `startTurn`, то есть всегда с EDT) — вылечен тем же помощником.

- [x] **Внешняя проверка журнала** — ✅ (01.09.2026, next) `verifyAuditChain.sh`: тридцать строк, без IDE, коды возврата 0/1/2. Прогнан на пяти случаях (целый, подделанный, старый без звеньев, пустой, смешанный) — и прогон поймал две мои ошибки: на журнале без звеньев скрипт утверждал «не правили» (проверять было нечего), а на смешанном считал, что звеньев нет вовсе. Обе исправлены.
- [x] **Прогноз выгорания окна** — ✅ (01.09.2026, next) `timeToLimitMs`: доля говорит, куда ушли деньги, темп — когда они кончатся, и это разные решения. Темп за последний час, потому что окно, усреднённое по себе самому, прячет всплеск; при нулевом темпе прогноза нет вовсе — «никогда» читалось бы как успокоение. 4 теста.

### Вторая ревизия 01.09.2026

- [x] **Диск в потоке интерфейса** — ✅ (01.09.2026, next) проверка потолка расхода вызывалась в `onSend`, то есть на EDT, а `entries()` читает файл и сбрасывает его на диск: фриз на медленном томе. Добавлен `cachedEntries` (только память) и `prime()` в фоновой загрузке панели; «ещё не прочитано» трактуется как «не знаю» и **не** блокирует ход — потолок, отказывающий из-за незагруженности, отказывает по причине, не имеющей отношения к деньгам.
- [x] **Хардкод и легаси убраны** — ✅ (01.09.2026, next) `LINK_OVERHEAD_BYTES` выводится из имени поля и длины звена (скопированное число разошлось бы в день, когда изменится любое из двух); двойная проверка `ok` в `AuditSummary` схлопнута; `ModelQuirksRegistry.forget` перестал быть мёртвым кодом — закрытый проект больше не решает, как зовут модели; строка расхода вынесена в `SpendLines` и не существует двумя копиями.
- [x] **Исчерпанный потолок спрашивает, а не отказывает молча** — ✅ (01.09.2026, next) вопрос в IDE и в Telegram (отвечает первый), «Всё равно продолжить» проводит один ход и пишется в журнал с окном и решением. Автопилот в этом месте останавливается без вопроса: у клавиатуры никого, а вопрос, на который некому ответить, — ход, висящий до утра.
- [x] **`/spend` в чате** — ✅ (01.09.2026, next) расход по ролям за сутки и остаток по каждому окну прямо в разговоре: деньги спрашивают посреди работы, а ответ, ради которого надо уйти из чата, смотрят один раз.
- [x] **Экспорт журнала предупреждает о разрыве цепочки** — ✅ (01.09.2026, next) журнал выгружают, чтобы кому-то показать; узнать про разрыв от получателя — худший из возможных порядков.
- [~] **Проверка формата звена** — ✅ тест на длину звена и устойчивость к байтам со старшим битом. Подозрение на знаковое форматирование **не подтвердилось**: проверил запуском — Java форматирует `Byte` как беззнаковый. Падал мой же тест (сообщение об ошибке вычислялось всегда, даже при успехе); маска оставлена явной, комментарий исправлен на правдивый.

### Из разбора дайджеста 02.09.2026

Источники, проверка по коду и обоснования отказов — [references/digest20260902.md](references/digest20260902.md).
Порядок — по польза/трудоёмкость.

- [x] **Чтение `usage` из ответа** — ✅ (02.09.2026, next) `TokenUsage`: Anthropic отдаёт ввод и кэш в `message_start`, вывод в `message_delta`; у OpenAI всё в финальном чанке и **только если попросить** `stream_options.include_usage` (без этого поля прямой чат бился догадками). Кэшированные токены на openai-проводе входят в `prompt_tokens` — вычитаются, иначе оплачивались бы дважды. Слияние берёт максимум по полю, а не сумму: провод, повторяющий нарастающий итог, иначе удвоил бы его. 4 теста. Прежний пункт: — в прямом чате мы не читаем его вовсе: токены оцениваются по длине **ответа**, стоимость не считается. Отсюда три следствия, которых до сих пор никто не замечал: ввод не учитывается вообще (запрос на 200k токенов стоит в отчёте столько же, сколько длина ответа), потолок расхода на прямом чате не срабатывает никогда, а кэш-попадания невидимы.
- [x] **Цена модели в `providers.json`** — ✅ (02.09.2026, next) `ModelPricing` (`input`/`output`/`cacheRead`/`cacheWrite`/`currency` за миллион токенов), необязательный и наследуемый слоями. Ставка кэша отдельная, а не коэффициент скидки: у Fable 5.1 это 0,025 базового ввода против 0,1 у прочих моделей Claude, и один коэффициент пришлось бы переписывать при каждом таком изменении. Незаданная цена отвечает `null`, а не нулём — «бесплатно» и «неизвестно» разные ответы. В ленту выводится строка экономии на кэше. 5 тестов, раздел в спеке. Прежний пункт: — необязательные поля, которые заполняет владелец ключа. Своей таблицы цен не ведём (устареет в день смены тарифа), но объявленную человеком цену обязаны применять; иначе расход в деньгах есть только там, где провайдер прислал его сам.
- [x] **История append-only** — ✅ (02.09.2026, next) `WirePrefix`: измеряется, сколько ведущих сообщений запроса совпало с прошлым ходом — кэш-попадание это не «разговор тот же», а «запрос начинается теми же байтами». Найден и закрыт настоящий виновник: граница, за которой сообщения теряют картинки, пересчитывалась **каждый ход**, поэтому пятый скриншот переписывал сообщение с первым и рвал кэш на ровном месте; теперь она липкая — только вперёд и только при новой картинке. Разрыв префикса объявляется строкой в ленте. Маркер подрезки в провод не уходит вовсе (`Role.OTHER` отфильтрован) — проверено, подозрение снято. 7 тестов. Прежний пункт: — `appendCapped` вырезает середину и вставляет маркер, `branch` переписывает хвост: и то и другое меняет префикс запроса, то есть рвёт промпт-кэш на каждой подрезке. Подрезка «с головы» кэш сохраняет; на Fable 5.1 кэш-чтение стоит 0,025 базового входа против 0,1 у остальных моделей Claude, так что цена ошибки выросла вчетверо.
- [x] **Одобрение скилла, привязанное к содержимому** — ✅ (02.09.2026, next) `SkillApproval`: digest тела скилла **плюс имена файлов рядом** (скилл, получивший скрипт, получил возможность, даже если текст не менялся); изменился digest — спрашиваем заново, показывая, что скилл делает **теперь**. Одобрение по проекту: скилл с тем же именем в другом репозитории — другой скилл. Порядок листинга файловой системы изменением не считается, иначе вопрос задавался бы впустую, а вопрос впустую прокликивают. 4 теста. Прежний пункт: — единственное, что применимо из SEP-2640 без MCP: скиллы лежат в репозитории и меняются вместе с ним, значит одобренный вчера скилл сегодня может делать другое. Digest файла и переспрос при изменении — та же защита, что у сидов через манифест.
- [x] **Предупреждение о переписывании файла целиком** — ✅ (02.09.2026, next) `WholeFileRewrite`: строки считаются **по содержимому, а не по позиции** — иначе вставка одной строки сверху читалась бы как переписывание всего файла. Порог 20 % и минимум 30 строк: предупреждение, срабатывающее на каждом настоящем рефакторинге, учатся пролистывать. Не блокируем — переписывание, дающее верный файл, не дефект, но цена, которой не видно, это цена, о которой нельзя решить. 5 тестов. Прежний пункт: — подтверждённая регрессия 5.1 («more likely to rewrite the entire file than make a targeted edit»); дифф мы и так считаем для превью, данные под рукой.
- [x] **Счётчик вызовов инструментов на тёрн** — ✅ (02.09.2026, next) `TurnTrace.toolBatches`/`averageBatch` и строка в `/trace`. Детектор петель эту регрессию не видит по построению: вызовы **разные**, ничего не повторяется — выдаёт её только форма трассы, прогон одиночных вызовов там, где были пачки. Число сообщается, а не судится: что считать «мало», зависит от задачи. 4 теста. Прежний пункт: — 5.1 может слать по одному вызову вместо батча; это не петля (вызовы разные), и иначе регрессию видно только по счёту в конце месяца.
- [x] **Стоимость треда** — ✅ (02.09.2026, next) запись расхода помнит чат, `/spend` первой строкой отвечает «во что обошёлся этот». Записи старого формата чата не помнят и в ответ на вопрос о треде дают null, а не ноль: «этот чат был бесплатным» — утверждение, которого мы сделать не можем. 3 теста.
- [x] **Фоновые задачи: список и остановка** — ✅ (02.09.2026, next) `TaskRegistry`: `/bg` показывает, `/bg stop <id>` останавливает, `/bg stop` — все. Номер выдаётся до старта работы. Остановка снимает «стопор» вместе с задачей: держать его дальше значило бы дать поздней команде убить процесс, который ОС уже отдала кому-то другому. Найдено в собственном коде до коммита: в остановке всех задач `count` считался выражением с `|| true` (всегда истинно) и порядок был обратный — `finish` снимает стопор, так что процессы остались бы жить, а список бодро отчитался бы об остановке. 6 тестов.
- [x] **`ttlMs`/`pollIntervalMs` для фоновых задач** — ✅ (02.09.2026, next) `TaskLimits`: срок жизни объявляется до старта и называется человеку, отчёт о ходе идёт раз в минуту от **последнего слова**, а не от старта (иначе вместо пульса получается залп просроченных уведомлений). Найдено по дороге: `/bg` молча одалживал таймаут, написанный для **замеров** (15 мин), и на настоящей длинной сборке убил бы работу сообщением про замер — теперь у фоновой команды свой цикл ожидания. Ноль означает «без ограничения» и это явный выбор. 5 тестов. Прежний пункт: — идея из MCP Tasks, применимая без MCP: у наших `/bg` нет ни срока жизни результата, ни объявленного интервала опроса.
- ⬜ **Не берём**: причуду на `tool_choice` (мы его не посылаем нигде — ни в прямом чате, ни через ACP), инструкцию про батчинг в тело запроса (тело для ACP-агента строим не мы), MCP Tasks как протокол и Enterprise-Managed Authorization — MCP в форке нет, а у второго вдобавок **нет спецификации в источнике**: обзорная статья говорит о возможности как о существующей, не ссылаясь ни на SEP, ни на дату. Остальное из SEP-2640 — протокол, которого у нас нет; сам SEP — открытый черновик, и предусловия, побочные эффекты, retry и эскалацию он **явно оставляет за рамками**, вопреки формулировке дайджеста.

### Из разбора дайджеста 01.09.2026

Источники, проверка по коду и обоснования отказов — [references/digest20260901.md](references/digest20260901.md).
Порядок — по польза/трудоёмкость.

- [x] **`actor` в журнале аудита** — ✅ (01.09.2026, next) поле `actor` в `AuditEvent` **без значения по умолчанию**: умолчание однажды выбралось бы здесь и молча наследовалось каждым будущим вызовом — тем самым способом, каким журнал и потерял ответ на «по чьей воле». Три вида: `human`, `agent` (с ролью и именем цели), `ide` — гейты, хуки и предохранители не агент и не человек, и называть их агентом значило бы записывать наши решения на его счёт. Автопилот подставляет себя: продолженный им ход в журнале отличим от набранного руками. Прежний пункт: — человек / агент / роль в `AuditEvent`: сейчас запись файла человеком и та же запись агентом в автопилоте в журнале неразличимы (NIST: раздельная атрибуция как условие неотказуемости).
- [x] **Журнал недоступен инструментам агента** — ✅ (01.09.2026, next) `.vibe/audit.jsonl`, `.vibe/checkpoints.jsonl` и ротированные архивы — `READ_ONLY` в `AccessPolicy` раньше всех прочих правил и поверх любых настроек; READ_ONLY, а не DENIED: «что ты уже делал» — честный вопрос, опасна перезапись. Проверено снятием правила: три теста падают. Прежний пункт: — `.vibe/audit.jsonl` и `checkpoints.jsonl` в жёсткий deny-list `AccessPolicy`: сейчас они внутри проекта, то есть `READ_WRITE`, и защищены только диалогом подтверждения — процедурно, а не конструктивно.
- [x] **`protocol` на уровне модели** — ✅ (01.09.2026, next) `ModelEntry.protocol` перекрывает провайдерский, молчание наследует, незнакомое имя откатывается к `openai` (файл для более новой IDE не должен выключать реестр). Сид `opencode-go.jsonc` объявил MiniMax и Qwen как Anthropic-совместимые; спека дополнена разделом «когда у одного провайдера несколько форматов API». 4 теста, включая наложение слоёв: слой, не сказавший о протоколе, его не стирает. Прежний пункт: — у OpenCode Go один ключ и три формата API, привязанных к моделям (`/v1/messages` у MiniMax и Qwen, `/v1/chat/completions` у GLM/Kimi/DeepSeek, `/v1/responses` у Grok/GPT). Наш `protocol` — поле провайдера, поэтому засеянный `opencode-go.jsonc` с `models.fetch` зовёт половину моделей не тем протоколом.
- [x] **Гранулярные причуды выборки** — ✅ (01.09.2026, next) `NO_TEMPERATURE`/`NO_TOP_P`/`NO_TOP_K` рядом с `NO_SAMPLING`; агрегат оставлен, а не заменён — он честно описывает рассуждающие семейства и уже написан в чужих `.vibe/modelQuirks.json`. Запись про MiniMax взята из **его собственной документации** (`top_k` и `stop_sequences` игнорируются), а не из чужого симптома: причуда, выведенная из симптома без списка событий, переписывала бы запрос вслепую — тест это фиксирует. Прежний пункт: — `NO_SAMPLING` снимает `temperature`, `top_p` и `top_k` разом, а MiniMax документированно игнорирует только `top_k` и `stop_sequences`. Разделить на `NO_TEMPERATURE`/`NO_TOP_P`/`NO_TOP_K` (старое имя — синоним-агрегат), добавить запись про MiniMax.
- [x] **Долларовые потолки в окнах** — ✅ (01.09.2026, next) `SpendCeiling`: три катящихся окна (5 ч / неделя / 30 дней), проверка ДО хода, предупреждение на 80 % — на самом краю предупреждать поздно, следующий ход уже отказной. Считается только то, что провайдер сам назвал ценой: своя таблица цен устаревает в день, когда вендор меняет одну строку, а выдуманный потолок останавливает работу по выдуманной причине. Все три окна в отчёте, самое полное первым: остановленному коротким окном нужно знать, что месяц почти кончился. Настройки в целых долларах, 0 — выключено. 6 тестов. Прежний пункт: — лимиты подписок задаются в деньгах (OpenCode Go: $12 за 5 ч, $30 в неделю, $60 в месяц), а наши потолки только токенные. `SpendLedger` уже хранит `costAmount`/`costCurrency`.
- [x] **Строка про Claude Agent SDK в документации** — ✅ (01.09.2026, next) раздел «Чей счёт оплачивает работу» в [acpAgentsSpec.md](manuals/acpAgentsSpec.md): агент — программа пользователя на его учётке, мы не проксируем и не участвуем в биллинге; usage сторонних приложений идёт с лимитов подписки, а разделение **приостановлено 15.06.2026** — пауза, не отмена. Прежний пункт: — usage сторонних приложений идёт с лимитов подписки, изменения приостановлены 15.06.2026 (то есть состояние временное). Сам путь у нас уже есть — ACP-агент на подписке пользователя.
- ⬜ **Не берём** (обоснования в разборе): причуды MiniMax по SSE — поимённого списка событий нет ни в одном источнике; OAuth-делегирование и короткоживущие креды — нет второй стороны, агент локальный и на ключах владельца; запрет межагентных каналов — каналов в продукте нет; `/v1/responses` — третий транспорт до того, как проблема стала выразимой; Agent SDK отдельным адаптером — уже покрыт ACP.

### Из разбора дайджеста 31.08.2026

Источники и обоснования — [references/digest20260831.md](references/digest20260831.md).

- [x] **Фолбэк при отзыве доступа к модели** — ✅ (2026-08-31, next) новый класс отказа `UNAVAILABLE` (402/403/404): не ждём, не повторяем, уходим к запасной цели сразу; 401 по-прежнему фатален.
- [x] **Детектор застоя** — ✅ (2026-08-31, next) `StallDetector`: три хода без изменений файлов и без сдвига плана — строка в ленте, для автопилота это необычность и возврат управления.
- [x] **Дата отключения модели** — ✅ (2026-08-31, next) `sunsetDate` + `ModelSunset`: предупреждение за 30 дней в диагностике, день отключения ещё рабочий, после даты модель не предлагается.
- [x] **Цепочка фолбэка внутри одного вендора** — ✅ (2026-08-31, next) `FailoverPlan.isSingleVendor` + строка в диагностике; предупреждение, а не запрет.
- [x] **Аудит факта использования секрета** — ✅ (2026-08-31, next) событие `secret_used`: имена и потребитель, значения никогда.
- [x] **Потолок расхода на отрезок автопилота** — ✅ (2026-08-31, next) `STOP_BUDGET` и настройка «токенов на отрезок» (дефолт 200 000, 0 — выключено).
- [x] **Мануал сравнения моделей** — ✅ (2026-08-31, next) [manuals/compareModels.md](manuals/compareModels.md): порядок прогонов, возврат дерева, что считать кроме метрики.
- [x] **Секреты в окружении ACP-агента** — ✅ (2026-08-31, next) `${secret:ИМЯ}` в `.vibe/acp.json` резолвится при старте из хранилища ОС и `.env`; токен уходит из файла. Время жизни внутри процесса не сокращено — названо ограничением, а не починено.
- [x] **Каталог навыков** — ✅ (2026-08-31, next) `SkillCatalog` + действие «Навыки проекта»: состояние относительно набора (как в наборе / изменён / свой / удалён), версия, вердикт валидатора и возврат к версии набора. Установка отдельной командой не понадобилась: сеялка ставит набор целиком и возвращает удалённое.
- [x] **Доменные навыки в VibeBrains** — ✅ (2026-08-31, next) `triage-failing-test` и `incident-review` в наборе (обзор PR уже был `review-pr`); манифест сеялки и указатель submodule подняты.
- [x] **Правило «бенчмарк меряет связку»** — ✅ (2026-08-31, next) запись в базе знаний со строкой в индексе.
- [x] **Занятый порт: освободить, сессионный порт или отмена** — ✅ (2026-08-31, next) диалог поверх готового `PortConflict`; освобождение проверяется, конфиг не правится, по умолчанию отмена.
- [x] **Голосовые в Telegram** — ✅ (2026-08-31, next) `VoiceNote`: локальный whisper снаружи, эхо распознанного до старта, предел 3 минуты, фильтр выдумок на тишине.
- [x] **Переключение формата инструментов** — ⬜ (2026-08-31) не применимо: схемы инструментов объявляет ACP-агент, прямому LLM-пути мы `tools` не отправляем — переключать нечего.

## Перенос из роадмапа VibeIDE (2026-08-31)

Прочитан открытый хвост роадмапа VibeIDE (65 незакрытых пунктов). Большая часть — их внутренние
долги и вещи, привязанные к VS Code; ниже то, что применимо к нам, с проверкой по нашему коду.
Отклонённое названо в конце, чтобы вопрос «а это почему не взяли» не задавался дважды.

- [~] **Семантические петли: сделано НАБЛЮДЕНИЕ, детектора нет** — (2026-08-31, next) `/trace` показывает похожие вызовы («Read Foo.kt (1-50)» и «(40-90)» сливаются в одну строку с числом повторов). Условие пункта соблюдено: сперва смотрим на реальных ходах, детектор — только по факту случая. Останавливать ход по догадке о чужих аргументах — то, что VibeIDE счёл хрупким, и мы согласны.
- [x] **Предохранитель повторных таймаутов и «N из последних M»** — ✅ (2026-08-31, next) `ThrashDetector`: одна и та же команда, зависшая трижды в окне из десяти вызовов (успех между таймаутами счётчик НЕ обнуляет — это и была чужая дыра), и половина окна в отказах, включая ошибки вперемешку с успехами, которых «подряд»-счётчик не видит. Порог выведен тестом: при шести из десяти чередование ошибки с успехом не срабатывало никогда.
- [x] **Мульти-окно на одном файле состояния** — ✅ (2026-08-31, next) `PlanMerge`: перед записью файл перечитывается и сливается — свой поток пишет пишущий, чужие берутся по более свежей отметке, при ничьей побеждает файл; запись атомарная (временный файл + move), подрезка никогда не выбрасывает план пишущего.
- [x] **Вложенные правила проекта** — ✅ (2026-08-31, next) `.cursor/rules` читаются в папках-предках затронутых файлов; одноимённое правило пакета перекрывает корневое, вложенное едет только с файлами своей папки. Обхода дерева нет: цена пропорциональна файлам в работе, а не размеру монорепозитория.
- [x] **Атрибуция расхода по файлам** — ✅ (2026-08-31, next) `FileSpend`: доля хода делится между вложенными файлами по их размеру, остаток уходит крупнейшему (сумма долей равна целому), верхушка недели с числом ходов — в отчёте о расходе. Помечено оценкой прямо в отчёте: запрос оплачивается целиком, доля не измеряется.
- [x] **Очистка расшифровки локальной моделью** — ✅ (2026-08-31, next) `VoiceCleanup`: промпт запрещает домысливать, результат проверяется против исходного (длина, признаки «модель ответила вместо чистки») и при любом сомнении берётся сырой текст; работает ТОЛЬКО на локальном провайдере, дословная расшифровка показывается рядом.
- [x] **Свой значок каждой панели, и панели видны** — ✅ (03.09.2026, next) семь тулвиндоу делили одну картинку (у дев-стека значка не было вовсе), а пять были помечены `secondary="true"` и уезжали под «…» — часть продукта, которую не видно. Метафоры: искра, щит с галочкой, линейка, пульс, книга, часы, серверная стойка. Четвёрка файлов на значок (16/20 × светлый/тёмный) генерируется из одного описания `vibe-plugins/tools/makeIcons.py`. Гейт UI ловит четыре случая — расхождение с описанием, один значок у двух панелей, панель без значка, панель во «вторичных»; все прогнаны на настоящих поломках. Вид проверен рендером рядом с платформенными значками.
- [x] **Блок версий в «О программе»** — ✅ (03.09.2026, next) по образцу VibeIDE: версия продукта → платформа (intellij-community 2026.3, сборка) → коммит; `VibeAboutInfo` чистый (4 теста), провайдер через `aboutPopupDescriptionProvider` — без патча диалога; «Копировать» рендерится из тех же строк. Зависимость на `intellij.platform.buildData` ради ревизии из product-info.
- [x] **Канал обновлений** — ✅ (03.09.2026, next) решение 31.08 «не делаем до релизной инфраструктуры» пересмотрено: инфраструктура появилась (теги, GitHub Releases, штамп и публикация). `VibeExternalProductResourceUrls` (единственная точка расширения платформы, патч не нужен) читает `updates/updates.xml` с ветки `main`; тем же переключением баг-репорт ведёт в наш трекер. Нашлось по дороге и стало половиной работы: **`263.SNAPSHOT` читается платформой как `Integer.MAX_VALUE`** — установка считает себя новее любого числа, и канал молчал бы навсегда. Релизная сборка получает настоящий номер в нашем `VibeIdeaInstallersBuildTarget` (`VibeBuildNumber`: `0.3.1` → `263.301`, без правок скриптов обеих ОС). `releaseUpdatesXml.sh` пишет файл по `product-info.json` артефакта; штамп отказывает на SNAPSHOT и на файле без записи. Одного канала достаточно: stable/beta/nightly без сборок под них — переключатель, который ничем не управляет. Не проверено до сборки: что артефакт получит `263.300`. Разбор — [knowledge/build/updateChannel.md](knowledge/build/updateChannel.md).
- [x] **Установка языковых серверов кнопкой** — ✅ (2026-08-31, next) в уведомлении о недостающем сервере есть «Поставить»: login-оболочка, проверка результата поиском бинаря, отказ от команд с `sudo` и `| sh`. Остальные внешние движки (`yt-dlp`, whisper, `pdftotext`) — той же кнопкой, когда дойдут руки.

## Поставка языковых серверов вместе с IDE (решение владельца, поставлен 2026-08-31)

**Вопрос владельца:** «нужно сразу подумать, как поставлять с программой, как это делает PhpStorm».

**Как это на самом деле у PhpStorm.** Он не поставляет языковой сервер — у него **свой** парсер и
индекс PHP внутри IDE, закрытый код JetBrains. Повторить это невозможно и не нужно: наш ответ на
ту же задачу — LSP, и вопрос сводится к тому, класть ли серверы в дистрибутив.

**Что измерено (31.08.2026, на машине владельца):**

| Что | Размер | Нужен рантайм | Лицензия |
|---|---|---|---|
| `phpactor.phar` | **4,3 МБ** | PHP ≥ 8.1 на машине | MIT |
| `@vtsls/language-server` + TypeScript | **29 МБ** | Node | MIT / Apache-2.0 |
| `vscode-langservers-extracted` целиком | **93 МБ** (из них 64 МБ — второй экземпляр TypeScript, 15 МБ — core-js) | Node | MIT |
| только css+eslint из него, без чужого TypeScript | ориентировочно **8–10 МБ** (нужна проверка сборкой) | Node | MIT |
| Node 24 | ~110 МБ на платформу | — | MIT |

Сегодняшний dmg — 784 МБ.

**Три ступени, каждая осмысленна сама по себе:**

- [x] **Ступень 1 — Phpactor в комплекте** — ✅ (2026-08-31, next) phar 4,3 МБ закреплённой версии с проверкой sha256 и лицензией рядом, кладётся в `plugins/vibe-lsp/servers`; свой сервер сильнее встроенного, доктор называет источник и предупреждает об отсутствии PHP.
- [x] **Ступень 2 — npm-серверы в комплекте, Node с машины** — ✅ (2026-08-31, next) `npm ci` по закреплённому локу, выброшена вложенная копия TypeScript (64 МБ, нужна только неподключённым html/markdown — проверено, что css, eslint и vtsls без неё стартуют). Итог **56 МБ**, а не 35–40, как я оценил до замера. Свой сервер по-прежнему сильнее встроенного; доктор называет недостающий рантайм.
- ⬜ **Ступень 3 — Node в комплекте — ИЗБЫТОЧНО** (решение владельца 2026-08-31). Node ставится
  отдельно по классике, и предполагать обратное про фронтендера незачем. Цена была бы +110 МБ на
  платформу и второй рантайм, который придётся обновлять при каждой его уязвимости, — ради
  предусловия, которое на деле выполнено у всех. Пункт закрыт, к нему не возвращаемся.

**Что обязательно вместе с любой ступенью:**

- [x] **Свой сервер вместо встроенного** — ✅ (2026-08-31, next) сделано порядком поиска, а не
  настройкой: сначала пользовательский бинарь, потом наш. Отдельный тумблер не заводим — он бы
  спрашивал о том, у чего есть единственный правильный ответ.
- [x] **Отчёт о третьих лицах** — ✅ (2026-08-31, next) LSP4IJ, Phpactor, vtsls и `vscode-langservers-extracted` объявлены в `allLibraryLicenses` (отчёт строится по зависимостям модулей и phar с npm-деревом не видит), а гейт дистрибутива сверяет версии в отчёте с закреплёнными в `download.sh` и `package.json`.
- [x] **Ответ на устаревание** — ✅ (2026-08-31, next) названо в мануале: встроенный сервер стареет
  вместе с релизом IDE, свой всегда сильнее, обновляется установкой своего.

**Решение за владельцем.** Моя рекомендация: ступень 1 сейчас (дёшево и закрывает целый язык),
ступень 2 следующей волной, ступень 3 — не делать.
- [x] **Гейт дистрибутива** — ✅ (2026-08-31, next) `checkVibeDist.sh` по собранному dmg: плагины в индексе, zxing внутри плагина, серверы на месте и запускаются (CSS отвечает на `initialize`), висячих ссылок нет. Проверен подложенным дефектом; ловит всё семейство граблей упаковки этого дня.
- [x] **Конфликт засева: «Смержить» и «Обновить»** — ✅ (2026-08-31, next) к «Сравнить» и «Оставить своё» добавлены перенос правок в редактируемом diff и взятие релиза с сохранением своей версии рядом (`<имя>.mine`).

## Публикация (2026-08-31)

- [x] **Релиз v0.2.0** — ✅ (01.09.2026) отладка из коробки (адаптеры в комплекте, конфигурация одним действием, формы запуска), потолки расхода в деньгах с прогнозом, атрибуция и цепочка целостности журнала с проверкой вне IDE, внешний каталог причуд, протокол на уровне модели, свой CI. 25 коммитов сверх v0.1.0, dmg 826 МБ, sha256 `804c709594…`. Заметки — [references/releaseNotes-v0.2.0.md](references/releaseNotes-v0.2.0.md).
  - Гейт дистрибутива **остановил публикацию** и оказался прав ровно наполовину: падал не дистрибутив, а сам гейт — зашитый номер порта был занят процессом от прошлого прогона, и проверка обвиняла сборку в том, чего в ней нет. Порт теперь выбирает ОС; проверено двумя прогонами подряд.
  - Загрузка 826 МБ не укладывается в лимит одной команды — публикация и заливка ассета делаются раздельно (записано в мануал).


- [x] **Репозиторий опубликован** — ✅ https://github.com/VibeBrains/VibeIDEA, ветка по умолчанию `main`, описание и темы проставлены. Пуш из поверхностного клона отвергался сервером — корень истории пересобран (разбор в [knowledge/gitAndTools/shallowClonePush.md](knowledge/gitAndTools/shallowClonePush.md)).
- [x] **Первый релиз v0.1.0** — ✅ https://github.com/VibeBrains/VibeIDEA/releases/tag/v0.1.0, dmg 833 МБ загружен, заметки в стиле VibeIDE, блок поддержки с QR.
- [x] **Порядок релиза записан** — ✅ [manuals/release.md](manuals/release.md): каждый шаг с фактом-подтверждением, включая права учёток и ловушку поверхностного клона.
- [x] **Своя лицензия в дистрибутиве** — ✅ условия JetBrains описывают их сборки; в наш dmg едет свой `LICENSE.txt` (Apache 2.0, «не продукт JetBrains», состав сборки, отсутствие телеметрии).
- [~] **Прогон сборки на живой Windows-машине** — ✅ частично (02–03.09.2026, Windows 11): сборка прошла через `vibeidea-installers.cmd` и bash-скрипты из Git Bash (гейты исходников, `download.sh`, `checkVibeDist.sh`, штамп и публикация — всё исполнено, артефакты в релизе v0.3.0; разбор — [knowledge/build/windowsBuild.md](knowledge/build/windowsBuild.md)). Из PowerShell-близнецов прогнаны два: `download.ps1` — **работает** (хеши совпали, vsix распакован с `out/phpDebug.js`); `checkVibeDist.ps1` на том же zip — **красный**: «адаптер vscode-php-debug не ответил успехом на initialize», хотя тот же адаптер отвечает bash-гейту и ручному запросу; вероятная причина — фиксированная пауза 4 с и закрытие stdin до старта адаптера (bash-гейт на этом же плавал, пока не стал ждать признак ответа вместо паузы). `vibeidea-build.bat` и `getPlugins.bat` не запускались (сборка была уже сделана апстримовским `.cmd`). Осталось: починить проверку адаптера в `.ps1` тем же способом, прогнать `.bat` с нуля.
- [ ] **Подпись кода: macOS и Windows** — ⛔ блокер владельца: нужны учётки и деньги (Apple Developer Program; сертификат OV/EV или Azure Trusted Signing). **Всё остальное готово** (03.09.2026): `signMac.sh` (codesign `--deep --options runtime` → dmg → `notarytool --wait` → `stapler`) и `signWindows.ps1` (`signtool` с меткой времени по всем exe/dll и инсталлятору) работают после сборки и до штампа, без учётки останавливаются первой строкой и говорят, чего нет; `--dry-run` перечисляет, что подпишут. Порядок — [manuals/release.md §2в](manuals/release.md). Повод по Windows: Касперский удалил `idea64.exe` по поведению (`PDM:Trojan.Win32.Generic`, разбор — [knowledge/build/windowsAntivirus.md](knowledge/build/windowsAntivirus.md)); по macOS — Gatekeeper «Open Anyway» с первого dmg. Не проверено: сами подписи — проверить нечем, пока нет сертификата; на этой машине `codesign`, `notarytool`, `stapler` есть, идентичностей 0.
- [x] **Голосовой ввод в IDE** — ✅ (03.09.2026, next) развилка «какую нативную библиотеку брать» решена ответом «никакую»: запись `javax.sound.sampled` из поставляемой JRE, 16 кГц моно — формат whisper, ffmpeg не нужен. Расшифровка вынесена из Telegram-моста в общий `VoiceTranscription` (иначе фильтр выдумок whisper на тишине жил бы в двух копиях). Кнопка микрофона в композере: результат в черновик, не в отправку. Предохранители: <0,7 с — промах, >5 мин — микрофон выключается сам. 4 теста. Не проверено на живой машине: захват с настоящего микрофона (нужен человек у клавиатуры).
- [x] **Три плагина PhpStorm-паритета из своего же дерева** — ✅ (03.09.2026, next) `intellij.dotenv` (MIT, JetBrains бандлит в PhpStorm с 2024.3.2), `intellij.xpath` (раскладка уже была написана), `intellij.jsonpath`. Модуль без раскладки получает `pluginAuto` (`DistributionJARsBuilder.kt:554`), поэтому цена — строка на плагин. Проверено сборкой: все три в образе. Гейт при этом поймал мою ошибку — каталог зовётся по МОДУЛЮ (`plugins/dotenv`), а не по папке исходников (`env-files-support`).
- [~] **Релиз 0.3.1 остановлен по решению владельца (03.09.2026)** — версия была поднята, `main` подтянут, гейты и 1281 тест зелёные, сборка запущена; на вопрос «рано или нет» владелец ответил «ждём БД с HTTP-клиентом». Откат сделан полностью: бамп снят, `main` возвращён на `origin/main`, тега нет, артефактов нет — публиковать было нечего и ничего не публиковалось. Следующий выпуск несёт работу с БД и HTTP-клиент, то есть это **0.4.0**, а не 0.3.1. В нём же поедет уже готовое и лежащее в `next`: PHP на Windows, MCP, задачи и трекеры, `.env`/XPath/JSONPath, голосовой ввод, канал обновлений, блок версий в «О программе». Цена ожидания названа вслух: у пользователей Windows до выпуска PHP не работает вовсе, а канал обновлений начинает действовать только с той версии, которая его несёт.
- [ ] **Работа с БД — развилка для владельца** — ⛔ решение: (А) вбандлить Database Navigator (Oracle, Apache-2.0, даёт браузер объектов и SQL-консоль) ценой 76,6 МБ архива, половина из которых ораклов стек; выпущенная версия объявляет `until-build=262.*`, но для ВШИТОГО плагина платформа совместимость не проверяет (`PluginInitializationContext.validatePluginIsCompatible` — по коду, **на живой сборке не прогонялось**); (Б) свой инструмент поверх открытого `grid/` (табличный движок уже в бандле; JDBC-консоли и источников данных в дереве нет). Database Tools and SQL от JetBrains недоступен: проприетарный, исходников нет. Разбор — [references/phpstormParity.md](references/phpstormParity.md).
- [~] **HTTP-клиент («как Postman») — свой** — ✅ первый слой (03.09.2026, next), новый плагин `vibe-http`: разбор `.http` (разделители, пометки, тело из файла, сохранение ответа), окружения двумя файлами (приватный сильнее общего), cURL в обе стороны, сборка настоящего запроса и отправка, панель со списком запросов и ответом, действие по Ctrl+Alt+Enter, тип файла и значок. 32 теста нашли три настоящих дефекта: `# @name` перед строкой запроса молча терялся, собственный вывод cURL не читался обратно из-за экранирования апострофа, а неподставленная переменная в адресе отдавала «адрес не разобран» вместо «переменная не подставлена». Осталось: подсветка синтаксиса `.http` (сейчас файл открывается как текст), история ответов, вкладка «cookies», генерация запроса из OpenAPI. Спека — [manuals/httpClientSpec.md](manuals/httpClientSpec.md).
- [x] **Задачи и трекеры как в PhpStorm** — ✅ (03.09.2026, next) в `bundledPluginModules` добавлен `intellij.tasks.core` (community, Apache 2.0, раскладка уже описана в `CommunityRepositoryModules`). Платформа давала только ядро Tasks & Contexts — без этого плагина нет ни Open Task, ни трекеров. Проверка по собранному дистрибутиву: `checkVibeDist.sh` теперь требует `plugins/tasks`.
- [x] **PHP на Windows работает встроенным Phpactor** — ✅ (03.09.2026, next) сначала за день был сделан обход — Intelephense по умолчанию для Windows; владелец потребовал «без костылей», и разбор распакованного phar показал, что требование `ext-posix` у Box **ложное**: все `posix_*` внутри защищены `function_exists`, у самого Phpactor их нет. Решение — однострочный `phpactorNoPosixCheck.php` рядом с phar и `-d auto_prepend_file` на Windows (не окружение: процесс запускает клиент LSP). Проверено на macOS с отключёнными 40 функциями posix и на распакованном проверяющем Box; `checkVibeDist.sh` повторяет эмуляцию, `checkVibeDist.ps1` запускает phar с выключателем по-настоящему — живое подтверждение придёт с первой сборкой на Windows. Движок остался выбором (`PhpEngine`: АВТО = встроенный Phpactor везде, Intelephense — явно), один вход `vibePhp` в LSP4IJ. Разбор — [knowledge/languages/phpOnWindows.md](knowledge/languages/phpOnWindows.md).
- [x] **Гейт локализации перестал считать комментарии кодом** — ✅ (03.09.2026, next) найдено по дороге: проверка искала `t("ключ")` по сырому тексту, поэтому упоминание ключа в комментарии считалось использованием — так в каталоге пережили две мёртвые строки, а комментарий, объясняющий вызов, наоборот требовал завести строку под цитату. Разбор вынесен в `vibe-plugins/tools/ktComments.py` (знает про строковые литералы и raw-строки): наивный `//` съедал хвост строки после `https://`. Тем же разбором считает храповик — и сразу увидел русский литерал, спрятанный внутри raw-строки.
- [x] **Бит исполнения на новых скриптах** — ✅ (03.09.2026, next) `pythonLf.sh`, `pythonBin.sh` и `releasePackagingOnly.sh` приехали из Windows-сессии режимом 100644: под Git Bash это незаметно, а на macOS и Linux все четыре гейта исходников падали с «Permission denied». Режим правится в индексе (`git update-index --chmod=+x`), иначе следующий клон получит то же самое.
- [x] **Первый прогон на Windows: три дефекта** — ✅ (03.09.2026, next) владелец запустил сборку на живой машине, и она сразу дала то, чего не даёт ни один тест на macOS: (1) агент не стартовал — `Cannot run program "C:\Program Files\nodejs\npx": CreateProcess error=193`: рядом с `npx.cmd` лежит `npx` для Git Bash, и `Files.isExecutable` считает исполняемым его тоже, а наш резолвер брал первый попавшийся; теперь имена с расширением из `PATHEXT` пробуются раньше голого (6 тестов); (2) тот же дефект в поиске языковых серверов — порядок перебора стал внешним по именам, а не по каталогам; (3) кнопка «Поставить» звала `/bin/sh`, которого в Windows нет, а команды установки были unix-однострочниками с `mkdir -p` и `unzip` — теперь оболочка по системе и отдельные команды для Windows (4 теста).
- [ ] **Подпись и нотаризация сборки** — нужна учётка разработчика Apple (решение владельца). Сейчас macOS пугает при первом запуске, и обход описан в заметках релиза.
- [~] **Инсталляторы Windows и Linux** — ✅ (02.09.2026, next) **скрипты сборки написаны**: `vibeidea-build.bat` (одна команда: зависимости → сборка → подсказка про проверку), `vibe-plugins/deps/download.ps1` и гейт дистрибутива `checkVibeDist.ps1`; версии внешних артефактов вынесены в единый `vibe-plugins/deps/pins.env`, который читают оба скрипта загрузки и оба гейта — вторая копия версий разошлась бы молча. Гейт дистрибутива научился Linux-архивам (`.tar.gz`, распакованная папка, `.app`) — разница между платформами свелась к одной строке про расположение `plugins`, а не ко второй копии проверок, которая разошлась бы молча. Проверено на четырёх входах: dmg, распакованный `.app`, синтетический Linux-архив (даёт поимённые претензии и код 1) и чужой архив (говорит «это не наша сборка»). Мануал — [manuals/buildWindows.md](manuals/buildWindows.md). ✅ **Windows собран** (02.09.2026, main): на Windows 11 через `vibeidea-installers.cmd` и bash-скрипты из Git Bash, проверен гейтом дистрибутива, IDE из архива стартует, артефакты в релизе v0.3.0 (см. «Релиз 0.3.0»); при слиянии `main` → `next` bash-гейт объединён: dmg, tar.gz, win.zip, .app и папка в одном скрипте. Linux — не собирался, в релизе помечен как непроверенный.
