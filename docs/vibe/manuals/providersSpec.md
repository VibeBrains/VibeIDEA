# Спека: провайдеры — каталог `.vibe/providers/` + `providers.json`

> Скопируйте этот файл целиком своей LLM и попросите собрать конфиг провайдера под вашу задачу — этого документа достаточно, в интернет и исходники ходить не нужно.

## Что это

Реестр LLM-провайдеров для прямого чата в панели **Vibe Agent** (записи вида `LLM: <провайдер> · <модель>` в выпадашке). Формат записи совместим с VibeIDE (форк VSCode) — конфиг переносится между продуктами. Ключи API в файлах **не хранятся никогда** — файлы можно коммитить.

## Откуда собирается реестр

Четыре слоя, от слабого к сильному (поздний перекрывает ранний по `id`, по-полево; `models.static` мёржится по id модели):

1. **Каталог `~/.vibe/providers/`** — глобальные файлы-провайдеры (`*.jsonc` или `*.json`, по файлу на провайдера, читаются автоматически, внутри каталога — по алфавиту имён).
2. **Каталог `<проект>/.vibe/providers/`** — то же на уровне проекта. VibeIDEA засевает его готовыми конфигами (OpenCode Go/Zen, OpenRouter, MiniMax, Z.AI, Kimi, DeepSeek активны; OpenAI, Anthropic, Alibaba Coding Plan, Meta Muse, локальные Ollama/vLLM лежат выключенными).
3. **`~/.vibe/providers.json`** — глобальный пользовательский файл.
4. **`<проект>/.vibe/providers.json`** — проектный пользовательский файл, самый сильный.

Каталог играет роль «встроенных провайдеров» VibeIDE — и, как встроенные, лежит **под** обоими пользовательскими файлами: засеянная запись не может заглушить или переписать ваш `providers.json` ни на одном уровне.

**Зачем вообще глобальный уровень `~/.vibe`, если каждый проект получает свой каталог.** Каталог там не нужен — его сеют в проект и обновляют по ревизиям. Глобальный уровень остаётся ровно для двух вещей: решения, которые должны действовать во ВСЕХ проектах (например, `{"id":"openai","active":false}` — выключить провайдера везде), и ваши личные записи, которых нет в наборе (корпоративный прокси, локальный сервер), чтобы не копировать их в каждый проект. Всё остальное держите в проекте: чем меньше в `~/.vibe/providers.json`, тем меньше шансов, что глобальная запись молча перекроет свежий сид — она сильнее его по определению. Дубликат засеянного провайдера в глобальном файле — верный способ получить «в файле каталога написано одно, а в IDE другое».

- **Тумблер — `active`**: все засеянные провайдеры включены, заработает тот, у кого появится ключ; не нужен — `"active": false` в его файле (запись остаётся в слиянии, её можно `extends`-ить и патчить по `id`, но в списках её нет). Важно: **запись без поля `active` считается активной** — поэтому ваш давний `providers.json` продолжает работать, даже если засеянный файл с тем же `id` выключен. Обратная сторона: патч по `id`, который не должен включать выключенную запись, обязан явно повторить `"active": false`.
- Выключить провайдера, не трогая каталог (и не ловить потом вопросов при обновлении набора): `{ "id": "openai", "active": false }` в своём `providers.json` — проектном для проекта, глобальном для всех.

## Жизненный цикл засеянного файла

Файлы каталога принадлежат релизу, `providers.json` — вам. Что происходит с засеянным файлом, когда набор обновляется:

| Ваша копия | Что делает IDE |
|---|---|
| не тронута (совпадает с любой прошлой ревизией набора) | **обновляется молча** — терять нечего |
| совпадает с новой ревизией | ничего |
| изменена вами, ревизия набора та же | ничего и никогда — это ваш файл |
| изменена вами, а набор ушёл вперёд | **не трогаем**, показываем уведомление: «Сравнить» (diff релиз ↔ ваш файл), «Обновить, сохранив мои тумблеры» (доступно, когда единственное отличие — значения `active`: файл обновляется, а ваши «выключено» переезжают в `providers/zz-local-toggles.jsonc` — он сортируется последним в каталоге и потому сильнее сидов), «Оставить своё» (запоминается — не спросим до следующей ревизии) |
| файл удалён/переименован в наборе | не правленная копия удаляется сама, правленная остаётся |

Ревизии живут в `versions.json` набора (номер, sha256 текущей версии и всех прошлых); ваш `providers.json` и любые собственные файлы каталога в этом не участвуют вовсе — их никто не трогает.
- Каждый файл разбирается независимо: сломанный файл не гасит остальных; битая запись пропускается с предупреждением в чате и не роняет реестр.
- Формат JSONC: `//`-комментарии и висячие запятые разрешены. Изменения подхватываются на лету: ключ из настроек — сразу, правки файлов (`providers.json`, каталог, `.vibe/.env`) ловит вотчер обоих уровней и открытая панель перечитывает реестр сама; FIM перечитывает реестр раз в 30 секунд.

## Формат

Каждый файл каталога — самодостаточный документ той же схемы, что `providers.json`:

```jsonc
{
  "version": 1,
  "providers": [
    {
      // Обязательное: id (уникальный) и baseURL.
      "id": "ollama",
      "name": "Ollama (локально)",
      "protocol": "openai",              // openai (дефолт) | anthropic | gemini
      "baseURL": "http://localhost:11434/v1",
      "auth": "bearer",                  // "bearer" | "none" | {"type":"header","name":"x-api-key"} | {"type":"query","name":"key"}
      "apiKeyEnv": "OLLAMA_API_KEY",     // имя переменной: ищется в <проект>/.vibe/.env → ~/.vibe/.env → окружении ОС
      "apiKeyRef": "ollama",             // или ключ из защищённого хранилища ОС (PasswordSafe), сильнее apiKeyEnv
      "headers": {},                     // статические заголовки
      "query": {},                       // статические query-параметры
      "timeoutMs": 600000,
      "active": true,                    // тумблер: false прячет провайдера и его модели (но слияние/extends работают)
      "order": 10,                       // порядок среди ваших провайдеров (меньше = выше)
      "models": {
        // fetch: true (дефолт) = авто-каталог с <baseURL>/models; строка = полный URL каталога;
        // false = только static. Ответ понимается и openai-вида {data:[{id}]}, и gemini-вида {models:[{name}]}.
        // Найденные модели добавляются к static; static сильнее по совпавшему id.
        "fetch": true,
        "static": [                      // кастомные модели — см. раздел ниже; обычно НЕ нужны
          {
            "id": "qwen3:14b",           // обязательное: как принимает API
            "name": "Qwen3 14B",
            "default": true,             // модель по умолчанию (первая в списке)
            "pinned": false,
            "active": true,
            "contextWindow": 131072,
            "maxOutputTokens": 8192,
            "temperature": 0.7,
            "topP": 0.95,
            "topK": 40,
            "extraBody": {},             // доливается в тело запроса дословно (квирки вендора)
            "protocol": "anthropic",     // протокол ЭТОЙ модели, если он отличается от провайдерского;
                                         // отсутствует = берётся протокол провайдера (см. раздел ниже)
            "cost": {                    // цена ВАШЕГО тарифа за миллион токенов; необязательно
              "input": 10,               // свежий ввод
              "output": 50,              // ответ
              "cacheRead": 0.25,         // чтение из промпт-кэша — отдельная ставка, не скидка
              "cacheWrite": 12.5,        // запись в кэш
              "currency": "USD",         // по умолчанию USD
              "timeOfDay": {             // цена по часу (раздел «Цена по часу»); необязательно
                "peakUtc": ["01:00-04:00"],  // окна пика по UTC; ставки выше — пиковые
                "peakDays": ["mon", "tue", "wed", "thu", "fri"], // дни пика; нет поля — каждый день
                "offPeakFactor": 0.5     // множитель ко всем ставкам вне пика
              }
            },
            "costNote": "docs вендора",  // где цена объявлена: попадёт в предупреждение о сроке
            "reasoning": {               // что умеет ЭТА модель; необязательно
              "canTurnOff": true,        // можно ли выключить рассуждение совсем
              "effort": ["low", "high"], // уровни, которые она принимает
              "off": { "thinking": { "type": "disabled" } } // что слать в положении «выключено»
            },
            "cacheTtl": "1h",            // срок жизни кэша промпта: "5m" (умолчание вендора) или "1h"
            "costValidUntil": "2026-09-09", // до какой даты цена выше верна (ISO); необязательно.
                                         // Промо кончаются по расписанию и вдвое: у GLM-5.3-Flash
                                         // $0.075/$0.25 стали $0.15/$0.50 в 24:00 UTC+8 09.09.2026.
                                         // Без срока учёт расхода молча считается по старой цене.
            "costAfter": {               // цена, которая наступит после срока; формат тот же.
              "input": 0.15,             // Решение принимают по НЕЙ: «вдвое» и «вдесятеро» —
              "output": 0.50             // разные решения, а фраза «срок вышел» у них одна.
            },                           // Расход по ней не считается никогда: это будущее.
            "fim": false,                // отдать модель inline-автокомплиту (openai-протокол, см. ниже)
            "vision": true               // модель принимает изображения; отсутствует = неизвестно (вложение не блокируется),
                                         // false = композер заблокирует отправку картинки с этой моделью
          }
        ]
      }
    },
    // Клон: новый id + extends — унаследовать все поля другой записи и переопределить нужные.
    // extends работает и поперёк файлов (базой может быть запись любого файла реестра, включая
    // неактивную — клон при этом активен, если сам не скажет иного); одноуровневый, без цепочек.
    { "id": "ollama-fast", "extends": "ollama", "name": "Ollama fast", "models": { "static": [ { "id": "qwen3:4b", "default": true } ] } }
  ]
}
```

## Общий набор: кому адресована запись (`products`)

Каталог провайдеров приезжает из **общего набора**, который пишут несколько продуктов сразу — VibeIDE, VibeIDEA, дальше VibeCLI. Необязательное поле `products` говорит, кому запись адресована: массив стабильных id в нижнем регистре (`vibeide`, `vibeidea`, `vibecli`). Поля нет — запись **для всех**, и для каталога моделей это верно почти всегда: вендор один на все продукты.

Это НЕ то же самое, что `active` (см. выше): «кому адресовано» и «включено ли» — разные вопросы, и запись бывает чужой и включённой у соседа.

- **Запись не для этого продукта пропускается молча.** Это не ошибка и не предупреждение.
- **Незнакомый id продукта — тоже молча мимо.** Сборка, выпущенная сегодня, увидит запись для продукта, которого ещё нет, и промолчит.
- **`products` не защищает от старых сборок.** Сборка, которая о поле ещё не знает, игнорирует его и запись применяет. Поэтому адресная запись, пока она не должна работать везде, едет с `"active": false`.
- **Выключенная запись не проверяется вообще** — она не заработает, и претензии к её содержимому предъявлять некому.

## Цена модели: зачем её писать руками

Своей таблицы цен у VibeIDEA нет и не будет: она устареет в день, когда вендор поменяет строку, а
потолок расхода, остановивший работу по выдуманному числу, останавливает её по выдуманной причине.
Но цена, которую вы написали сами, — факт о вашем договоре, и её IDE обязана применять.

Что это включает:

- **расход в деньгах** там, где провайдер не присылает стоимость сам (а таких большинство);
- **потолки расхода** (Settings → Tools → VibeIDEA → Чат) начинают работать в прямом чате;
- **строку экономии на кэше**: «кэш сэкономил $7,80» — единственная статья, на которую человек
  может повлиять сам, не меняя модель.

Токены при этом **не оцениваются**, а берутся из ответа провайдера: оба протокола сообщают `usage`,
и мы читаем его — вместе с числом токенов, прочитанных из кэша. Провайдер, который `usage` не
прислал, оставляет прежнюю оценку по длине ответа, и цена тогда не считается вовсе: пустая ячейка
честнее выдуманной.

Ставка чтения из кэша задаётся **отдельно, а не коэффициентом**: у Claude Fable 5.1 это 0,025
базового ввода против 0,1 у остальных моделей Claude, и один «коэффициент скидки» пришлось бы
переписывать при каждом таком изменении.

## Когда у одного провайдера несколько форматов API

Обычно протокол — свойство провайдера, и поле `protocol` пишется один раз наверху. Но бывает
иначе: у **OpenCode Go** один ключ, один `baseURL` и три формата, привязанных к моделям —
MiniMax и Qwen обслуживаются Anthropic-совместимым `/v1/messages`, GLM/Kimi/DeepSeek —
`/v1/chat/completions` ([документация](https://opencode.ai/docs/go/)).

Для такого случая `protocol` пишется **у модели** и перекрывает провайдерский:

```jsonc
{
  "id": "opencode-go",
  "protocol": "openai",              // умолчание для всех, кто ничего не сказал
  "baseURL": "https://opencode.ai/zen/go/v1",
  "models": {
    "fetch": true,
    "static": [
      { "id": "minimax-m3", "protocol": "anthropic" },   // этот зовём по /v1/messages
      { "id": "qwen3.8-max", "protocol": "anthropic" }
    ]
  }
}
```

Правила: молчание наследует провайдерский протокол; написанное перекрывает; незнакомое имя
откатывается к `openai`, а не роняет реестр — файл, написанный для более новой IDE, не должен
выключать остальные записи, а неверный протокол всё равно падает громко на первом же запросе.
При наложении слоёв протокол модели ведёт себя как любое другое необязательное поле: слой,
который о нём не сказал, его не стирает.

## Кастомные модели (для гиков)

Вшитых списков моделей нет — модели приходят с каталога провайдера (`models.fetch`) после подключения ключа. Запись в `models.static` — **кастомная модель**: она нужна, только когда

- модели нет в каталоге провайдера (или каталога нет вовсе — `fetch: false`);
- модели нужны фиксированные параметры: `temperature`/`topP`/`topK`, `maxOutputTokens`, вендоро-специфичное поле в `extraBody` (например `{"thinking":{"type":"enabled"}}` у Moonshot или `{"tool_stream":true}` у DashScope/GLM);
- надо объявить `vision: false` для text-only модели (композер начнёт блокировать картинки заранее);
- модель отдаётся inline-автокомплиту: `fim: true` (openai-протокол, legacy `/completions`).

В списке моделей кастомная запись помечается бейджем **«кастом»** — осознанно: она задана рукой, и если модель перестала отвечать, возможно, у провайдера её уже нет (каталог этого не заметит за вас). Закомментированный образец — в засеянном `_template-openai-compatible.jsonc`.

## Ключи API

**Удобный путь: Settings → Tools → VibeIDEA → Провайдеры** — поле ключа на каждого активного провайдера, значение сохраняется в защищённое хранилище ОС (Keychain) под `apiKeyRef` (или id провайдера, если `apiKeyRef` не задан). Один `apiKeyRef` у нескольких провайдеров = один ключ на всех (так у OpenCode Go/Zen).

Приоритет резолва: защищённое хранилище (`apiKeyRef` → id) → `.vibe/.env` проекта → `~/.vibe/.env` → переменная окружения ОС (имя из `apiKeyEnv`). Формат `.env`: строки `ИМЯ=значение`, `#`-комментарии; интерполяции нет; файл в `.gitignore`. Для localhost-провайдера ключ не обязателен, чат помечается меткой `[локальная модель]`.

Точный путь: клиент дописывает к `baseURL` только имя метода — anthropic: `/messages`, openai: `/chat/completions`, gemini: `/models/<id>:streamGenerateContent` — поэтому версию пути включайте в `baseURL` сами: `https://api.anthropic.com/v1`, `http://localhost:11434/v1`, `https://generativelanguage.googleapis.com/v1beta`. Для gemini дефолтный auth-способ — `{"type":"query","name":"key"}` либо заголовок `x-goog-api-key` (auth `header`).

## Безопасность (Config Guard)

При загрузке реестр сканируется чистой функцией; находки печатаются в чат:
- `provider-endpoint-non-https` (critical) — не-HTTPS endpoint (localhost/127.0.0.1 легитимны, не флагаются);
- `provider-endpoint-raw-ip` (high) — сырой IP;
- `provider-hardcoded-secret` (critical) — креды в URL (`user:pass@`) или секрет-подобный литерал в `headers`/`query`.

## Отличия от VibeIDE (честные границы этой реализации)

- Встроенных (вшитых в код) провайдеров нет — их роль играет засеваемый каталог `providers/*.jsonc`: та же библиотека готовых конфигов, но обычными файлами, которые можно читать, править и `extends`-ить.
- `models.fetch` по умолчанию ходит на `<baseURL>/models` (в VibeIDE — `<baseURL>/v1/models`): у нас `baseURL` всегда включает версионный корень. Нестандартный путь — строкой с полным URL, как и в VibeIDE.
- `vision` — тристейт: `true`/`false`/отсутствует (неизвестно). При `extends` и слиянии незаданный `vision` наследуется от базовой записи той же модели. Незаданный `vision` заполняет и сам каталог провайдера, если он об этом говорит: `supports_image_in` у Moonshot, `architecture.input_modalities` у OpenRouter. Записанное руками сильнее каталога; молчащий каталог оставляет «неизвестно».
- `fim: true` питает inline-автокомплит (в VibeIDE для файловых провайдеров это осталось follow-up); `maxTools`/`maxPromptDirectoryChars` не читаются (нет потребителей).
- Вотчер файлов есть (оба уровня, включая `~/.vibe` вне проекта); кэша последнего рабочего набора нет осознанно — панель читает локальные файлы асинхронно за миллисекунды (решение №26).

## Проверка

Откройте любой проект — каталог `.vibe/providers/` уже засеян, семь провайдеров активны. Введите ключ в Settings → Tools → VibeIDEA → Провайдеры (кнопка «Проверить» дёргает каталог моделей), затем на странице «Модели» включите нужные модели из каталога (по умолчанию они скрыты — список выбора остаётся curated; кастомные из `static` видимы сразу) и нажмите OK/Apply — открытая панель Vibe Agent сама перечитает реестр: в выпадашке появятся `LLM: …`-записи → выберите и напишите сообщение: ответ стримится прямо с endpoint, без ACP-агента.

## Как называются поля цены

Канонические имена — `cost`, `costValidUntil`, `costAfter`, `costNote`: набор сидов `.vibe` общий
с VibeIDE, и так они пишутся там. Прежние наши имена `pricing`, `priceValidUntil`, `priceAfter`
остаются **принятыми синонимами** — уже написанные вручную файлы не ломаются, — но в новых записях
используйте `cost*`.

Урок, из-за которого это здесь написано (09.09.2026): общий набор переименовал поля, наш разбор
продолжал спрашивать старые, и цена, срок её годности и цена «после» разом перестали читаться.
Ошибки не было ни одной: модель просто выглядела как модель без цены, то есть как норма.

## Глубина рассуждения: что умеет модель

`reasoning` объявляет, какие уровни принимает **эта** модель: `canTurnOff` (можно ли выключить
рассуждение совсем) и `effort` (список уровней — `low`, `medium`, `high`; `max` читается как
`high`). Ползунок глубины один на всё приложение, а наборы уровней у вендоров разные, и просимый
уровень приводится к объявленному:

- просили уровень, которого у модели нет → берётся ближайший **снизу** (человек, двигавший ползунок
  влево, просил экономии), а если ниже ничего нет — самый низкий объявленный;
- просили «выключить» у модели с `canTurnOff: false` → берётся самый низкий объявленный уровень;
- поля нет → уровень уходит **как есть**: придумывать за вендора список уровней мы не вправе.

Без этого объявления ползунок отправлял выбранный уровень любой модели, и там, где его нет, вендор
либо отвечал 400, либо — хуже — молча игнорировал: человек двигает ручку и не видит разницы.

## Уровни рассуждения: словарь у каждого вендора свой

`"reasoning": { "canTurnOff": false, "effort": ["low", "high", "max"] }` — что принимает **эта**
модель. Список пишется **словами вендора и в порядке возрастания усилия**, а не переводится на
наши: у GLM-5.3 их три (`low`, `high`, `max`, без `medium`), у GPT-6 Astra пять (добавлены
`xhigh` и `max`).

Ползунок в чате трёхпозиционный плюс «выключено», поэтому его положение читается как «меньше
всего / посередине / больше всего», и отправляется **соответствующее слово из вашего списка**:
нижнее, среднее, верхнее. Тогда верхнее положение честно означает верхний уровень модели.

Списка нет — отправляется наше слово (`low`/`medium`/`high`): придумывать за вендора мы не вправе,
а молчать значило бы отключить ползунок тем, кто ничего не объявил.

`canTurnOff: false` — для моделей, которые рассуждение не выключают (GLM-5.3, Kimi K3). С ним
положение «выключено» отправляет **самый низкий объявленный уровень**; без него не отправляется
ничего, и модель думает на уровне вендора по умолчанию — у Kimi K3 это самый дорогой `max`.

## Как выключить рассуждение: `off`

Там, где рассуждение включено по умолчанию, «ничего не отправлять» не значит «выключить»: DeepSeek
V4.1 думает на `high`, пока в запросе нет `{"thinking": {"type": "disabled"}}`. Поэтому модель
объявляет **фрагмент тела**, который выключает рассуждение, и ползунок в положении «выключено»
отправляет его как есть:

```jsonc
"reasoning": {
  "canTurnOff": true,
  "effort": ["low", "high", "max"],
  "off": { "thinking": { "type": "disabled" } }
}
```

- Фрагмент, а не флаг: написание переключателя у вендоров разное, и его знает тот, кто пишет запись.
- Нет `off` — в положении «выключено» не уходит ничего: придумывать переключатель за вендора мы не
  вправе.
- `canTurnOff: false` сильнее `off`: модель, которая не выключается, получает самый низкий уровень,
  и до фрагмента дело не доходит.
- Поле читают не все сборки: там, где его не знают, выключить рассуждение из интерфейса нельзя.

## Надбавка за длинный промпт

Некоторые вендоры берут больше, когда промпт переваливает за порог. У GPT-6 Astra это объявлено
так: *«Prompts with more than 272K input tokens are priced at 2x input and cache rates and 1.5x
output **for the full request**»*. Ключевое — «for the full request»: дорожает весь запрос, а не
превышение над порогом.

```jsonc
"cost": {
  "input": 10, "output": 50, "cacheRead": 1, "cacheWrite": 12.5,
  "longContext": { "overInputTokens": 272000, "input": 2, "cache": 2, "output": 1.5 }
}
```

- `overInputTokens` — порог. Считается по **промпту целиком**: свежий вход, чтение из кэша и запись
  в кэш — кэшированная часть тоже отправлена. Запись в кэш легко потерять: Anthropic отдаёт её
  отдельным полем, и полный вход у него — сумма трёх (`cache_read_input_tokens +
  cache_creation_input_tokens + input_tokens`). Без неё первый длинный запрос — тот самый, что пишет
  в кэш, — проходил бы порог как короткий. Длина ответа порога не поднимает — в момент, когда цена
  определяется, ответа ещё нет.
- `input`, `cache`, `output` — множители, а не вторая таблица цен: вендор объявляет именно «во
  столько-то раз», и вторая таблица разошлась бы с первой при первом изменении базовой ставки.
  Не указан — единица.
- Множители применяются и к экономии на кэше, которую показывает отчёт: посчитанная по базовой
  ставке при действующей надбавке, она назвала бы число, которого не было ни в одном счёте.

Зачем это вообще: агентный цикл перечитывает контекст, и именно он в такой порог упирается. Без
объявления отчёт о расходе занижает счёт ровно вдвое и молчит об этом. **Каталоги-агрегаторы порог
обычно не указывают** — берите числа со страницы вендора.

## Цена по часу

Некоторые вендоры берут разную цену в разные часы. У DeepSeek с 10.09.2026 пик — 01:00–04:00 и
06:00–10:00 UTC с понедельника по пятницу, а всё остальное время — половина пиковой ставки
(api-docs.deepseek.com/quick_start/pricing).

```jsonc
"cost": {
  "input": 0.30, "output": 1.20, "cacheRead": 0.006,
  "timeOfDay": {
    "peakUtc": ["01:00-04:00", "06:00-10:00"],
    "peakDays": ["mon", "tue", "wed", "thu", "fri"],
    "offPeakFactor": 0.5
  }
}
```

- Ставки в `cost` — **пиковые**, вне пика все они умножаются на `offPeakFactor`. Пиковые, потому что
  вендоры называют внепиковую цену долей пиковой, и запись повторяет их прайс, а не пересчитывает.
- `peakUtc` — окна `ЧЧ:ММ-ЧЧ:ММ` по UTC, конец не входит; окно через полночь пишется как есть
  (`22:00-02:00`), конец суток — `24:00`. `peakDays` — трёхбуквенные английские дни (`mon` … `sun`);
  нет поля — окна действуют каждый день.
- Час берётся по моменту **окончания хода**. Где момент неизвестен (сводка налога контекста по
  разговору), считается по пиковой ставке: завышение безопаснее для потолка расходов, чем занижение.
- Множитель действует и на экономию на кэше, и вместе с надбавкой за длинный промпт.
- Битое окно или день роняет **весь блок** и называется в предупреждении при загрузке: цена по
  половине расписания выглядела бы верной и ошибалась молча.

## Срок годности цены

`costValidUntil` — дата (ISO, `2026-09-09`) или момент с зоной (`2026-09-09T16:00:00Z` — вендоры
объявляют сроки с часом), до которой верна цена в `cost`. Своего прайс-листа
у VibeIDEA нет и не будет: цена — факт о **вашем** договоре, а таблица в коде продукта врёт в день,
когда вендор поменял строку. Обратная сторона в том, что записанная цена протухает молча, и весь
отчёт о расходе продолжает выглядеть достоверным.

- День окончания **ещё действует**: цена меняется в конце суток.
- За неделю до срока и после него говорит **диагностика** (действие «Диагностика», строка «Срок
  годности цен»), а отчёт о расходе помечает суммы, посчитанные по просроченной цене. Обе
  строки называют `costNote`, если он записан: «сверьтесь с вендором» без адреса откладывают.
- Просроченная цена **не выбрасывается**: считать по устаревшей честнее, чем не считать вовсе —
  порядок величины она даёт верный, а молчащая колонка расхода не учит ничему.
- Срок без `cost` игнорируется: это описка в файле, а не находка.
- `costAfter` — цена **после** срока, в том же формате, что `cost`. Вендор объявляет её вместе
  с датой (Gemini 3.8 Flash: $0.75/$3.75 до 31.12.2026, затем ровно вдвое), а решение принимается
  именно по ней: предупреждение диагностики называет множитель подорожания по входной цене.
  По цене «после» ничего не считается — счёт про сегодня, а она про будущее.

## Окно контекста: чьё число верно

`contextWindow` в этом файле **сильнее** того, что сообщает провайдер, и это не упрямство. У
MiniMax-M3 клиент показывал 200 000, сторонний каталог models.dev — 512 000, а справочник вендора
— 1 000 000: три источника, три числа. Громче всех звучало первое, и оно оказалось догадкой клиента:
для незнакомого id Claude Code сам предполагает окно (code.claude.com/docs/en/model-config), а поля
«200000» у вендора нет.

Поэтому при чтении каталога моделей VibeIDEA запоминает, какое окно провайдер приписывает модели, и
**называет расхождение** в диагностике (строка «Окно контекста»), не исправляя ни одну из сторон:

- в конфиге больше — провайдер занижает, и вы недоиспользуете модель, за которую платите;
- в конфиге меньше — разговор упрётся в отказ раньше, чем вы ожидаете.

Округления вендоров (131 072 против 128 000) расхождением не считаются. Отдельного похода в сеть
ради этой проверки нет — цифра берётся из того же обхода каталога, который IDE делает и так.

## Часовой кэш промпта

`cacheTtl: "1h"` включает часовой кэш вместо пятиминутного (Anthropic-протокол). Смысл в цене: у
Fable 5.1 чтение кэша стоит $0.25 за 1M против $10 за свежий ввод, а запись в часовой кэш — $20
против $12.50 в пятиминутный. То есть часовой окупается там, где между ходами проходит больше пяти
минут: ревью, ожидание CI, отладка.

- Умолчание — пятиминутный кэш: он дешевле в записи, и включать часовой за пользователя мы не будем.
- При `1h` клиент сам добавляет бета-заголовок вендора (`extended-cache-ttl-2025-04-11`) — без него
  вендор молча оставит пять минут по цене часовой записи.
- Чужое написание (`"1 hour"`, `"60m"`) не принимается и считается несказанным: отправленное
  вендору, оно дало бы отказ на каждом запросе.
