# Спека: детектор нейрослопа и правки проекта (`.vibe/slop.json`)

> Скопируйте этот файл целиком своей LLM и попросите: «собери мне `.vibe/slop.json` для …» — этого документа достаточно.

## Что это

Нейрослопом здесь называются приметы машинного письма: словарь, который разросся в текстах моделей
(`бесшовный`, `синергия`, `delve`), пустые обороты, риторические заготовки вроде «это не X — это Y»,
голос ассистента, безымянные «исследования показывают», одинаковый ритм и оформление вместо
структуры. Одна примета ничего не доказывает, их набор узнаётся за пару фраз.

VibeIDEA читает прозу детерминированным детектором: одни и те же правила, одна и та же арифметика,
каждая находка — со строкой, найденным куском и способом исправить. Модель не участвует и ключ не
нужен. Каталог правил общий (русская и английская части), приезжает со сборкой и в проект не
копируется; проект говорит только, чем он от каталога отличается, — файлом `.vibe/slop.json`.

Где работает детектор:

| Где | Что делает |
|---|---|
| Гейт хода | после хода, в котором агент записал прозу (`.md`, `.mdx`, `.markdown`, `.txt`, `.rst`, `.adoc`), читает эти файлы; режимы ниже |
| Действие «Нейрослоп в тексте» | контекстное меню редактора: выделение или весь файл прозы; список находок, выбор выделяет найденный кусок |
| Инструмент `vibe_text_slop_check` | MCP-сервер IDE; аргумент `path` (путь в проекте) или `text`; ответ — счёт и находки |
| Дизайн-детектор, правило `copy-slop` | тексты живой страницы читаются теми же списками, одна находка на элемент |

Правки из `.vibe/slop.json` действуют везде, где работает детектор.

## Как начать

1. **Проверить текст.** Откройте файл `.md` или выделите абзац и выберите «Нейрослоп в тексте» в
   контекстном меню редактора. Агенту то же доступно инструментом `vibe_text_slop_check`.
2. **Включить гейт хода.** Settings → Tools → VibeIDEA → Агент, раздел «Нейрослоп в тексте»:
   - `off` — гейт молчит;
   - `notify` (по умолчанию) — строка в ленте, если записанный текст не прошёл проверку;
   - `enforce` — агент возвращается к тексту, пока тот не пройдёт, но не больше заданного числа раз
     (1–5, по умолчанию 2); после потолка решает человек.
   Текст, прошедший проверку, — тишина в любом режиме.
3. **Сказать, чем проект отличается от каталога.** Засеянный `.vibe/slop.json` пуст; в него пишут
   выключенные правила, настоящие термины проекта и свои правила (формат ниже).
4. **Чистить текст по навыку.** Навык `anti-slop` из `.vibe/skills/` описывает порядок правки:
   переписать, проверить детектором, отдать свежему рецензенту. Пайплайн `de-slop` из
   `.vibe/pipelines.json` делает то же двумя шагами: автор правит тексты, изменённые в рабочей копии,
   рецензент другого семейства моделей судит правку по диффу.

## Формат `.vibe/slop.json`

JSONC: комментарии и висячие запятые разрешены. Все поля необязательны.

```jsonc
{
  "version": 1,
  // Правила каталога, которые в этом проекте не действуют. Id правила написан в каждой находке.
  "disable": ["F5", "RU-D1"],
  // Настоящие термины проекта: находка, чей текст ЦЕЛИКОМ совпал с таким словом, не считается.
  // Звёздочка в конце слова — любое окончание.
  "allow": ["экосистем*", "платформ*"],
  // Свои правила в формате каталога. Правило с id из каталога заменяет его целиком.
  "rules": [
    { "id": "P1", "lang": "ru", "kind": "phrases", "severity": "minor",
      "name": "Канцелярит отдела", "fix": "Скажите, кто что делает.",
      "items": ["в разрезе", "в части касающейся", "осуществ* деятельност*"] },
    // Правило с id каталога заменяет его целиком, вместе со списком: так меняют вес и состав правила.
    { "id": "RU-W5", "lang": "ru", "kind": "words", "severity": "minor",
      "name": "Слова-пустышки", "fix": "Вычеркните.", "items": ["реально", "по сути", "как бы"] }
  ],
  // С какого счёта текст проходит проверку в этом проекте (по умолчанию 90).
  "passScore": 85
}
```

| Поле | Тип | Что значит |
|---|---|---|
| `version` | число | версия формата; сейчас `1` |
| `disable` | список строк | id правил, которые не действуют; регистр не важен |
| `allow` | список строк | слова и фразы проекта в записи каталога (`*` в конце слова, `ё` ловит и `е`) |
| `rules` | список правил | свои правила; одинаковые id внутри файла — работает первое |
| `passScore` | число | порог прохода |

Битое правило (нет `id`, незнакомый `kind` или `severity`, нечего искать, регулярка не компилируется)
отбрасывается с предупреждением, остальные работают. Предупреждения видны там, где работает
детектор: в ответе инструмента, внизу списка действия, в ленте. Файл, который не разбирается как
JSON, не применяется вовсе, и детектор работает по каталогу.

## Правило

| Поле | Обязательно | Что значит |
|---|---|---|
| `id` | да | уникальный идентификатор; находка называет его |
| `kind` | да | вид правила, см. ниже |
| `severity` | да | вес: `note`, `minor`, `major`, `blocker` |
| `lang` | нет | какие абзацы правило читает: `ru`, `en`, `any` (по умолчанию `any`) |
| `name` | нет | имя в находке; по умолчанию `id` |
| `fix` | нет | подсказка «как исправить» в находке |
| `items` | для `words`, `phrases`, `opener` | список слов или фраз |
| `patterns` | для `regex`, `density` | регулярные выражения в записи, которую понимают обе IDE (см. `regex`) |
| `caseSensitive` | нет | различать регистр; по умолчанию нет |
| `minCount` | нет, для `density` | с какого числа вхождений правило считает частоту; по умолчанию 3 |
| `thresholdPer1000` | нет, для `density` | с какой частоты на тысячу слов абзацев этого языка это находка |

Виды:

- **`words`** и **`phrases`** — список. Совпадение целым словом: `рост` не находится внутри `проросток`.
  Звёздочка в конце слова — любое окончание (`синерги*` ловит «синергия», «синергией»), пробел —
  любой пробельный промежуток, `ё` в списке ловит и `е`, апостроф — оба вида. Разница между видами
  только в смысле: слово или оборот.
- **`opener`** — слова в **начале предложения**, за которыми идёт запятая, двоеточие или пробел:
  «Кроме того, …» в начале — находка, «и кроме того» посреди фразы — нет.
- **`regex`** — регулярные выражения по всему тексту. Флаги: без учёта регистра (если не
  `caseSensitive`), Юникод, `^` и `$` — границы строк. Файл правок читают две IDE: VibeIDEA движком
  Java, VibeIDE — JavaScript. Поэтому шаблон пишется в записи, которую понимают обе:
  - `\b`, `\w` и `\d` в обеих юникодные и видят кириллицу;
  - буква — `\p{L}`, а не `[а-я]`;
  - письменность — `\p{sc=Cyrillic}`, а не `\p{IsCyrillic}`: вторая запись есть только в Java;
  - нельзя: притяжательные квантификаторы (`*+`, `++`), атомарные группы `(?>…)`, флаги внутри шаблона
    `(?i)`, `\A`, `\Z`, `\Q…\E`, `\h`, `\R`, пересечения классов `[a-z&&[^b]]`. В JavaScript их нет,
    и VibeIDE отбросит такой шаблон с предупреждением.
- **`density`** — приём, обычный один раз и ставший привычкой: `patterns` считаются по тексту, и
  находка одна на документ, если вхождений не меньше `minCount` и частота на тысячу слов не ниже
  `thresholdPer1000`. Находка указывает на первое вхождение и перечисляет строки остальных.
- **Проверки в коде** — `invisible`, `rhythm-uniform`, `rhythm-fragments`, `decorative-bold`,
  `heading-stub`, `title-case`. Логика живёт в IDE, у правила только имя, вес и подсказка. Свои
  такие правила смысла не имеют; объявить заново правило каталога этого вида — способ сменить его
  вес или подсказку.

## Строка, которую надо оставить

С находкой можно не согласиться. Директива в самом тексте:

```markdown
<!-- slop-ignore RU-W2 — цитата из договора, её не переписывают -->
```

- Директива в конце строки покрывает эту строку и следующую.
- Директива на отдельной строке покрывает абзац под ней (до пустой строки).
- Id можно перечислить через запятую; `ALL` выключает все правила.
- Причина после тире не обязательна для детектора, но нужна читателю: подавление без причины через
  месяц никто не сможет ни снять, ни оправдать.

## Как считается

**Что не читается.** Блоки кода (```` ``` ```` и `~~~`), код в обратных кавычках, строки с отступом в четыре
пробела, цитаты (`>`), адреса ссылок и голые URL, фронтматтер, HTML-комментарии и текст в кавычках
(`"…"`, `“…”`, `«…»`, `„…“`): это чужие слова, подписи интерфейса или примеры. Текст ссылки —
проза и читается. Невидимые символы (`U1`) ищутся и в коде: пробел нулевой ширины в сниппете
сломает того, кто его скопирует.

**Переводы строк.** Windows (`\r\n`) и одиночный `\r` читаются как обычный перевод строки: фронтматтер и
код узнаются так же, номера строк те же.

**Язык.** Каждый абзац относится к русскому или английскому по обычным словам в нём; имена в
camelCase, аббревиатуры, числа и пути не голосуют. В абзаце меньше чем из четырёх обычных слов
язык берётся по документу. Правило с `lang: ru` читает только русские абзацы, `en` — английские,
`any` — все. Длинное тире поэтому — примета английской прозы и обычная грамматика русской.

**Одна проблема — одна находка.** Если несколько правил нашли один и тот же кусок, считается одно,
самое тяжёлое: «является неотъемлемой частью» — раздувание значимости, и слово «неотъемлемой» внутри
не становится второй находкой.

**Счёт.** Текст начинает со 100. Первая находка правила стоит по весу, каждая следующая того же
правила — меньше, и одно правило не может снять больше трёх своих первых цен:

| Вес | Первая находка | Каждый повтор | Потолок правила |
|---|---|---|---|
| `note` | 1 | 0.5 | 3 |
| `minor` | 5 | 2 | 15 |
| `major` | 15 | 5 | 45 |
| `blocker` | 30 | 10 | 90 |

Текст **проходит** при счёте от 90 (или от `passScore` проекта) и без находок тяжелее `minor`:
одна находка `major` не пропускает текст при любом счёте.

**Счёт — пол, а не вердикт.** До 100 можно дойти, написав текст настолько пресный, что в нём нечего
найти. Детектор не читает смысл: выдуманную цифру, выпавшую оговорку и сломанный довод он не видит.
Их видит рецензент — для этого навык `anti-slop` и пайплайн `de-slop`.

## Тексты страницы (`copy-slop`)

Дизайн-детектор прогоняет видимый текст каждого элемента страницы через списки и шаблоны каталога
(виды `words`, `phrases`, `opener`, `regex`; ритм и оформление читают документы, а не кнопку).
Одна находка на элемент — по самой тяжёлой примете: заголовок с тремя штампами переписывают один раз.
Правило вкусовое, пол не пробивает; проект может принять его как идентичность в `.vibe/design/`
(раздел «Детектор», строка `- copy-slop — слоган бренда`). Старый id `marketing-promise`
по-прежнему принимается и означает `copy-slop`. Правки `.vibe/slop.json` действуют и здесь.

## Правила каталога

Id, язык, вес и имя, как они приходят в находке. Полные списки и шаблоны — в каталоге сборки.

Всего правил: 64.

| Id | Язык | Вес | Имя |
|---|---|---|---|
| `U1` | любой | `major` | Невидимый символ |
| `F2` | любой | `minor` | Эмодзи вместо структуры |
| `F5` | любой | `note` | Жирный посреди фразы |
| `F6` | любой | `note` | Заголовок над одной-двумя фразами |
| `F7` | любой | `note` | Каждое Слово С Заглавной |
| `R1` | любой | `note` | Метроном: предложения одной длины |
| `R2` | любой | `minor` | Рубленые фразы подряд |
| `D2` | любой | `minor` | Много риторических вопросов |
| `RU-W1` | ru | `minor` | Слова-штампы |
| `RU-W2` | ru | `major` | Пустые фразы |
| `RU-W3` | ru | `minor` | Шаблонное начало фразы |
| `RU-W4` | ru | `major` | Голос ассистента |
| `RU-W5` | ru | `note` | Слова-пустышки |
| `RU-W6` | ru | `major` | Безымянный авторитет |
| `RU-W7` | ru | `note` | Ложная крайность |
| `RU-W8` | ru | `minor` | Размытое «все» |
| `RU-S1` | ru | `major` | Отрицательный параллелизм |
| `RU-S2` | ru | `major` | Прочистка горла |
| `RU-S3` | ru | `major` | Фальшивое откровение |
| `RU-S4` | ru | `major` | Риторическая подводка |
| `RU-S5` | ru | `minor` | Раскрытие через двоеточие |
| `RU-S6` | ru | `minor` | Оценочный хвост |
| `RU-S7` | ru | `major` | Раздувание значимости |
| `RU-S8` | ru | `minor` | Толкование за читателя |
| `RU-S9` | ru | `minor` | Канцелярит вместо глагола |
| `RU-S10` | ru | `minor` | Ложное действующее лицо |
| `RU-S11` | ru | `minor` | Рубленые фразы для драмы |
| `RU-S12` | ru | `major` | Фальшивая глубина в конце |
| `RU-S13` | ru | `major` | Итоговый пересказ |
| `RU-S14` | ru | `minor` | Размытое утверждение |
| `RU-S15` | ru | `note` | Нагромождение оговорок |
| `RU-S16` | ru | `major` | Шаблон «половина истории» |
| `RU-S17` | ru | `minor` | Вычурный регистр |
| `RU-F4` | ru | `minor` | Рекламная пара прилагательных |
| `RU-D1` | ru | `minor` | Противопоставление на каждом шагу |
| `EN-W1` | en | `minor` | Slop vocabulary |
| `EN-W2` | en | `major` | Slop phrases |
| `EN-W3` | en | `minor` | Canned sentence opener |
| `EN-W4` | en | `major` | Assistant voice |
| `EN-W5` | en | `note` | Filler intensifier |
| `EN-W6` | en | `major` | Weasel attribution |
| `EN-W7` | en | `note` | Lazy extreme |
| `EN-S1` | en | `major` | Negative parallelism |
| `EN-S2` | en | `major` | Throat-clearing opener |
| `EN-S3` | en | `major` | Faux-insight setup |
| `EN-S4` | en | `major` | Rhetorical setup |
| `EN-S5` | en | `minor` | Colon reveal |
| `EN-S6` | en | `minor` | Trailing -ing analysis |
| `EN-S7` | en | `major` | Importance puffery |
| `EN-S8` | en | `minor` | Interpretive metadiscourse |
| `EN-S9` | en | `minor` | Fake-strong verb |
| `EN-S10` | en | `minor` | False agency |
| `EN-S11` | en | `minor` | Dramatic fragmentation |
| `EN-S12` | en | `major` | Fake-profound kicker |
| `EN-S13` | en | `major` | Summary-recap ending |
| `EN-S14` | en | `minor` | Vague declarative |
| `EN-S15` | en | `note` | Hedge stacking |
| `EN-F1` | en | `minor` | Em dash or en dash |
| `EN-F3` | en | `note` | Rule of three |
| `EN-F4` | en | `minor` | Promotional adjective stack |
| `EN-W8` | en | `minor` | Vague population |
| `EN-S16` | en | `major` | Partition template |
| `EN-S17` | en | `minor` | Stilted register |
| `EN-D1` | en | `major` | Contrastive tail |

## Чего детектор не делает

- Не переписывает текст: он называет находку и способ исправить, правит человек или агент.
- Не судит об авторстве: находка говорит о шаблоне в тексте, а не о том, кто его написал.
- Не видит смысла: выдумку, сдвиг смысла и выхолащивание ловит рецензент, а не правила.
- Не читает код: комментарии в исходниках — забота гейтов проекта.

## Происхождение

Метод и английская часть каталога — `misbahsy/anti-ai-slop` (MIT): маскирование не-прозы, списки и
шаблоны, частоты для приёмов-привычек, ритм, оформление и фиксированная арифметика. Русская часть
каталога, язык по абзацу и начало фразы как граница для `opener` сделаны для VibeIDEA. Каталог
общий с продуктами семейства и живёт в общем наборе `.vibe` (`slop/catalog.jsonc`).
